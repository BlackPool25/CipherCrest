# Git LFS & Large Files — SecureMailScope audit 2026-08-26, doc 2026-08-26 docs/LARGE_FILES.md
# Decision: NO LFS needed yet (<1M all non-wheelhouse tracked files). Wheelhouse stays gitignored air-gap USB (NOT LFS).
# Audit: models/*.pkl 276K total (124K+76K+76K <5M), lab/pcaps 1KB each (45 pcap ~45K), eval/*.png 42K+17K — all <5M GitHub LFS threshold.
# .git 345M pack (history 1647199 retains blob until filter-repo; HEAD 0 via b9d18b4 git rm --cached, gc loose 0). See docs/LARGE_FILES.md table + turnup scripts/turnup.sh --check.
# For future stretch (MicroAE 27-8-1 ~50M, torch 180M) — RECOMMENDED GitHub Releases 2GB/asset free (scripts/download_models.sh) OR uncomment LFS below + git lfs install && git lfs track — DO NOT `git lfs migrate` without user approval.
# To enable LFS: git lfs install && git lfs track "models/**/*.pkl" "lab/pcaps/**/*.pcap" "eval/*.png" "lab/reassembled/**/*.bin"
# Verify: bash scripts/turnup.sh --check && bash scripts/download_models.sh --check && git lfs ls-files (empty now) && git ls-files | grep -E "^wheelhouse/|^dashboard/dist" || echo "HEAD clean" 

# Future LFS patterns — COMMENTED until >5M file appears (MicroAE/torch stretch)
# Uncomment to activate:
# models/**/*.pkl filter=lfs diff=lfs merge=lfs -text
# models/*.pkl filter=lfs diff=lfs merge=lfs -text
# lab/pcaps/**/*.pcap filter=lfs diff=lfs merge=lfs -text
# lab/pcaps/*.pcap filter=lfs diff=lfs merge=lfs -text
# eval/*.png filter=lfs diff=lfs merge=lfs -text
# lab/reassembled/**/*.bin filter=lfs diff=lfs merge=lfs -text

# Keep wheelhouse and dashboard/dist NEVER in LFS — wheelhouse is air-gap USB gitignored, dist is built in CI (tracked via force-add in 1647199 should be ignored going forward)
# wheelhouse/** filter=lfs -DO NOT ADD- (stay gitignored)
# dashboard/dist/** filter=lfs -DO NOT ADD- (build artifact, `npm run build` in CI)

# Binary handling — ensure diff behaves
*.pkl binary
*.pcap binary
*.png binary
*.bin binary
