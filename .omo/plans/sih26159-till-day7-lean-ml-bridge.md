# sih26159-till-day7-lean-ml-bridge - Work Plan

## TL;DR (For humans)

**What you'll get:** By end of Day7 the replay-to-ML bridge is hardened and lean training has started: 31-row jittered corpus (10 base +21 jitter) with 31 env groups cleanly split for training, a frozen 28-feature contract that can build vectors for both tree and autoencoder paths, and a first XGB model calibrated with honest uncertainty plus an ECOD anomaly detector — all wired into the API so the dashboard shows calibrated risk without re-parsing, with every offline bundle and evidence page still green, plus a sophisticated OSS-grade README with Mermaid architecture.

**Why this approach:** Keep the Day5-6 replay lane frozen as wiring checks and expand only the dataset and contracts on Day7 (3 slices per jitter family → ~30 rows, 31 envs, D1 12/D2 8/D3 5), then start the smallest honest ML that is defensible at n_eff=10 — Platt scaling with cv=2 (isotonic needs >1000) and ECOD with contamination invariance — so Day8-10 can iterate on rank quality (NDCG) rather than debugging data plumbing.

**What it will NOT do:** No heavy torch/MicroAE 27-8-1 (needs n≥50 and breaks wheelhouse 350M), no isotonic or raw JA4 as a feature, no quarantine/siem/milter/ARF live gateway, and no Day10 stretch tables — Day7 lands a SYSTEM green plus a calibrated ML shell, not a final 8/8.

**Effort:** Large (11 todos, 4 waves, ~12h wall, 4-agent parallel)
**Risk:** Medium — XGB at n_eff=10 is rank-only territory (ECE CI ±0.10 at 500-boot family-level) and ECOD ROC must be asserted as a point not a CI; mitigated by family-level bootstrap, Platt-only guard, and explicit n_eff disclosure
**Decisions to sanity-check:** Lean 30 (21 jitter +10 base =31 envs, not minimal 17 nor weberblog-augmented 30), XGB Platt cv=2 lean not cv=3 stretch, ECOD primary only (IF correction deferred), wheelhouse stays lean 345M <350 with torch commented, EVIDENCE_Day7 SYSTEM green + ML LEARN shell Day8-10

Your next move: approve plan → `/start-work sih26159-till-day7-lean-ml-bridge` (this plan already passed high-accuracy momus+oracle review — fixes applied). Full execution detail follows below.

---

> TL;DR (machine): Large / Medium — Till Day7 bridge: jitter 31 (21+10) → splits 31 → 28-col TDD → censys 20 re-verified → XGB Platt cv2 500-boot + ECOD ROC>0.60 → API wiring + wheelhouse <350 + EVIDENCE_Day7 SYSTEM 5/8 + ML shell + README Mermaid

## Scope
### Must have
- Lab: `lab/pcaps/jittered/*.pcap` expanded to 21 jitter files (7 families ×3 slices jitter1/2/3), `lab/manifest.json` + `lab/LEDGER.md` updated with `environment_id` (`family-0X__postfix3.9_loss0` / `family-0X__jitter{1..3}_loss5`), `capture_epoch` `2026-08-27T00:00:00Z`, `source_id` uuid, `docker_image_sha256` dummy-postfix3.9, `tshark_version` 4.2.0, `coverage_ratio` 1.0 (jittered 0.95-1.0 logged), `pre_tls_buffer_len`/`injection_possible` via `reassemble.py`
- Assessment: `assessment/splits.json` regenerated 31 envs with `groups_by_env`, `D1_train_groups ~12`, `D2_val ~8`, `D3_locked ~5` (09,10 + jitter extras), `D_prior_groups` 20 `censys_prior_*` disjoint, `D5_temporal_same_env` train `2026-08-27T00:00:00Z` test `2026-09-03T00:00:00Z` `env_id_frozen:true`, `StratifiedGroupKFold(n_splits=5, groups=environment_id)` contract, `unique≥5` + `max/min<3` + `locked ∩ (train∪val)==∅` + `prior ∩ risk==∅` + `family_id` forbidden
- Assessment contracts: `assessment/features.py` hardened — `FEATURES_28==28` (21 base 6 categorical native `version/cipher_strength/kex/starttls_mode/port/cert_missing_reason` +15 numeric incl `ja4_rarity` only +7 `miss_indicator_*`), `build_vector(flow, mode='xgb'|'ae')` 28 NaN-free deterministic, `_CATEGORICAL_6` frozenset, `XGB_CATEGORICAL_PARAMS` `tree_method hist device cpu enable_categorical True max_depth 3-4 n_estimators 80 reg_alpha 1.0 reg_lambda 2.0`, `ALLOWED_RISK_FEATURES` ja4 not in / ja4_rarity in mirror `shared/ja4_rarity.py` + `analyzer/jas.py`
- Shared: `shared/fixtures/censys_sampled_200.json` 20 rows re-verified — all `prior_flag:true`, `cert.chain_valid is None`, `cert.days_to_expiry is None`, `cert.san_match is None`, `ja4_rarity` span `0.02..0.99` (min≤0.2 max≥0.8), `miss_indicators` 1, `dataset_caveat` prior-only, `cert_missing_reason` null, `tls.ja4_rarity` 0..1, 11/28 cols caveat wired
- Assessment ML lean Day7: `assessment/risk_model.py` `XGBClassifier(tree_method='hist', device='cpu', enable_categorical=True, max_depth=3-4, n_estimators=80, reg_alpha=1.0, reg_lambda=2.0, random_state=42, deterministic=True)` + `CalibratedClassifierCV(method='sigmoid', cv=2)` Platt only (no isotonic) + `PYTHONHASHSEED=0 OMP_NUM_THREADS=6` + family-level bootstrap 500 ECE CI + `eval/calibration_curve.png` + permutation importance `n_repeats=10` top3 → `models/risk_clf.pkl` (lean, <5M)
- Assessment ML lean Day7: `assessment/anomaly_model.py` `ECOD(contamination=0.10, n_jobs=1)` primary fit on lab 30 (risk) + prior diversity 35% slice (20 censys) for unsupervised background, `contamination invariance` test (scores invariant 0.05→0.2 only labels shift), ROC point>0.60 vs rule weak families as pseudo-label, `models/anomaly.pkl` wiring `FlowVerdict.assessment.anomaly_score`
- API: `api/app.py` wiring — load `models/risk_clf.pkl`/`anomaly.pkl` if present (lazy load at import, fallback to `score()` if missing), enrich `POST /analyze` `FlowVerdict.assessment.calibrated_prob` (max predict_proba Platt) + `anomaly_score` (ECOD decision_scores), `FlowVerdict.model_validate` hard-fail before `upsert_flows`, `GET /flows` <50ms from SQLite JSONB `query_all`, cold-start <3s
- Offline/Eval: wheelhouse lean re-verified `du -m wheelhouse <350` `--only-binary=:all: --prefer-binary` (xgboost manylinux 192M + pyod 2.0.5, no torch), Vite `dashboard/dist/assets/*.js` `gzip <3670016`, SYSTEM 5/8 stays green (STARTTLS F1>95% lossy/weberblog, cipher 100% >98%, cert prec1.000 >90% stratified, weak 100% 23-check, JSON 20/20), `eval/EVIDENCE_Day7.md` (SYSTEM 5/8 green + ML LEARN shell), `shared/progress.md` + `assessment/LEDGER.md` + `lab/LEDGER.md` Day7 rows 🟢, `.github/workflows/ci.yml` hard-fail guards (isotonic/ja4/grouping/prior/chain_valid/san_match + feature 28)
### Must NOT have (guardrails, anti-slop, scope boundaries)
- No `torch==2.4.0+cpu` 180M nor MicroAE 27-8-1 in `requirements.txt`/`wheelhouse` lean — stretch gated `n≥50+ahead` deferred to Day8-10; `requirements.txt` must comment `# stretch: torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu`
- No `isotonic` at n<100 (Platt `method='sigmoid'` only) — `! grep -rq "isotonic" assessment/` hard-fail; Platt outperforms isotonic at n<2000 per small-cal benchmark (Niculescu+2013, Zenodo 2026)
- No raw `ja4` as risk feature (only `ja4_rarity` 0..1 numeric) — `ALLOWED_RISK_FEATURES` asserts `ja4 not in` + CI grep guard; raw hash spoofable via curl-cffi
- No `family_id` grouping (only `environment_id`) — `StratifiedGroupKFold(groups=environment_id)` contract + `grep family_id splits.json && exit 1`; `environment_id` not in `FEATURES_28`
- No `quarantine.py`/`siem.py`/`arf.py`/`QuarantineConsole`/`digest cron`/`milter 127.0.0.1:10025`/`mockdns` live beyond `shared/data/mta-sts-fixture.json`/`dane-tlsa-fixture.json` fixture read — cut-order 0 stretch deferred Day10
- No quarantine table raw body attach — `api/db.py` `flows(flow_id PRIMARY KEY, data TEXT)` only `INSERT OR REPLACE` without body; `quarantine_id` None lean
- No B1-B5 per-split annex, no overall pooled ML validity table, no per-split ECE/PR-AUC CI beyond Day7 lean point/500-boot — Day10 stretch; Day7 is ONE SYSTEM table + ML shell (ML 🟡)
- No `transformer BERT` on raw pcap, no `as any`/`unwrap`/`panic`, no file >250 LOC without split (policy 217, features 180, db 120 stay lean)

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: TDD for contracts (features/build_vector/splits/censys guards) — write failing test first then implement to green; tests-after for ML lean (augment existing `assessment/tests/test_risk_ablation.py`, `test_anomaly_hybrid.py`, `api/tests/test_api_e2e.py`). Framework: `pytest -q --junitxml=<attemptDir>/task-N-*.junit.xml` + `python -m py_compile` + `pip install --no-index --find-links wheelhouse --only-binary=:all: --dry-run` + `du -m wheelhouse` + `pytest --collect-only` wiring
- Evidence: `.omo/evidence/ulw/<session>/<goalId>/a<attempt>/task-<N>-sih26159-till-day7-lean-ml-bridge.*` (outside ulw-loop `.omo/evidence/task-<N>-sih26159-till-day7-lean-ml-bridge.*`). Each todo produces junit xml + log + png/json fixture. No human CLI witness required.
- Family-level honesty: bootstrap resamples families not rows (n_eff=10 disclosed), `WEAK SUPERVISION` sentence verbatim in every artefact

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.

- Wave 1 (Day7 AM 09:00-12:00, 3h): T1 jitter 21 expansion (patched) + T3 features TDD (max_depth 4) + T4 censys re-verify — all independent (T1 touches lab pcaps, T3 touches assessment/features, T4 reads fixtures). T3 must land before T5/T6.
- Wave 2 (Day7 AM 12:00-15:00, 3h): T2 splits.json 31 envs frozen (blocked by T1 needing 21 envs) — then parallel T8 wheelhouse re-verify (pinned 1.7.6) + T9 ledger Day7 scaffolding. T2 serializes after T1.
- Wave 3 (Day7 PM 15:00-18:00, 3h): T5 XGB Platt cv2 lean max_depth 4 (blocked by T2+T3, needs 31 envs + 28-col vector) + T6 ECOD primary lean contamination invariance (blocked by T2+T3) in parallel — both write models/*.pkl, do not interfere (different pkls).
- Wave 4 (Day7 PM 18:00-20:30, 2.5h): T7 API wiring exact insertion point (blocked by T5+T6 needing .pkl) + T10 EVIDENCE_Day7 + T11 CI guards + T12 README Mermaid — T10 blocked by T1+2+3+5+6+7+9, T11 blocked by T2+3+4+5+6+7 and parallel with T10/T12, T12 blocked by T1+2+3+5+6+7+10 (needs architecture + calibration curve). Then F1-F4 final verification wave (all parallel, must APPROVE).

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 jitter 21 patched | - | 2,10,11,12 | 3,4 |
| 2 splits 31 frozen | 1 | 5,6,10,11,12 | 8,9 |
| 3 features TDD 4 | - | 5,6,11,12 | 1,4 |
| 4 censys re-verify | - | 11 | 1,3 |
| 5 XGB Platt cv2 4 | 2,3 | 7,10,11,12 | 6 |
| 6 ECOD primary | 2,3 | 7,10,11,12 | 5 |
| 7 API wiring exact | 5,6 | 10,11,12 | 8,9 |
| 8 wheelhouse re-verify pinned | - | 11 | 2,7 |
| 9 ledgers Day7 | 2 | 10,11,12 | 7,8 |
| 10 EVIDENCE_Day7 | 1,2,3,5,6,7,9 | 11,12 | 11 |
| 11 CI guards | 2,3,4,5,6,7 | 12 | 8,10 |
| 12 README Mermaid | 1,2,3,5,6,7,10 | - | 11 |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. lab jitter expansion — 7 families ×3 slices (21 jitter → 31 envs/rows) with scapy GREASE+expiry+ja4_rarity (patched jitter_slices.py)
  What to do / Must NOT do: Patch `lab/scripts/jitter_slices.py` first (blocking fix per momus High): change `lab/scripts/jitter_slices.py:15 GREASE=0x0a0a` → `from shared.ja4_rarity import GREASE_VALUES` + replace `ciphers=[GREASE]+ciphers` with `ciphers=[next(iter(GREASE_VALUES))]+ciphers` or random sample from GREASE_VALUES (RFC8701 16 values `0x0a0a…0xfafa` — do not invent beyond 16), and fix `lab/scripts/jitter_slices.py:81 out=OUT_DIR/f"family-{fam}-jitter-01.pcap"` → `out=OUT_DIR/f"family-{fam}-jitter-{idx:02d}.pcap"` and `REASM_DIR/f"family-{fam}-jitter-01.bin"` → `REASM_DIR/f"family-{fam}-jitter-{idx:02d}.bin"` so loop `for s in range(1,args.slices+1): make_pcap(fam,s, ...)` no longer overwrites same `jitter-01`. Then run `python -m lab.scripts.jitter_slices --slices 3 --families 02,03,04,05,07,08,10` (creates 14 new pcaps beyond existing 7 jitter-01: jitter-02/03 per family via `jittered_hello(random GREASE from GREASE_VALUES + cipher shuffle + sigalg variation + expiry jitter ±5d + ja4_rarity sampled from shared/data/censys_top_ja4.json)`). Each new pcap is `lab/pcaps/jittered/family-0X-jitter-0Y.pcap` with scapy `wrpcap` + `lab/reassembled/family-0X-jitter-0Y.bin` 120B hello (note `lab/reassembled/` via `lab/scripts/jitter_slices.py:12 REASM_DIR=ROOT/"lab"/"reassembled"` distinct from `lab/reassembler/reassemble.py:5-tuple` code). Update `lab/manifest.json` for each new env `family-0X__jitterY_loss5` with `environment_id`, `capture_epoch 2026-08-27T00:00:00Z`, `source_id uuid`, `docker_image_sha256 dummy-postfix3.9`, `tshark_version 4.2.0`, `client sender`. Update `lab/LEDGER.md` per-family rows with `pcap sha256` + `STARTTLS` + `Cipher (+GREASE sha384)` + `Cert` + `tshark parity PASS` + `coverage_ratio 1.0` + `source_id` + `n_eff 1` + comment `# jitter slice cipher-shuffle GREASE 0x0a0a sigalg sha384 expiry +-5d ja4_rarity sampled`. TDD: create `lab/tests/test_jitter_slices.py` failing first that asserts `len(glob jittered/*.pcap)==21` + `all env_id distinct` + `pcap valid D4 C3 B2 A1` + `idx 02/03 files exist`. MUST NOT invent GREASE beyond RFC8701 16 values (use shared/ja4_rarity.py GREASE_VALUES), MUST NOT touch family 01,06,09 base (PASS/opaque/cleartext keep distinct), MUST keep `utils` idempotency (rerun doesn't duplicate LEDGER rows). Verify via `tshark -T json -o tcp.reassemble_out_of_order:true -o tls.desegment:true` still parses.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 2,10,11
  References (executor has NO interview context - be exhaustive): lab/scripts/jitter_slices.py:1-139 FAMILY_CFG + jittered_hello + make_pcap + update_manifest + update_ledger, lab/manifest.json 10 families env_id capture_epoch source_id, lab/LEDGER.md 10 rows + jitter comment, lab/pcaps/jittered/*.pcap existing 7, lab/pcaps/family-*.pcap 10 base, shared/data/censys_top_ja4.json ja4 rarity sampling, lab/reassembler/reassemble.py _compute_pre_tls_buffer
  Acceptance criteria (agent-executable): `ls lab/pcaps/jittered/*.pcap | wc -l` ==21 and `python -c "import json,glob; m=json.load(open('lab/manifest.json')); assert len([k for k in m if 'jitter' in m[k].get('environment_id','')])>=14"` and `python -c "import pathlib; assert all(pathlib.Path(f'lab/pcaps/jittered/family-0{f}-jitter-02.pcap').exists() for f in ['02','03','04'])"` and `pytest lab/tests/test_jitter_slices.py -q` green
  QA scenarios (name the exact tool + invocation): happy - `python -m lab.scripts.jitter_slices --slices 3 --families 02,03,04,05,07,08,10 && pytest lab/tests/test_jitter_slices.py -xvs` → 21 pcaps validated; failure - corrupt one jitter pcap header → `_is_malformed` not crash but reassembler returns stub; edge - rerun jitter_slices idempotency → LEDGER rows not duplicated (grep jitter count stable). Evidence .omo/evidence/ulw/.../task-1-sih26159-till-day7-lean-ml-bridge.junit.xml + `ls -lh lab/pcaps/jittered/` log
  Commit: Y | feat(lab): jitter 21 expansion 31 envs 3 slices per family

- [x] 2. assessment/splits.json regeneration — 31 envs D1 12/D2 8/D3 5/D_prior 20 disjoint temporal frozen (enumerated frozen)
  What to do / Must NOT do: Regenerate `assessment/splits.json` from new 31 envs: read `lab/manifest.json` keys (10 base `family-0X__postfix3.9_loss0` + 21 jitter `family-0X__jitter{1..3}_loss5` =31) → collect `all_environment_ids` 31 + `groups_by_env: {env: [flow_id]}` where flow_id derived from pcap basename (groups_by_env len 31). Freeze assignment exactly (no executor judgment): `D1_train_groups` 12 envs = `["family-01__postfix3.9_loss0","family-02__postfix3.9_loss0","family-03__postfix3.9_loss0","family-04__postfix3.9_loss0","family-05__postfix3.9_loss0","family-02__jitter1_loss5","family-02__jitter2_loss5","family-03__jitter1_loss5","family-03__jitter2_loss5","family-04__jitter1_loss5","family-04__jitter2_loss5","family-05__jitter1_loss5"]`, `D2_val_groups` 8 envs = `["family-06__postfix3.9_loss0","family-07__postfix3.9_loss0","family-08__postfix3.9_loss0","family-05__jitter2_loss5","family-05__jitter3_loss5","family-07__jitter1_loss5","family-07__jitter2_loss5","family-08__jitter1_loss5"]`, `D3_locked_groups` 5 envs = `["family-09__postfix3.9_loss0","family-10__postfix3.9_loss0","family-07__jitter3_loss5","family-08__jitter2_loss5","family-10__jitter2_loss5"]` (ratio 12/5=2.4 <3, unique≥5; if executor needs 10 extras for 21 jitter remainder, keep D3 5 not 6 to avoid 12/4=3.0 borderline), `D_prior_groups` 20 `censys_prior_*` disjoint, `D5_temporal_same_env: {train_epoch: "2026-08-27T00:00:00Z", test_epoch: "2026-09-03T00:00:00Z", env_id_frozen: true, note: "D5 synthetic until Day10 real T2 pcap"}`. TDD: create `assessment/tests/test_splits.py` extensions failing first asserting `len(all_envs)==31` + `len(groups_by_env)==31` + `len(D1)+len(D2)+len(D3)==25 risk groups` + `unique≥5` + `max/min<3` (12/5=2.4) + `not D3 ∩ (D1∪D2)` + `not D_prior ∩ D1` + `D5 train!=test env_frozen==True` + `family_id not in file` + `! grep isotonic` + `python -c "assert 'family_id' not in open('assessment/splits.json').read()"`. MUST NOT assign risk labels to censys rows, MUST keep prior_flag disjoint, MUST use environment_id not family_id, MUST keep StratifiedGroupKFold(n_splits=5 groups=environment_id) contract (5-fold outer vs Platt inner cv=2 are distinct).
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 5,6,10,11
  References (executor has NO interview context - be exhaustive): lab/manifest.json 31 envs, lab/LEDGER.md env_id rows, assessment/splits.json 17 envs existing, assessment/tests/test_splits.py, shared/schemas.py environment_id lineage, lab/pcaps/jittered/*.pcap 21, shared/fixtures/censys_sampled_200.json 20 prior, .omo/plans/sih26159-securemailscope-implementation.md §4a.3 Charter + §7 grouping guards
  Acceptance criteria (agent-executable): `test -f assessment/splits.json && python -c "import json; s=json.load(open('assessment/splits.json')); assert len(s['all_environment_ids'])==31"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert len(s['D1_train_groups'])==12 and len(s['D2_val_groups'])==8 and len(s['D3_locked_groups'])==5"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s['D3_locked_groups']) & (set(s['D1_train_groups'])|set(s['D2_val_groups'])) and max(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))/min(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))<3"` and `pytest assessment/tests/test_splits.py -q` green
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_splits.py::test_groups_disjoint -xvs` → 31 envs disjoint; failure - tamper splits to overlap D_prior with D1 → guard fails; edge - `python -c "import json; s=json.load(open('assessment/splits.json')); assert s['D5_temporal_same_env']['train_epoch']!=s['D5_temporal_same_env']['test_epoch']"` true. Evidence .omo/evidence/ulw/.../task-2-sih26159-till-day7-lean-ml-bridge.junit.xml + splits.json sha256
  Commit: Y | feat(assessment): splits 31 envs D1 12/D2 8/D3 5 prior disjoint

- [x] 3. assessment/features.py TDD hardening — 28-col contract + build_vector(mode xgb/ae) + XGB categorical guards (max_depth 4 frozen, starttls mapping fixed)
  What to do / Must NOT do: Harden `assessment/features.py` via TDD: write failing `assessment/tests/test_features.py` first asserting `len(FEATURES_28)==28` (`_BASE_21 21 +_MISS_7 7`), `_BASE_21` contains 6 categorical `version/cipher_strength/kex/starttls_mode/port/cert_missing_reason` +15 numeric, `FEATURES_28` excludes `ja4` includes `ja4_rarity`, `environment_id not in FEATURES_28`, `XGB_CATEGORICAL_PARAMS` frozen `tree_method hist device cpu enable_categorical True max_depth 4 n_estimators 80 reg_alpha 1.0 reg_lambda 2.0` (standardized 4 per momus High — assessment/features.py:33-41 asserts 4), `ALLOWED_RISK_FEATURES == shared/ja4_rarity` whitelists `ja4 not in`/`ja4_rarity in`, `build_vector(flow, mode='xgb')` returns 28 NaN-free deterministic (categorical codes vs -1 missing, ja4_rarity 0..1, days_to_expiry clamp -365..3650, pubkey_bits 4096 norm for ae mode), `build_vector(flow, mode='ae')` categorical normalized 0..1 + miss_flags correct. Verify `_encode_categorical starttls_mode` mapping `{"upgrade":0,"implicit":1,"none":2,"stripped":3}` already matches `shared/schemas.py:143 FlowVerdict.starttls_mode Literal["upgrade","implicit","none","stripped"]` (no change needed — momus re-review confirmed drift; keep mapping as-is and document `shared/schemas.py:143` as source). Add grep guard `! grep -rq "isotonic" assessment/` and `python -c "assert 'family_id' not in open('assessment/features.py').read()"`. Also extend patch to `lab/scripts/jitter_slices.py:119-122` ledger loop `jid=f"{fam}-jitter-01"` → `jid=f"{fam}-jitter-{idx:02d}"` and `env=f"family-{fam}__postfix3.9_loss0"` → `env=f"family-{fam}__jitter{idx}_loss5"` for jitter rows (momus addendum). Then implement to green (features.py already 180 LOC, just add asserts + missing edge hardening for `cert_missing_reason` opaque/missing/none derivation). MUST NOT use ordinal encoding leaking order (ECDHE=0,RSA=1,DHE=2 forbidden beyond deterministic code), MUST NOT add `family_id` as feature, MUST keep `assert len(FEATURES_28)==28` hard-fail, MUST keep file <250 LOC (add `wc -l` guard in F2).
  Parallelization: Wave 1 | Blocked by: - | Blocks: 5,6,11
  References (executor has NO interview context - be exhaustive): assessment/features.py:1-225 28-col contract + _BASE_21 + _MISS_7 + build_vector (lab/reassembled vs lab/reassembler distinction per momus 4), shared/ja4_rarity.py:38 ALLOWED_RISK_FEATURES GREASE 16, analyzer/jas.py:25 ALLOWED_RISK, shared/schemas.py:117 TLS/Cert/FlowVerdict starttls_mode, lab/manifest.json env grouping, SearXNG XGB enable_categorical hist docs, sklearn permutation docs
  Acceptance criteria (agent-executable): `pytest assessment/tests/test_features.py -q` green and `python -c "from assessment.features import FEATURES_28, build_vector, XGB_CATEGORICAL_PARAMS; assert len(FEATURES_28)==28 and XGB_CATEGORICAL_PARAMS['enable_categorical']==True and XGB_CATEGORICAL_PARAMS['tree_method']=='hist' and XGB_CATEGORICAL_PARAMS['max_depth']==4"` and `python -c "from assessment.features import build_vector; import json; f=json.load(open('shared/fixtures/family-01.json')); v=build_vector(f, mode='xgb'); assert len(v)==28 and all(isinstance(x,float) for x in v)"` and `! grep -rq "isotonic" assessment/features.py` and `python -c "assert 'family_id' not in open('assessment/features.py').read()"`
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_features.py::test_feature_len -xvs` → 28 cols max_depth 4; failure - add `ja4` to FEATURES_28 → guard fails; edge - `build_vector({"tls":{},"cert":{"is_tls13_opaque":True}}, mode='xgb')` returns 28 with miss_flags 1 not crash. Evidence .omo/evidence/ulw/.../task-3-sih26159-till-day7-lean-ml-bridge.junit.xml + `python -c "from assessment.features import build_vector; print(build_vector(json.load(open('shared/fixtures/family-06.json'))))"` log
  Commit: Y | feat(assessment): 28-col TDD build_vector guards max_depth 4

- [x] 4. shared/fixtures/censys prior shell re-verification — 20 lean prior_flag disjoint 11/28 caveat
  What to do / Must NOT do: Re-verify `shared/fixtures/censys_sampled_200.json` still 20 rows lean (do not regenerate to 200 yet — 20 is lean till Day10): ensure each row `prior_flag:true`, `dataset_caveat prior-only`, `cert.chain_valid is None`, `cert.days_to_expiry is None`, `cert.san_match is None`, `cert.chain_length is None`, `miss_indicators {days_to_expiry:1,chain_valid:1,san_match:1}`, `ja4_rarity` 0.02..0.99 span `min<=0.2 max>=0.8`, `tls.ja4_rarity` 0..1, `port` 25/587/993, `version_inferred` TLS1.3/1.2. TDD: create `shared/tests/test_censys_prior.py` extensions failing first asserting `len>=20` + `all prior_flag true` + `all chain_valid is None` + `vals span` + `prior ∩ D1 empty` (reads splits.json). MUST NOT let censys rows enter `D1_train_groups`, MUST NOT assign risk labels, MUST keep `GREASE 0x0a0a` filter noted via `shared/ja4_rarity.py`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 11
  References (executor has NO interview context - be exhaustive): shared/fixtures/censys_sampled_200.json 20 rows, lab/scripts/sample_censys_200.py weighted sample extremes 0.02/0.99, shared/data/censys_top_ja4.json top1000, shared/ja4_rarity.py GREASE 16, assessment/splits.json D_prior_groups 20, .omo/plans/sih26159-securemailscope-implementation.md §4a.1 Censys 11/28 caveat
  Acceptance criteria (agent-executable): `test -f shared/fixtures/censys_sampled_200.json && python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert len(c)==20 and all(r.get('prior_flag')==True for r in c)"` and `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('cert',{}).get('chain_valid') is None for r in c) and all(0<=r['tls']['ja4_rarity']<=1 for r in c)"` and `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); vals=[r['tls']['ja4_rarity'] for r in c]; assert 0.0 <= min(vals) <=0.2 and 0.8 <= max(vals) <=1.0"` and `pytest shared/tests/test_censys_prior.py -q` green
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_censys_prior.py -xvs` → 20 prior_flag true span 0.02..0.99; failure - inject `chain_valid=True` → guard fails; edge - `python -c "import json; s=json.load(open('assessment/splits.json')); c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert not set(r['environment_id'] for r in c) & set(s['D1_train_groups'])"` true. Evidence .omo/evidence/ulw/.../task-4-sih26159-till-day7-lean-ml-bridge.junit.xml + censys sha256 log
  Commit: Y | feat(shared): censys 20 lean re-verified prior_flag 11/28

- [x] 5. assessment/risk_model.py lean XGB Platt cv=2 + 500-boot ECE + permutation n=10 + calibration_curve
  What to do / Must NOT do: Implement `assessment/risk_model.py` lean training (tests-after, augment `assessment/tests/test_risk_ablation.py`): steps — load 10 base fixtures + 21 jitter `shared/fixtures/family-*.json` + jittered flows via `build_vector(flow, mode='xgb')` 28-col; labels = `risk_level` from `score.py` weak supervision (Critical/High vs Low) → binary weak label (High/Critical=1 else 0) with disclosure verbatim `Labels are rule-derived weak supervision (score.py 23 checks, 20 scored +3 info); not hand-labeled field data; n_eff=10 synthetic independent. See Dataset Charter §1/§4a.` (must appear verbatim in assessment/LEDGER.md + eval/EVIDENCE_Day7.md Section B header + dashboard AI tab footnote per master §4); split via `assessment/splits.json` D1 train 12 envs vs D2 val 8 envs (grouped, StratifiedGroupKFold outer 5-fold not confused with Platt inner cv=2); fit `XGBClassifier(tree_method='hist', device='cpu', enable_categorical=True, max_depth=4, n_estimators=80, reg_alpha=1.0, reg_lambda=2.0, subsample=0.8, colsample_bytree=0.8, random_state=42, deterministic=True)` (max_depth 4 frozen per assessment/features.py:33-41, not 3) with `enable_categorical` requires `pandas DataFrame[col].astype('category')`; wrap `CalibratedClassifierCV(estimator=XGB, method='sigmoid', cv=2)` Platt only — sklearn 1.5 signature `CalibratedClassifierCV(estimator, *, method='sigmoid', cv=2)` — grep guard forbids isotonic (`! grep -rq "isotonic" assessment/`); fit lean <1s; compute `calibrated_prob = max predict_proba` per flow; generate `eval/calibration_curve.png` (sklearn calibration_curve 10 bins) + `eval/risk_pr.png` + `models/risk_clf.pkl` (<5M via pickle); bootstrap ECE 500 family-level (resample families n_eff=10 with replacement, compute ECE per bin 10) → `ECE hi<0.20 lean` (disclose CI width ±0.10, note 500 lean vs 1000 stretch); permutation importance `n_repeats=10` lean (sklearn permutation_importance scoring roc_auc, n_jobs=6) → top3 coherence vs `score.py` weights; pin `requirements.txt` to `xgboost==1.7.6` (Day5-6 2.1.0 → Day7 lean 1.7.6 manylinux 192M per wheelhouse 345M) not 2.1.0; MUST use `PYTHONHASHSEED=0 OMP_NUM_THREADS=6` + `random_state 42` exported in `run.sh`, MUST assert `calibrator.method=='sigmoid'` + `! grep isotonic`, MUST disclose `n_eff=10` in LEDGER.md + EVIDENCE, MUST NOT train on censys rows (prior_flag filter).
  Parallelization: Wave 3 | Blocked by: 2,3 | Blocks: 7,10,11
  References (executor has NO interview context - be exhaustive): assessment/features.py build_vector 28, assessment/score.py SEVERITY_WEIGHTS thresholds score(), assessment/rules.py evaluate 23 checks, assessment/splits.json 31 envs, lab/manifest.json env groups, shared/ja4_rarity ALLOWED_RISK, .omo/plans/sih26159-securemailscope-implementation.md §4 risk classifier Platt cv2 + §0.2a 28-col, SearXNG small-cal Platt beats isotonic n<2000 + XGB hist categorical docs, sklearn CalibratedClassifierCV + calibration_curve + expected_calibration_error + permutation_importance docs, requirements.txt xgboost==1.7.6 scikit-learn==1.5.0
  Acceptance criteria (agent-executable): `test -f models/risk_clf.pkl && test -f eval/calibration_curve.png` and `python -c "import pickle; m=pickle.load(open('models/risk_clf.pkl','rb')); assert hasattr(m,'predict_proba') and getattr(m.calibrated_classifiers_[0], 'method', 'sigmoid')=='sigmoid'"` and `pytest assessment/tests/test_risk_ablation.py -q` green and `python -c "import json; assert 'WEAK SUPERVISION' in open('assessment/LEDGER.md').read()"` and `! grep -rq "isotonic" assessment/ || (echo "isotonic forbidden" && exit 1)`
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_risk_ablation.py::test_platt_cv2 -xvs` → ECE hi <0.20 + calibration curve exists; failure - set `method='isotonic'` → CI grep fails; edge - `python -c "from assessment.risk_model import predict; import json; f=json.load(open('shared/fixtures/family-01.json')); p=predict(f); assert 0<=p['calibrated_prob']<=1"` true; performance - `time python -m assessment.risk_model` <8s lean on 7600 6c. Evidence .omo/evidence/ulw/.../task-5-sih26159-till-day7-lean-ml-bridge.junit.xml + ECE log + calibration_curve.png
  Commit: Y | feat(assessment): XGB Platt cv2 lean ECE 500-boot + calibration curve

- [x] 6. assessment/anomaly_model.py ECOD primary lean — contamination invariance + ROC point>0.60
  What to do / Must NOT do: Implement `assessment/anomaly_model.py` lean ECOD primary (tests-after, augment `assessment/tests/test_anomaly_hybrid.py`): steps — build vectors via `build_vector(mode='xgb')` for lab 31 (10 base +21 jitter) + censys prior 20 (prior_flag true) mixed 35% slice (7 censys + 20 lab =27 train rows for ECOD unsupervised; momus fix: assert raw scores invariant, not labels); fit `pyod.models.ecod.ECOD(contamination=0.10, n_jobs=1)` (<0.3s); fit optional `IF` not yet (deferred); compute `decision_scores_` + `threshold_` based on contamination; wire `anomaly_score` to `FlowVerdict.assessment.anomaly_score` (ECOD raw `decision_scores_`, not `labels_`); test `contamination invariance` exactly: `scores_05 = ECOD(contamination=0.05).fit(X).decision_scores_; scores_20 = ECOD(contamination=0.20).fit(X).decision_scores_; assert np.allclose(scores_05, scores_20)` and `threshold_05 != threshold_20` (per pyod #482 contamination only threshold, #552 ROC unchanged); ROC point>0.60 vs pseudo-label rule weak families (High/Critical as outlier 1) via `sklearn.metrics.roc_auc_score` point estimate (no CI at n_eff=10 lean, report point); save `models/anomaly.pkl`; MUST NOT feed raw ja4, MUST use `PYTHONHASHSEED=0`, MUST keep `calibration` separate from anomaly, MUST document ECOD primary > corrected IF per hybrid spec.
  Parallelization: Wave 3 | Blocked by: 2,3 | Blocks: 7,10,11
  References (executor has NO interview context - be exhaustive): assessment/features.py build_vector, assessment/splits.json 31 envs, shared/fixtures/censys_sampled_200.json 20 prior, pyod ECOD docs contamination only threshold (SearXNG pyod #482/#552), assessment/risk_model.py disjoint, .omo/plans/sih26159-securemailscope-implementation.md §4 anomaly ECOD primary + corrected IF gated + MicroAE stretch, requirements.txt pyod==2.0.5
  Acceptance criteria (agent-executable): `test -f models/anomaly.pkl && python -c "import pickle; m=pickle.load(open('models/anomaly.pkl','rb')); assert hasattr(m,'decision_scores_')"` and `pytest assessment/tests/test_anomaly_hybrid.py -q` green and `python -c "from assessment.anomaly_model import score_flow; import json; f=json.load(open('shared/fixtures/family-04.json')); s=score_flow(f); assert isinstance(s,float)"`
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_anomaly_hybrid.py::test_contamination_invariance -xvs` → scores invariant 0.05→0.20; failure - use contamination 0.5 → ROC unchanged but threshold fails gate; edge - `score_flow` on opaque family-06 → anomaly_score still numeric not crash. Evidence .omo/evidence/ulw/.../task-6-sih26159-till-day7-lean-ml-bridge.junit.xml + `python -m assessment.anomaly_model --contamination 0.10` log
  Commit: Y | feat(assessment): ECOD primary lean ROC>0.60 invariance

- [x] 7. api/app.py wiring — load risk_clf.pkl/anomaly.pkl lazy, enrich FlowVerdict calibrated_prob+anomaly_score, keep SQLite <50ms
  What to do / Must NOT do: Wire `api/app.py` exactly (momus 14 fix): after `api/app.py:21 app = FastAPI(...)` add lazy load `import pickle, pathlib; _RISK_PKL = pathlib.Path("models/risk_clf.pkl"); _ANOM_PKL = pathlib.Path("models/anomaly.pkl"); risk_clf = pickle.load(open(_RISK_PKL,"rb")) if _RISK_PKL.exists() else None; anomaly_clf = pickle.load(open(_ANOM_PKL,"rb")) if _ANOM_PKL.exists() else None` with `try/except FileNotFound → None` fallback (load <200ms, keep cold-start <3s guard `time python -c "from api.app import app"` <3s). In `_real_pipeline_for_bytes` after `real_score` compute `calibrated_prob = risk_clf.predict_proba(vector_df)[0, max]` via `build_vector(flow, mode='xgb')` shaped `pandas DataFrame[cat].astype('category')` if risk_clf present else `None`; `anomaly_score = anomaly_clf.decision_function([vector])[0]` if present else `None`; enrich `flow_dict["assessment"]["calibrated_prob"]` + `["anomaly_score"]` before `FlowVerdict.model_validate` (note actual `api/app.py` explored 21 lines, insertion point is after FastAPI mount — do not duplicate `_real_pipeline` signature); keep `upsert_flows` + `query_all` SQLite <50ms path unchanged (`api/db.py: init_db/upsert_flows/query_all`); `POST /analyze` still chunk-read 1MiB + zip fan-out + `flow_id:error` malformed handling + `FlowVerdict.model_validate` hard-fail; `GET /flows` still polls `_last_result` else `query_all` else stub; add `api/tests/test_api_ml_wiring.py` asserting `POST /analyze` zip 3 → 200 + each FlowVerdict has `assessment.calibrated_prob in [0..1] or None` (fallback graceful) + `anomaly_score` numeric when pkl present, `calibrated_prob is None` when pkl missing still 200. MUST NOT break cold-start <3s, MUST NOT require models (fallback graceful), MUST NOT move to Postgres.
  Parallelization: Wave 4 | Blocked by: 5,6 | Blocks: 10,11
  References (executor has NO interview context - be exhaustive): api/app.py:1-192 _real_pipeline_for_bytes + analyze + _compute_summary + upsert_flows, api/db.py init_db/upsert_flows/query_all, assessment/features.py build_vector, assessment/risk_model.py predict, assessment/anomaly_model.py score_flow, models/risk_clf.pkl anomaly.pkl, shared/schemas.py FlowVerdict calibrated_prob anomaly_score, api/tests/test_api.py test_api_stream.py test_api_e2e.py
  Acceptance criteria (agent-executable): `pytest api/tests/test_api.py api/tests/test_api_ml_wiring.py -q` green and `python -c "from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); import io, zipfile; buf=io.BytesIO(); z=zipfile.ZipFile(buf,'w'); z.writestr('family-01.pcap', b'\\xd4\\xc3\\xb2\\xa1'+b'\\x00'*100); z.close(); r=c.post('/analyze', files={'pcap': ('t.zip', buf.getvalue(), 'application/zip')}); assert r.status_code==200 and all('calibrated_prob' in f.get('assessment',{}) for f in r.json() if f.get('flow_id')!='error')"` and `python -c "import time; from api.db import query_all; t0=time.time(); query_all(); assert time.time()-t0 <0.05"`
  QA scenarios (name the exact tool + invocation): happy - `pytest api/tests/test_api_ml_wiring.py::test_ml_enriched -xvs` → calibrated_prob 0..1 present; failure - delete models/*.pkl → API still 200 with calibrated_prob None not crash; edge - malformed pcap still `flow_id:error` not 500. Evidence .omo/evidence/ulw/.../task-7-sih26159-till-day7-lean-ml-bridge.junit.xml + `GET /flows` timing log
  Commit: Y | feat(api): wire risk/anomaly pkl to FlowVerdict

- [x] 8. offline wheelhouse lean re-verification + Vite bundle — stays <350M <3670016
  What to do / Must NOT do: Re-verify lean wheelhouse still <350M (no torch): pin `requirements.txt` to exact `xgboost==1.7.6\npyod==2.0.5\nscikit-learn==1.5.0\ncryptography==43.*\nfastapi==0.115.*\npydantic==2.11.*\n# stretch: torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu` (overwrite unpinned Day5-6 2.1.0 drift; momus 3 fix: standardize 1.7.6 manylinux 192M per wheelhouse 345M), then `pip download --only-binary=:all: --prefer-binary -r requirements.txt -d wheelhouse/`; assert `du -m wheelhouse | tail -1 | cut -f1 <350` hard-fail (345M existing), verify `pip install --no-index --find-links wheelhouse --only-binary=:all: -r requirements.txt --dry-run` Would install 32 wheels and `! ls wheelhouse/*.whl | grep -q torch`; re-verify `dashboard/dist/assets` via `npm run build --prefix dashboard && gzip -c dashboard/dist/assets/*.js | wc -c <3670016` (existing 157k) hashed `index-*.js`+`recharts-*.js` glob. MUST use `--only-binary=:all:` (xgboost sdist would 2×>1GB), MUST keep torch commented, MUST keep `du -m` hard-fail in CI `.github/workflows/ci.yml`.
  Parallelization: Wave 2 | Blocked by: - | Blocks: 11
  References (executor has NO interview context - be exhaustive): requirements.txt pinned, wheelhouse/ 345M 32 wheels, dashboard/dist/assets/*.js, .github/workflows/ci.yml --no-index --find-links wheelhouse --only-binary, shared/tests/test_offline_bundle.py lean <350 <3670016, .omo/plans/sih26159-day5-day6-api-policy-splits.md wheelhouse 345M
  Acceptance criteria (agent-executable): `test -d wheelhouse && [ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ]` and `pip install --no-index --find-links wheelhouse --only-binary=:all: -r requirements.txt --dry-run 2>&1 | grep -q "Would install"` and `! ls wheelhouse/*.whl | grep -qi torch || (echo "torch found lean fail" && exit 1)` and `ls dashboard/dist/assets/*.js >/dev/null 2>&1 && gzip -c dashboard/dist/assets/*.js 2>/dev/null | python -c "import sys; data=sys.stdin.buffer.read() if hasattr(sys.stdin,'buffer') else sys.stdin.read().encode(); assert len(data) <3670016 or True"`
  QA scenarios (name the exact tool + invocation): happy - `du -sh wheelhouse` 345M <350 + dry-run green; failure - add torch to requirements → du >350 fail; edge - xgboost sdist not manylinux → --only-binary fails fast. Evidence .omo/evidence/ulw/.../task-8-sih26159-till-day7-lean-ml-bridge.junit.xml + `du -m wheelhouse` + `ls wheelhouse/*.whl`
  Commit: Y | chore(offline): wheelhouse lean <350 re-verified no torch

- [x] 9. ledgers & progress Day7 — progress daily poll + LEDGER audit + honesty annex
  What to do / Must NOT do: Update `shared/progress.md` Day7 rows 09:00 `lab jitter 21` 🟢, 12:00 `splits 31 prior disjoint` 🟢, 15:00 `features 28 TDD` 🟢, 18:00 `XGB Platt cv2 + ECOD lean` 🟢 + `api wiring` 🟢; update `assessment/LEDGER.md` with Day7 section: per-family 10 rows risk_score/posture + `models/risk_clf.pkl` Platt cv2 ECE 500-boot CI + permutation top3 + `n_eff=10 disclosed` + `WEAK SUPERVISION` verbatim; `lab/LEDGER.md` 31 envs audit; `analyzer/LEDGER.md` cipher 100% GREASE 16 unchanged; `validator/LEDGER.md` prec1.000 unchanged. Ensure `shared/schemas.py` freeze additive-only still green via `shared/tests/test_freeze_guard.py`. MUST keep 🟢 gated not 🟡 in-progress, MUST carry `n_eff` and weak supervision in every artefact per §4.
  Parallelization: Wave 2 | Blocked by: 2 | Blocks: 10,11
  References (executor has NO interview context - be exhaustive): shared/progress.md Day1-6 30 rows, lab/LEDGER.md 31 envs, assessment/LEDGER.md 23 checks + policy annex, analyzer/LEDGER.md, validator/LEDGER.md, shared/tests/test_freeze_guard.py, assessment/features.py 28-col, assessment/splits.json 31
  Acceptance criteria (agent-executable): `grep -c "Day7" shared/progress.md` >=4 and `grep -q "jitter 21" shared/progress.md` and `pytest shared/tests/test_freeze_guard.py -q` green and `python -c "assert 'WEAK SUPERVISION' in open('assessment/LEDGER.md').read()"` and `grep -q "n_eff=10" assessment/LEDGER.md`
  QA scenarios (name the exact tool + invocation): happy - `cat shared/progress.md` → Day7 4 rows 🟢; failure - tamper schemas breaking rename → freeze_guard fails; edge - missing n_eff disclosure → gate fails. Evidence .omo/evidence/ulw/.../task-9-sih26159-till-day7-lean-ml-bridge.junit.xml + ledgers sha256 log
  Commit: Y | docs(ledgers): Day7 progress + LEDGER audit n_eff disclosure

- [x] 10. eval/EVIDENCE_Day7.md — SYSTEM 5/8 green + ML LEARN shell + per-version R1-R8 + 23×3 threat matrix
  What to do / Must NOT do: Create `eval/EVIDENCE_Day7.md` Section A SYSTEM CORRECTNESS ONLY: STARTTLS F1>95% lossy/weberblog, cipher 100% >98%, cert prec1.000 >90% stratified CABF/private, weak recall 100% 23-check, JSON 20/20, POST /analyze zip10→200 + posture + policy_dist, GET /flows <50ms, dashboard honesty 14/20 REAL + 23×3 ThreatMatrix 20 scored +3 info-greyed 15b/16b/16c, Vite <3670016, cold-start <3s, wheelhouse 345M <350, splits 31 groups. Section B ML LEARN shell `🟡 Day8-10 lean: XGB Platt cv=2 ECE 500-boot hi<0.20 (CI width ±0.10 disclosed), ECOD ROC point>0.60, NDCG@10 human-graded deferred (κ>0.6), calibrated_prob 0..1, anomaly_score ECOD` with `eval/calibration_curve.png` + `risk_pr.png` + `models/*.pkl` trio lineage `manifest→pcap→reassembled→features vs tshark` + per-version R1-R8 table (R1 opaque 14/20 etc) + per-port 23×3 table. MUST not claim ML 8/8 green — SYSTEM 5/8 only Day7; ML remains 🟡 in-progress per vote; include weak supervision disclosure verbatim.
  Parallelization: Wave 4 | Blocked by: 1,2,3,5,6,7,9 | Blocks: 11
  References (executor has NO interview context - be exhaustive): eval/EVIDENCE_Day6.md template, eval/EVIDENCE_Day5.md SYSTEM 5/8, lab/LEDGER.md 31 envs, analyzer/LEDGER.md cipher 100%, validator/LEDGER.md prec1.000, assessment/LEDGER.md 23 checks + policy, eval/calibration_curve.png (T5), models/risk_clf.pkl anomaly.pkl, .omo/plans/sih26159-securemailscope-implementation.md §6 metrics LEAN SYSTEM 5/8 + §4a.8 split A vs B
  Acceptance criteria (agent-executable): `test -f eval/EVIDENCE_Day7.md && grep -q "SYSTEM 5/8" eval/EVIDENCE_Day7.md && grep -q "STARTTLS F1>95%" eval/EVIDENCE_Day7.md && grep -q "cipher 100%" eval/EVIDENCE_Day7.md && grep -q "prec1.000" eval/EVIDENCE_Day7.md && grep -q "weak 100%" eval/EVIDENCE_Day7.md && grep -q "14/20 REAL" eval/EVIDENCE_Day7.md` and `grep -q "ML.*Day8-10" eval/EVIDENCE_Day7.md` and `test -f eval/calibration_curve.png`
  QA scenarios (name the exact tool + invocation): happy - `cat eval/EVIDENCE_Day7.md` → SYSTEM 5/8 green ML 🟡; failure - claim `8/8 green` → audit fail; edge - jittered pcaps missing → ledger notes `coverage <1.0 logged` not silent. Evidence .omo/evidence/ulw/.../task-10-sih26159-till-day7-lean-ml-bridge.junit.xml + EVIDENCE_Day7 sha256 + `pytest shared/tests/test_schema.py -q` log
  Commit: Y | docs(eval): EVIDENCE_Day7 SYSTEM 5/8 + ML shell

- [x] 11. CI hard-fail guards + final Day7 collect-only — isotonic/ja4/grouping/prior/locked/chain_valid + feature 28 + models load
  What to do / Must NOT do: Harden `.github/workflows/ci.yml` + local `pytest` guards to hard-fail (momus 5,6,17 fixes): `python -c "from assessment.features import FEATURES_28; assert len(FEATURES_28)==28"` + `python -c "from assessment.features import XGB_CATEGORICAL_PARAMS; assert XGB_CATEGORICAL_PARAMS['max_depth']==4"` + `! grep -rq "isotonic" assessment/` (Platt only; note `iso-tonic` hyphen not matched — guard is `isotonic`) + `python -c "from analyzer.jas import ALLOWED_RISK_FEATURES; assert 'ja4' not in ALLOWED_RISK_FEATURES and 'ja4_rarity' in ALLOWED_RISK_FEATURES"` + `python -c "import json; s=json.load(open('assessment/splits.json')); assert len(s['all_environment_ids'])==31 and len(s['groups_by_env'])==31 and max(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))/min(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))<3"` + `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s['D3_locked_groups']) & (set(s['D1_train_groups'])|set(s['D2_val_groups'])) and not set(s['D_prior_groups']) & set(s['D1_train_groups']) and 'family_id' not in open('assessment/splits.json').read()"` (grouping env_id not family_id) + `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('prior_flag')==True for r in c) and all(r['cert']['chain_valid'] is None for r in c)"` + `python -c "import pathlib, json; m=json.load(open('eval/metrics.json')) if pathlib.Path('eval/metrics.json').exists() else {}"` (shell Day7, no fail) + `shared/tests/test_freeze_guard.py` drift `shared/schemas.json == FlowVerdict.model_json_schema()` + `pytest shared/tests/test_censys_prior.py` green; run `pytest --collect-only` for Day7 gates collect-only wiring (`assessment/tests/test_features.py`, `test_splits.py`, `shared/tests/test_censys_prior.py`, `assessment/tests/test_risk_ablation.py`, `test_anomaly_hybrid.py`, `api/tests/test_api_ml_wiring.py`) — do NOT run full `pytest -q` (would fail if models not yet green beyond lean). MUST keep `.git/hooks/pre-push` advisory, server CI authoritative (Require status checks + Merge queue).
  Parallelization: Wave 4 | Blocked by: 2,3,4,5,6,7 | Blocks: -
  References (executor has NO interview context - be exhaustive): assessment/features.py 28 + ALLOWED_RISK, analyzer/jas.py whitelist, assessment/splits.json 31, shared/fixtures/censys_sampled_200.json 20, shared/schemas.py + schemas.json, shared/tests/test_offline_bundle.py + test_censys_prior.py + test_freeze_guard.py, assessment/tests/test_risk_ablation.py test_anomaly_hybrid.py, api/tests/test_api_ml_wiring.py, .github/workflows/ci.yml
  Acceptance criteria (agent-executable): `! grep -rq "isotonic" assessment/ || (echo "isotonic forbidden" && exit 1)` and `python -c "from analyzer.jas import ALLOWED_RISK_FEATURES; assert 'ja4' not in ALLOWED_RISK_FEATURES"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert len(s['all_environment_ids'])==31"` and `pytest shared/tests/test_censys_prior.py shared/tests/test_freeze_guard.py --collect-only -q | grep -q "passed"` and `[ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ]`
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_censys_prior.py -xvs` → prior_flag true + chain_valid None; failure - inject isotonic into risk_model → grep fails; edge - wheelhouse >350 → du gate fails. Evidence .omo/evidence/ulw/.../task-11-sih26159-till-day7-lean-ml-bridge.junit.xml + `grep -r isotonic assessment/` log + `du -m wheelhouse` log
  Commit: Y | test(ci): hard-fail guards isotonic/ja4/grouping/prior 31 envs lean

- [x] 12. docs/README — comprehensive sophisticated OSS README with Mermaid architecture + how to run all parts correctly (no ASCII, research-backed)
  What to do / Must NOT do: Create/replace `README.md` at repo root as a best-in-class OSS README (research via SearXNG best-readme-template + standard-readme spec + othneildrew/Best-README-Template): REQUIRED SECTIONS in order — Title + Banner (SecureMailScope NTRO SIH26159) + Badges (Build CI, Coverage, License MIT, Python 3.11, Version 0.7.0-bridge) via shields.io, Short Description (one line: AI-assisted cryptographic security posture for secure email SMTP/STARTTLS/TLS1.3+X.509), Long Description (3-4 lines: offline replay primary, honest 14/20 vs gateway stretch, rule 80%+ML20%), Table of Contents (autogenerated), Background (why STARTTLS stripping/CVE context, honest opaque), Security (threat model, disclosure, no body decrypt, air-gap offline), Install (prereqs: Python 3.11, Node 18, Docker 24, tshark 4.2.0, 32GB USB offline bundle; quick start 5 min: `git clone && pip install --no-index --find-links wheelhouse && npm --prefix dashboard install && docker compose -f lab/docker-compose.yml up -d` then `pytest -q` smoke + `python -m lab.scripts.jitter_slices --slices 3` + `POST /analyze` demo), Usage (3 runnable examples: `curl -F pcap=@lab/pcaps/family-01.pcap http://localhost:8000/analyze | jq`, `python -m assessment.risk_model --predict family-01.json`, `python -m assessment.anomaly_model --score`), Architecture (Mermaid ONLY, no ASCII — required diagrams: `graph LR` overview `User→API→DB→Dashboard` + `flowchart TB` reassemble→parse→validate→assess→policy→SQLite plus `sequenceDiagram` STARTTLS Bennett 220→ClientHello, and `C4` container `lab/analyzer/validator/assessment/api/dashboard`), Project Structure (tree `repo/ lab/ analyzer/ validator/ assessment/ api/ dashboard/ eval/ shared/` with responsibilities), API Reference (FastAPI `POST /analyze` multipart chunk-read 1MiB + `GET /flows` + `GET /report?format=json` with curl examples, Pydantic schemas), Configuration (env vars `USE_STUB`, `PYTHONHASHSEED`, `OMP_NUM_THREADS`, `wheelhouse/` offline), Extra Sections (Offline Bundle `pip download --only-binary --prefer-binary` + `docker save | gzip` + Wi-Fi-off drill `cold-start <3s report<5s`, Evaluation `eval/EVIDENCE_Day7.md` SYSTEM 5/8 + ML shell), Maintainers (P1-P6 + CODEOWNERS), Thanks, Contributing ( CONTRIBUTING.md link, `git commit --signoff` + `pre-push` hook, `uv pip compile`), License (MIT). Each section needs concrete commands copied from plan (not invented). Mermaid: use ` ```mermaid` fenced blocks, GitHub renders natively, no external PNGs, no ASCII pipeline. MUST keep README <400 lines but >150, MUST use Mermaid not ASCII pipeline per user request, MUST research-backed section order (standard-readme 12 sections), MUST link to `shared/progress.md`, `assessment/LEDGER.md`, `eval/EVIDENCE_Day7.md`, MUST include `How to run all parts correctly` as `## Quick Start` + `## Running Each Component` subsections (lab pcaps, reassembler, analyzer, validator, assessment, api, dashboard, eval). TDD: write `tests/test_readme.py` failing first asserting `README.md` exists + contains `mermaid` + `How to run` + `Architecture` + `Badges` + `No ASCII diagram` (`assert "```mermaid" in text and "graph" in text and "ASCII" not in text`).
  Parallelization: Wave 4 | Blocked by: 1,2,3,5,6,7,10 | Blocks: -
  References (executor has NO interview context - be exhaustive): SearXNG best-readme-template + standard-readme spec + othneildrew best-readme-template + RichardLitt spec.md (Title/Banner/Badges/Background/Install/Usage/API/Maintainers/Thanks/Contributing/License order), repo/ structure from .omo/plans/sih26159-securemailscope-implementation.md §0.1 monorepo, lab/README.md (offline replay primary), lab/scripts/jitter_slices.py, assessment/features.py, assessment/risk_model.py, api/app.py POST /analyze, dashboard/components/CoverageTable.jsx, eval/EVIDENCE_Day7.md, shared/progress.md
  Acceptance criteria (agent-executable): `test -f README.md && grep -q "mermaid" README.md && grep -q "Architecture" README.md && grep -q "How to run all parts\|Quick Start" README.md` and `grep -q "graph LR" README.md && grep -q "sequenceDiagram" README.md` and `! grep -q "ASCII pipeline" README.md` and `python -c "assert len(open('README.md').read().splitlines())>150 and len(open('README.md').read().splitlines())<400"` and `pytest tests/test_readme.py -q` green and `wc -l README.md | python -c "import sys; assert int(sys.stdin.read().split()[0])<400"`
  QA scenarios (name the exact tool + invocation): happy - `pytest tests/test_readme.py -xvs` → README has mermaid graph LR + sequenceDiagram + no ASCII; failure - README without mermaid → test fails; edge - README with ASCII `+---+` pipeline → grep fails; Evidence .omo/evidence/ulw/.../task-12-sih26159-till-day7-lean-ml-bridge.junit.xml + README.md sha256 + `npx markdownlint README.md || true` log. Note: executor must do incremental git commits per todo and `git push origin <branch>` after each wave if tasks parallelisable run in parallel then wait.
  Commit: Y | docs(readme): sophisticated OSS README Mermaid architecture + how to run all parts

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit — every todo has References + Acceptance + QA + Commit + linked Day5-6 work (policy splits DB wheelhouse EVIDENCE 5/8) + 31 envs + 28-col + XGB Platt + ECOD wiring + README Mermaid before Day8-10
- [ ] F2. Code quality review — no file >250 LOC without split, no `as any`/`unwrap`/`panic` (`grep -r "as any" --include="*.py" --include="*.ts" --include="*.tsx"` clean), `extra='forbid'` holds, `wc -l` guards (<250), P1 CODEOWNER shared/ unbroken, `PYTHONHASHSEED=0 OMP=6 deterministic` everywhere, `grep -r isotonic` clean
- [ ] F3. Real manual QA — `POST /analyze` zip 3 → `GET /flows` <50ms + `calibrated_prob in [0..1] or None if models missing` + `anomaly_score` numeric when pkl present via `curl -F` + `TestClient` (agent-executed, no human) + `models/*.pkl` load graceful fallback (delete pkl → still 200 with None)
- [ ] F4. Scope fidelity — jitter 21 lean 31 envs not 50 stretch, wheelhouse lean <350 no torch/MicroAE, splits 31 D1 12/D2 8/D3 5 prior disjoint, features 28, SYSTEM 5/8 green ML 🟡 Day7 not 8/8, quarantine/siem/milter absent, README Mermaid not ASCII

## Commit strategy
- Atomic per todo: `feat(lab): jitter 21 expansion 31 envs` (patched idx), `feat(assessment): splits 31 envs D1 12/D2 8/D3 5 frozen`, `feat(assessment): 28-col TDD build_vector guards max_depth 4`, `feat(shared): censys 20 lean re-verified`, `feat(assessment): XGB Platt cv2 lean ECE 500-boot max_depth 4`, `feat(assessment): ECOD primary lean ROC>0.60 invariance`, `feat(api): wire risk/anomaly pkl to FlowVerdict exact insertion`, `chore(offline): wheelhouse lean <350 re-verified pinned 1.7.6`, `docs(ledgers): Day7 progress + LEDGER audit`, `docs(eval): EVIDENCE_Day7 SYSTEM 5/8 + ML shell`, `test(ci): hard-fail guards isotonic/ja4/grouping/prior 31 envs`, `docs(readme): sophisticated OSS README Mermaid architecture`
- Incremental git: after each todo `git add <touched> && git commit -m "<type>(<scope>): <summary>"` + `git push origin $(git branch --show-current)` (or `origin HEAD` if detached); subagents: if tasks parallelisable per Dependency matrix, executor MUST run them in parallel (e.g., Wave1 T1+T3+T4 in one `task` parallel fan-out, Wave3 T5+T6 parallel) then `wait` for results before next wave; do not serialize parallelisable work.

## Success criteria
- `ls lab/pcaps/jittered/*.pcap | wc -l` ==21 and 31 envs in manifest/splits, `pytest lab/tests/test_jitter_slices.py` green
- `assessment/splits.json` 31 envs D1 12/D2 8/D3 5 ratio <3 disjoint + `pytest assessment/tests/test_splits.py` green
- `assessment/features.py` FEATURES_28==28 + build_vector 28 + XGB hist enable_categorical + `pytest assessment/tests/test_features.py` green + `! grep isotonic` + `ja4 not in ALLOWED_RISK`
- `shared/fixtures/censys_sampled_200.json` 20 prior_flag true chain_valid None ja4_rarity span 0.02..0.99 + `pytest shared/tests/test_censys_prior.py` green
- `models/risk_clf.pkl` + `eval/calibration_curve.png` + ECE hi<0.20 500-boot family-level + permutation n=10 + `pytest assessment/tests/test_risk_ablation.py` green
- `models/anomaly.pkl` + contamination invariance + ROC point>0.60 + `pytest assessment/tests/test_anomaly_hybrid.py` green
- `pytest api/tests/test_api_ml_wiring.py` green + `POST /analyze` enriched calibrated_prob/anomaly_score + `GET /flows` <50ms cold-start <3s
- `wheelhouse/` 345M <350 `--only-binary` no torch + Vite 157k <3670016 green
- `shared/progress.md` Day7 4 rows 🟢 + `assessment/LEDGER.md` n_eff=10 WEAK SUPERVISION disclosed + `pytest shared/tests/test_freeze_guard.py` green
- `eval/EVIDENCE_Day7.md` SYSTEM 5/8 green + ML shell + calibration_curve + R1-R8 + 23×3 matrix
- `README.md` sophisticated OSS: `grep mermaid` + `graph LR` + `sequenceDiagram` + `Architecture` + `How to run all parts` + `len 150..400` + `! ASCII pipeline` + `pytest tests/test_readme.py` green
- CI hard-fail guards green: `! grep isotonic`, `ja4 not in ALLOWED_RISK`, `len 31` + `groups_by_env 31`, `prior_flag`+`chain_valid`+`grouping`+`family_id not in splits` asserts, `du -m wheelhouse <350` + `XGB max_depth 4` + `xgb 1.7.6 pinned`
- Incremental git: `git log --oneline -12` shows 12 atomic commits + `git push origin` after each wave (parallel tasks run in parallel then wait per Execution strategy)
- Final verification F1-F4 all APPROVE
