# sih26159-closure-docker-ci-frontend-live-hardening - Work Plan

## TL;DR (For humans)

**What you'll get:** One command after cloning — `bash scripts/turnup.sh` checks everything then `docker compose up` on a single port 8000 — gives you a projector-readable app with five clear tabs, where families can be played live, custom packets are synthesized for real, and random traffic keeps the dashboard breathing with WebSocket packet hex, plus 50 independent families that move ML from 5/8 to 6.5/8 honest (8/8 needs hand-labeled field data) and a CI that actually runs Docker buildx + compose up wait instead of grep.

**Why this approach:** Keep Docker as the judge contract (no GHCR publish, local compose after clone) and keep ML honest by fixing the data first (50 families → p/n 0.10 → 3-bin calibration) before claiming 8/8; the UI is rebuilt from an unsummarized research file the workers read verbatim so they cannot reinvent it, using the four design skills you demanded.

**What it will NOT do:** No publish to any registry yet, no `impeccable` skill (uses your four mandated skills), no isotonic at n<1000, no raw ja4, no wheelhouse bake, no auto-down trap (separate turndown), no second port 5173 in prod.

**Effort:** XL (14 todos +4 verifiers, 4 waves, ~28h wall, 4-agent parallel, 2h human-agnostic)
**Risk:** High — 50-family expansion touches splits/manifest/models/eval simultaneously + Docker buildx multi-arch 650MB + WebSocket state with single-worker SQLite; mitigated by LOFAM gap gate <0.15 and per-family synthesis audit.
**Decisions to sanity-check:** Pure-Docker turnup (A1 A) vs hybrid; pinned lab images (boky/coredns) vs vendor build; WebSocket + synthetic continuum vs polling; 50 vs 70 families knee at 50.

Your next move: approve plan → `/start-work sih26159-closure-docker-ci-frontend-live-hardening` (dual high-accuracy momus+oracle review will run automatically — review_required: true). Full execution detail follows below.

---

> TL;DR (machine): XL / High — pure-Docker turnup/turndown + pinned tshark/lab + 5-tab WS live + scapy synthesis + 50-family honest ML + hard-fail CI

## Scope
### Must have
- Two-file Docker lifecycle pure compose up/down single 8000 + pinned lab images + tshark 4.2 + research file unsummarized + 5-tab shell BrowserRouter + Families Play + Lab scapy synthesis + Live WS hex tri-pane + continuum random + Reports PDF + 50-family honest + WS history + hard-fail CI + 6.5/8 interim evidence
- Real tests per todo: docker buildx call check + compose up wait + inspect healthy + dig MX + curl health/flows/analyze + websocat WS + sha256 distinct + vite build gzip + vitest + pytest junit

### Must NOT have (guardrails, anti-slop, scope boundaries)
- No GHCR publish yet, no impeccable, no isotonic at n<1000, no raw ja4, no family_id in FEATURES, no jitter theatre, no wheelhouse bake, no port 5173 prod, no auto-down trap, no modal where split needed, no color-only, no summarize of research file

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: TDD for splits/features/shell guards + tests-after for Docker/WS/synthesis/ML retrain + real docker turnup + CI hard-fail + frontend build/serve/vitest + backend endpoints metrics. Framework: `pytest -q --junitxml=<attemptDir>/task-N-*.junit.xml` + `python -m py_compile` + `docker buildx --call check` + `docker compose config` + `docker compose --profile lab up -d --wait && inspect healthy && dig MX` + `npm run build && gzip -c dist/assets/*.js` + `curl -fsS /health` + `websocat ws://localhost:8000/ws/flows` + `python -m lab.scripts.synth_families --synth-one`
- Evidence: <attemptDir>/task-<N>-sih26159-closure-docker-ci-frontend-live-hardening.<ext> (attemptDir = currentAttemptDir from 'omo ulw-loop status --json', .omo/evidence/ulw/<session>/<goalId>/a<attempt>; outside ulw-loop use .omo/evidence/)

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.

- Wave 1 (4h, Infra): T1 lab images pinned → T2 Dockerfile tshark bake + T4 research file (T1 blocks T3, T2 parallel, T4 independent) — 3 todos parallelizable
- Wave 2 (6h, Shell+Data): T3 two-file turnup/turndown (blocked by T1+T2) ‖ T5 dashboard shell 5-tab router + tokens (needs T4) ‖ T11 50-family expansion synthesis start (independent) — 3 parallel
- Wave 3 (6h, Features): T6 Families grid+Play ‖ T7 Lab synthesis ‖ T8 Live WS hex (all blocked by T5, T7 needs T3+T11, T8 needs T3) — 3 parallel; plus T12 API WS endpoint (blocked by T3+T11) parallel
- Wave 4 (8h, Hardening): T9 continuum ‖ T10 Reports PDF ‖ T13 CI hard-fail ‖ T11 retrain finishes → T14 EVIDENCE 6.5/8 + docs (blocked by all prior) — 4 parallel before gate
- Final verification wave F1-F4 parallel after all todos must APPROVE before handoff.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 lab images pinned | - | 3,13 | 2,4 |
| 2 Dockerfile tshark bake | - | 3,13 | 1,4 |
| 3 turnup/turndown pure Docker | 1,2 | 7,8,12,13 | 5,11 |
| 4 research file unsummarized | - | 5,10 | 1,2,3 |
| 5 dashboard shell 5-tab | 4 | 6,7,8,10 | 3,11 |
| 6 Families grid+Play | 5 | 9,14 | 7,8 |
| 7 Lab synthesis | 3,5,11 | 9,14 | 6,8 |
| 8 Live WS hex | 3,5 | 9,14 | 6,7 |
| 9 continuum random | 6,7,8,12 | 14 | 10,13 |
| 10 Reports PDF+JSON | 4,5 | 14 | 9,13 |
| 11 50-family expansion + retrain | - | 7,12,14 | 3,5 |
| 12 API WS endpoint + storage | 3,11 | 9,14 | 6,7,8 |
| 13 CI hard-fail everything | 1,2,3,5,11 | 14 | 9,10 |
| 14 EVIDENCE 6.5/8 + docs | 6,7,8,9,10,11,12,13 | - | - |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. lab/docker-compose.yml phantom images pinned to pullable Hub tags + healthcheck verify
  What to do / Must NOT do: Patch `lab/docker-compose.yml:30` `postfix:3.9-alpine` → `boky/postfix:3.9` (comment fallback `catatnight/postfix`), `:65` `dovecot:2.3` → `dovecot/dovecot:2.3` (alt `instrumentisto/dovecot:2.3`), `:90` `andyshinn/dnsmasq:2.86` → `andyshinn/dnsmasq:2.83` and also add `coredns/coredns:1.11` fallback comment, verify each via `docker manifest inspect <image> | jq .schemaVersion` before commit, keep network `172.18.0.0/24` and 5 services `postfix/dovecot/mockdns/mta-sts/sender`, healthcheck `postfix status` + `mockdns dig MX mta-sts TLSA` unchanged, `include: path: lab/docker-compose.yml profiles: ["lab"]` stays in root compose. MUST NOT keep phantom tags `3.9-alpine / 2.3 / 2.86`, MUST keep `172.18.0.0/24` bridge, MUST verify `docker compose config` OK plus `docker compose --profile lab config` OK. TDD: create `lab/tests/test_compose_pinned.py` asserting `yaml.safe_load(open('lab/docker-compose.yml'))['services'][s]['image'] not in ["postfix:3.9-alpine","dovecot:2.3","andyshinn/dnsmasq:2.86"]`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 3,13
  References (executor has NO interview context - be exhaustive): lab/docker-compose.yml:1-134 5 services 172.18.0.0/24, docker-compose.yml:1-21 include profiles ["lab"], lab/reassembler/reassemble.py:35-54 TSHARK_REQUIRED_PREFS 4, .github/workflows/ci.yml docker compose config, docs/TSHARK.md 2-lane, Dockerfile:28 tshark layer
  Acceptance criteria (agent-executable): `docker manifest inspect boky/postfix:3.9 >/dev/null && docker manifest inspect dovecot/dovecot:2.3 >/dev/null && docker manifest inspect andyshinn/dnsmasq:2.83 >/dev/null && docker compose config >/dev/null && docker compose --profile lab config >/dev/null && docker compose --profile lab up -d --wait && docker inspect --format '{{.State.Health.Status}}' lab-mockdns 2>/dev/null | grep -q healthy && dig @172.18.0.53 MX lab.local | grep -q 172.18.0.2 && docker compose --profile lab down && ! grep -q "postfix:3.9-alpine" lab/docker-compose.yml && ! grep -q "dovecot:2.3" lab/docker-compose.yml && ! grep -q "andyshinn/dnsmasq:2.86" lab/docker-compose.yml`
  QA scenarios (name the exact tool + invocation): happy - `docker compose config | grep -q demo && docker compose --profile lab config | grep -q mockdns && echo pinned ok` ; failure - keep phantom tag → `docker pull postfix:3.9-alpine` fails with `object not found`; edge - `andyshinn/dnsmasq:latest` fallback if `2.83` rate-limited. Evidence .omo/evidence/.../task-1-*.junit.xml + `docker manifest inspect` logs + `docker compose config` dumps
  Commit: Y | fix(lab): pin phantom images to pullable Hub tags healthcheck intact

- [x] 2. Dockerfile pinned tshark 4.2.* + tini + single-port 8000 harden + per-arch pip guard
  What to do / Must NOT do: Patch `Dockerfile:26-53` runtime stage `RUN apt-get update && (apt-cache madison tshark | grep 4.2 && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends curl tini tshark=4.2.* --allow-downgrades || DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends curl tini tshark) && rm -rf /var/lib/apt/lists/* && tshark --version | grep -Eq "4\.(2|6)" && useradd -m -u 10001 app` + keep builder `gcc python3-dev libffi-dev tshark=4.2.*` (redundant but pinned), ensure `EXPOSE 8000` only no `5173`, `HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 CMD curl -fsS --max-time 2 http://localhost:8000/health || curl -fsS --max-time 2 http://localhost:8000/flows || exit 1`, `USER app`, `PATCH requirements.txt add websockets scapy==2.7.0 requests + dashboard/package.json add ws nuqs`, `ENTRYPOINT ["/usr/bin/tini","--"]`, `CMD ["sh","-c","uvicorn api.app:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"]`, `COPY --from=builder /usr/local/lib/python3.11/site-packages` + `COPY --from=frontend /app/dashboard/dist`, `! grep -q "COPY wheelhouse" Dockerfile`, `.dockerignore:1 wheelhouse/` + `:20 dashboard/dist/` respected. MUST NOT `EXPOSE 5173`, MUST NOT `COPY wheelhouse`, MUST pin tshark, MUST add `--max-time 2` inside timeout. TDD: create `tests/test_dockerfile_pinned.py` asserting `grep -q "tshark=4.2"` and `grep -q "tini" Dockerfile`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 3,13
  References (executor has NO interview context - be exhaustive): Dockerfile:1-53 3-stage node:20-bookworm-slim + python:3.11-slim-bookworm builder/runtime, .dockerignore:1-26, api/app.py:23-25 StaticFiles /dashboard, dashboard/dist/index.html 1.5K, requirements.txt xgboost==1.7.6 no torch, docs/TSHARK.md parity 4 prefs
  Acceptance criteria (agent-executable): `test -f Dockerfile && grep -q "tshark=4.2" Dockerfile && grep -q "tini" Dockerfile && grep -q "HEALTHCHECK" Dockerfile && grep -q "USER app" Dockerfile && grep -q "EXPOSE 8000" Dockerfile && ! grep -q "EXPOSE 5173" Dockerfile && ! grep -q "COPY wheelhouse" Dockerfile && grep -q "websockets" requirements.txt && grep -q "scapy" requirements.txt && grep -q "requests" requirements.txt && docker buildx build --call check . 2>&1 | grep -q "Check complete"`
  QA scenarios (name the exact tool + invocation): happy - `docker build -t ghcr.io/ntro/securemailscope:demo . --load && docker run --rm -p 8000:8000 ghcr.io/ntro/securemailscope:demo & sleep 5; curl -fsS http://localhost:8000/health | jq .status; docker stop $(docker ps -q -f ancestor=ghcr.io/ntro/securemailscope:demo) || true` ; failure - unpinned tshark `bookworm 4.6.x` → `tshark -v | grep 4.2` fails guard; edge - builder `gcc` retained for llvmlite/xgboost manylinux. Evidence .omo/evidence/.../task-2-*.junit.xml + `docker history` + `tshark --version`
  Commit: Y | fix(docker): pin tshark 4.2.* + tini health single 8000

- [x] 3. Two-file lifecycle scripts/turnup.sh pure Docker + scripts/turndown.sh clean with preflight
  What to do / Must NOT do: Rewrite `scripts/turnup.sh:1-417` to pure Docker path: keep `scripts/turnup.sh --check` dry-run checks (python 3.11, node >=18, `python -c "from lab.reassembler.reassemble import get_tshark_prefs; assert len(get_tshark_prefs())==4"` 4-prefs, `du -m wheelhouse <370` no torch, `models/*.pkl prot4 <5M` via `scripts/download_models.sh --check` then `python -m assessment.risk_model` train fallback, `gzip -c dashboard/dist/assets/*.js | wc -c <3670016`), then `check_port_free 8000` via `ss -ltn` fallback `fuser`, then `docker compose up -d --build demo` (and `--profile lab up -d` if `--with-lab` flag) with `wait_for http://localhost:8000/health 30 0.5` + `curl -F pcap=@lab/pcaps/family-01.pcap http://localhost:8000/analyze | jq .[0].assessment`, trap only `INT TERM` (not EXIT auto-down) so demo stays up; detach `nohup` not needed because `docker compose up -d` detaches. Create new `scripts/turndown.sh:1-60` performing `docker compose --profile lab down` + `docker compose down` + `ss -ltnp | grep 8000 || echo cleaned` + `rm -f .tmp/ciphercrest_*.pid` + log rotation `logs/turndown_<ts>.log`, idempotent `--check` mode. Update `README.md:70-71` from `docker pull ghcr.io/...` to `git clone && docker compose up --build` → `http://localhost:8000/dashboard` + `/health` + `/docs`. MUST NOT auto-down on EXIT, MUST NOT start native uvicorn/vite when Docker available, MUST keep `PYTHONHASHSEED=0 OMP_NUM_THREADS=6`, MUST document `WITH_LAB=1` vs default core. TDD: create `scripts/tests/test_turnup_twofile.sh` asserting `grep -q "docker compose up -d" scripts/turnup.sh && test -f scripts/turndown.sh`.
  Parallelization: Wave 2 | Blocked by: 1,2 | Blocks: 7,8,12,13
  References (executor has NO interview context - be exhaustive): scripts/turnup.sh:1-417 trap pgrep ss pid .tmp, scripts/download_models.sh:1-70 Releases, lab/docker-compose.yml 5 services, api/app.py:23-25 /dashboard mount, dashboard/vite.config.js proxy /api→8000, resizable panels react-resizable-panels + shadcn resizable https://ui.shadcn.com/docs/components/resizable, .github/workflows/ci.yml turnup --check, README.md Quick Start
  Acceptance criteria (agent-executable): `bash scripts/turnup.sh --check 2>&1 | grep -q "tshark.*parity 4 prefs" && bash scripts/turndown.sh --check && grep -q "docker compose up -d" scripts/turnup.sh && test -f scripts/turndown.sh && bash scripts/turnup.sh --help | grep -q "with-lab"`
  QA scenarios (name the exact tool + invocation): happy - `bash scripts/turnup.sh --check && bash scripts/turnup.sh & sleep 8; curl -fsS http://localhost:8000/health | jq; bash scripts/turndown.sh; ss -ltnp | grep 8000 || echo cleaned` ; failure - port 8000 occupied → `ss -ltn` warns then `fuser -k 8000/tcp`; edge - `WITH_LAB=0` skips lab compose, demo still healthy via `curl /flows`. Evidence .omo/evidence/.../task-3-*.junit.xml + `docker ps` + `curl /health` log
  Commit: Y | feat(scripts): two-file pure Docker turnup/turndown + port preflight

- [x] 4. .omo/specs/frontend-research-ciphercrest.md full unsummarized research file + 4-skill handoff
  What to do / Must NOT do: Write `.omo/specs/frontend-research-ciphercrest.md` 8-12 pages verbatim covering: (1) SOC metrics taxonomy 5-KPI (MTTD/MTTR/Signal/Coverage/Crypto Posture Score) + crypto extension STARTTLS/Cipher/X.509/MTA-STS via NIST 800-52r2 CBOM CycloneDX, (2) F-pattern vs Z-pattern scan IEEE TVCG 2025 1,216 dashboards, (3) 12-col 1440px 24px gutter 8pt rhythm + 12px radius hairline border + WCAG AAA 7:1 ink #0F172A muted #475569 action #4338CA on canvas #F8FAFC, (4) Inter 400/600/700 + JetBrains Mono variable woff2 font-display swap + tabular-nums, (5) progressive disclosure 2-tier + Hick's log2(n+1) + Gestalt proximity/similarity + 5-second rule + Bento Box, (6) card grid + drag-drop + matrix + hex view + PDF export patterns, (7) threat matrix MITRE heatmap bitsight, (8) report 5-page Cynomi pentest structure, (9) inspiration verdict Donezo emerald vs Shopeers blue → indigo institutional + tokens table → tailwind.config.js + wireframe fidelity per tab (Dashboard low-fi, Families mid-fi, Lab hi-fi, Live mid-fi, Reports low-fi). Hand research from prior librarian tasks (ses_fc...) unsummarized — copy tables + URLs + tokens verbatim, never summarize. Add note `All frontend workers MUST invoke skill(dashboard-design-skill) + skill(information-architecture-navigation) + skill(interaction-patterns-components) + skill(webapp-ui-skill) and read this file in full before any code`. MUST NOT use impeccable, MUST NOT summarize, MUST include `TOK.*` code block for implementors. TDD: create `tests/test_frontend_research.py` asserting `pathlib.Path('.omo/specs/frontend-research-ciphercrest.md').stat().st_size > 50000 and "4338CA" in text and "12-col" in text`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 5,10
  References (executor has NO interview context - be exhaustive): prior librarian deliverables ses_fc... 28 searches + 2026 web citations, dashboard/src/App.jsx:711-825 single page clutter, dashboard/src/tokens.js:1-91 light SOC 91 lines, assessment/ LEDGER per-family, PS_TRACEABILITY.md, research folder STATUS.md + findings.md + PLAN_AUDIT_*
  Acceptance criteria (agent-executable): `test -f .omo/specs/frontend-research-ciphercrest.md && wc -l .omo/specs/frontend-research-ciphercrest.md | awk '{assert $1>400}' && grep -q "dashboard-design-skill" .omo/specs/frontend-research-ciphercrest.md && grep -q "4338CA" .omo/specs/frontend-research-ciphercrest.md && grep -q "12-col" .omo/specs/frontend-research-ciphercrest.md`
  QA scenarios (name the exact tool + invocation): happy - `cat .omo/specs/frontend-research-ciphercrest.md | head -40 | grep -q "information-architecture"` ; failure - summarize trap → `grep -q "TODO summarize" .omo/specs/frontend-research-ciphercrest.md && exit 1`; edge - file handed to subagent via `References:` path, not pasted summary. Evidence .omo/evidence/.../task-4-*.junit.xml + `wc -l` + `head`
  Commit: Y | docs(frontend): unsummarized research spec + 4-skill handoff

- [x] 5. Dashboard shell 5-tab sidebar + BrowserRouter layout + tokens canonicalize
  What to do / Must NOT do: **All subagents executing this todo MUST invoke skill(dashboard-design-skill), skill(information-architecture-navigation), skill(interaction-patterns-components), skill(webapp-ui-skill) AND read `.omo/specs/frontend-research-ciphercrest.md` in full before any code** — follow its tokens, IA, interaction patterns verbatim. Refactor `dashboard/src/App.jsx:711-825` single page into shell: create `dashboard/src/layout/Shell.jsx` with left sidebar `260px expanded icon+label Inter 600 11px uppercase tracking 1` / `64px collapsed rail tooltip 6-8px dot badge` + `nav aria-label Primary` + `aria-current page` + `aria-expanded` toggle + `prefers-reduced-motion`, `localStorage sidebar:collapsed` persist + auto-collapse `<1280px`; `BrowserRouter` + `Routes` `path / → Navigate /dashboard`, nested `Layout > Outlet` for `Dashboard | Families | Lab | Live | Reports` (5 tabs), `nuqs` query sync `?risk&port&tls&page&q`, hash alias `#/flow/:id` → redirect `useEffect` sync for offline StaticFiles. Port `dashboard/src/tokens.js:1-91` exhaustive `canvas #F8FAFC surface #FFFFFF border #E2E8F0 ink #0F172A 17:1 action #4338CA 7.9:1 success #047857 4.6:1` + `radius 12px shadow 0 1px 3px rgba(15,23,42,.06) font-sans Inter Variable + JetBrains Mono` `font-display swap` + `CSP font-src 'self'` + `tabular-nums`; delete duplicates `dashboard/app.jsx` + `dashboard/src/app.jsx` via `git rm`, fix `dashboard/src/components/Gauge.jsx:1` broken `from '../../app.jsx'` → `from '../App.jsx'`. Keep `dashboard/src/main.jsx:1-9` import canonical. MUST keep offline no CDN `! grep -rq fonts.gstatic`, MUST keep `12-col max 1440px 24px gutter 8pt rhythm` F-pattern bands, MUST read research file not summarize.
  Parallelization: Wave 2 | Blocked by: 4 | Blocks: 6,7,8,10
  References (executor has NO interview context - be exhaustive): .omo/specs/frontend-research-ciphercrest.md full file path must be read, dashboard/src/App.jsx:1-826 Gauge ThreatMatrix MasterList DrillDown, dashboard/src/tokens.js:1-91 TOK vars, dashboard/src/main.jsx ReactDOM.createRoot, dashboard/vite.config.js proxy /api→8000, resizable panels react-resizable-panels + shadcn resizable https://ui.shadcn.com/docs/components/resizable, dashboard/package.json react 18.2 recharts 2.12 vite 5, api/app.py:23-25 /dashboard mount, shared/schemas.py FlowVerdict
  Acceptance criteria (agent-executable): `test -f dashboard/src/layout/Shell.jsx && grep -q "BrowserRouter" dashboard/src/layout/Shell.jsx && grep -q "aria-current" dashboard/src/layout/Shell.jsx && test -f dashboard/src/tokens.js && grep -q "#F8FAFC" dashboard/src/tokens.js && ! test -f dashboard/app.jsx && ! test -f dashboard/src/app.jsx && npm --prefix dashboard run build && gzip -c dashboard/dist/assets/*.js | wc -c | awk '{assert $1<3670016}'`
  QA scenarios (name the exact tool + invocation): happy - `npm --prefix dashboard run build && ls dashboard/dist/index.html && grep -q "Shell" dashboard/dist/assets/*.js` ; failure - icon-only nav without labels → `grep -q "aria-label.*Primary" dashboard/src/layout/Shell.jsx || exit 1`; edge - collapsed 64px rail tooltip `title` + `role tooltip` via WAI. Evidence .omo/evidence/.../task-5-*.junit.xml + `npm run build` log + `docker/dist` gzip
  Commit: Y | feat(dashboard): 5-tab sidebar shell + BrowserRouter + tokens canonicalize

- [x] 6. Families tab card grid + Play live streaming into Dashboard
  What to do / Must NOT do: Same 4-skill mandate + read research file. Create `dashboard/src/pages/Families.jsx` mid-fi card grid responsive `1→2→3 cols` `gap 16px` `12-col` for all families `10 + 40 synthetic` from `lab/manifest.json` 50 entries, each card anatomy `media 16:9 cipher icon + badge severity emerald/amber/red-700 icon fallback not color-only + title mono family-id + meta cipher/cert/STARTTLS` + footer `▶ Play` → pushes `POST /api/analyze` via `FormData pcap @ lab/pcaps/<family>.pcap` live one-by-one (100ms stagger) into `Live` continuum + toast `Streaming family-03` + refetch `GET /flows` + `Dashboard` KPIs recompute + `Reports` history timeline version auto-inc via `api/db.py flows_history`; whole-card click → detail split pane `HSplitter 360:480` with `DrillDown` 5 tabs `Handshake/Cert/AI/Coverage/History` + `Hash #/flow/:id` deep link + `aria-selected`. Hover muted preview `HoverPlayCard` 2s loop hex shimmer. MUST NOT modal (use split view to compare behind), MUST keep `MasterList` virtualized `slice(0,10)` paginated 10/page, MUST invoke 4 skills + read research file.
  Parallelization: Wave 3 | Blocked by: 5 | Blocks: 9,14
  References (executor has NO interview context - be exhaustive): .omo/specs/frontend-research-ciphercrest.md wireframes, dashboard/src/pages/Families.jsx to create, dashboard/src/App.jsx MasterList 281-405 + DrillDown 513-609 + hash 664, lab/manifest.json 50 entries after T11, lab/pcaps/*.pcap + jittered *.pcap 1KB, lab/reassembled/*.bin 120B + sha256, api/app.py POST /analyze + GET /flows/history, shared/schemas.py 23 checks
  Acceptance criteria (agent-executable): `test -f dashboard/src/pages/Families.jsx && grep -q "Play" dashboard/src/pages/Families.jsx && grep -q "useNavigate" dashboard/src/pages/Families.jsx && grep -q "dashboard-design-skill" dashboard/src/pages/Families.jsx && npm --prefix dashboard run build | grep -q "Families"`
  QA scenarios (name the exact tool + invocation): happy - `npm --prefix dashboard run dev & sleep 2; curl -s http://localhost:5173 | grep -q "Families"` ; failure - `grep -q "div onClick" dashboard/src/pages/Families.jsx && exit 1` (must use `Link` not div onClick); edge - Play while continuum running → queue `liveQueue 6` badge. Evidence .omo/evidence/.../task-6-*.junit.xml + card grid screenshot
  Commit: Y | feat(dashboard): Families card grid + Play stream

- [x] 7. Lab tab Make workspace matrix that actually synthesizes scapy pcap
  What to do / Must NOT do: Same 4-skill mandate + read research file. Create `dashboard/src/pages/Lab.jsx` hi-fi Make workspace: `2-col gap16` matrix 8 fields `port 25/587/143/110/993`, `TLS 1.0/1.1/1.2/1.3/none`, `cipher IANA+GREASE 16` (CIPHER_OPTIONS via `shared/ja4_rarity.py GREASE_VALUES 16`), `KEX ECDHE/DHE/RSA`, `cert rsa2048/1024/p256/expired/selfsigned/chain-incomplete`, `STARTTLS implicit/starttls-upgrade/cleartext/stripped`, toggles `early_data/psk/ech` — controls `segmented/select/toggle` with strength badge `strong/medium/weak` + lineage side-by-side `manifest.json vs parsed` + `reassembled/*.bin 120B` proof; right sticky `Drop Zone 240px dashed 1.5px #CBD5E1 → TOK.action` + `onDragOver preventDefault + dragOver state + aria-label` + hidden `<input type=file accept=.pcap,.pcapng,.cap,.zip multiple>` + keyboard fallback, `1MiB chunk progress bar` `setTimeout 18ms` + `Progress %`, `Customize & Generate` button synthesizes real pcap via `lab/scripts/synth_families.py --synth-one --port <v> --tls <v> --cipher <v> --kex <v> --cert <v> --starttls <v> --early <v> --out /tmp/synth.pcap` (uses `scapy.layers.tls.record TLSRecord / TLSHandshakes / TLSClientHello` + `patrickmccanna.net/32` recipe) then `FormData append pcap + hints` → `POST /api/analyze` `413 guard >100MiB` + `flow_id:error` branch + `liveQueue` spinner + refetch `GET /flows`. MUST synthesize scapy pcap not just hints, MUST handle states `default/hover/focus/disabled/loading/error/success`, MUST invoke 4 skills + read research file.
  Parallelization: Wave 3 | Blocked by: 3,5,11 | Blocks: 9,14
  References (executor has NO interview context - be exhaustive): .omo/specs/frontend-research-ciphercrest.md Make workspace, dashboard/src/components/PcapCustomizer.jsx:1-446 8-field + drag-drop as reference but must synthesize, lab/scripts/synth_families.py to create (uses scapy TLSRecord Handshake), shared/ja4_rarity.py GREASE_VALUES 16 RFC8701, api/app.py:71-128 chunk 1MiB 413 BadZipFile->error, lab/manifest.json
  Acceptance criteria (agent-executable): `test -f dashboard/src/pages/Lab.jsx && grep -q "synth_families" dashboard/src/pages/Lab.jsx && grep -q "drag.*drop" dashboard/src/pages/Lab.jsx && grep -q "POST.*analyze" dashboard/src/pages/Lab.jsx && python -m lab.scripts.synth_families --synth-one --port 587 --tls TLS1.2 --out /tmp/test_synth.pcap && test -f /tmp/test_synth.pcap`
  QA scenarios (name the exact tool + invocation): happy - `npm --prefix dashboard run build && grep -q "Lab" dashboard/dist/assets/*.js` ; failure - hints only without scapy synthesis → `grep -q "scapy" dashboard/src/pages/Lab.jsx || exit 1`; edge - zip 35 pcaps → `curl -F pcap=@<(zip -j - lab/pcaps/family-0*.pcap) http://localhost:8000/analyze | jq length` 35. Evidence .omo/evidence/.../task-7-*.junit.xml + `/tmp/test_synth.pcap` hex
  Commit: Y | feat(dashboard): Lab Make workspace + scapy synthesis

- [x] 8. Live tab WebSocket hex inspector tri-pane + continuum 60fps
  What to do / Must NOT do: Same 4-skill mandate + read research file. Create `dashboard/src/pages/Live.jsx` mid-fi realtime dashboard: `api/app.py` add `@app.websocket("/ws/flows")` via `FastAPI WebSocket + WebSocketDisconnect` on same `8000` with `await ws.accept()` + initial `ws.send_json([f.model_dump() for f in query_all()])` + broadcast on `POST /analyze` via `asyncio.Queue` fan-out; dashboard `new WebSocket("ws://localhost:8000/ws/flows")` auto-reconnect exponential backoff + `setInterval` heartbeat + fallback `GET /flows 5s` polling + `visibilitychange` pause; top continuum scrolling `ApexCharts xaxis.range 60s` or `Liveline` `requestAnimationFrame` interpolation `transform: translateX` linear `350ms` + `prefers-reduced-motion` discrete steps, `isLive` spinner `App.jsx:738` + `liveQueue` count + bounded `600` points `content-visibility: auto`; middle `order feed virtualized 10/page` + `MasterList` filtered; bottom tri-pane hex inspector (`Frame list direction →/← opcode 1 blue/2 amber/8 grey` | `HEX+ASCII sync JetBrains Mono tabular-nums offset | hex 16 bytes | ASCII` offset sync `hover byte → highlight ASCII` | `Decode FIN/RSV/opcode MASK key unmasked payload` per Wireshark HEX Dump + Kraken). MUST `--workers 1` for in-process broadcast (document `Redis pub/sub` for multi-worker stretch), MUST `transform/opacity` only not `transition: all`, MUST invoke 4 skills + read research file.
  Parallelization: Wave 3 | Blocked by: 3,5 | Blocks: 9,14
  References (executor has NO interview context - be exhaustive): .omo/specs/frontend-research-ciphercrest.md Hex view + continuum, api/app.py:1-170 REST + WS to add, api/db.py query_all/upsert_flows, dashboard/src/App.jsx isLive 738 + poll 655, uvicorn docs wsproto, websockets, FastAPI WebSockets, lab/reassembled/*.bin sha256 coverage_ratio
  Acceptance criteria (agent-executable): `grep -q "WebSocket" api/app.py && grep -q "ws/flows" api/app.py && test -f dashboard/src/pages/Live.jsx && grep -q "HEX" dashboard/src/pages/Live.jsx && python -c "from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); assert any(r.path=='/ws/flows' for r in app.routes)"`
  QA scenarios (name the exact tool + invocation): happy - `uvicorn api.app:app --port 8000 & sleep 3; websocat ws://localhost:8000/ws/flows --ping-interval 5 | head -5 | grep -q "flow_id"` ; failure - `grep -q "workers 4" Dockerfile && exit 1` (must be 1 for WS); edge - WS disconnect → fallback `GET /flows` polling still works `curl -s http://localhost:8000/flows | jq length`. Evidence .omo/evidence/.../task-8-*.junit.xml + `websocat` capture + `curl /flows`
  Commit: Y | feat(live): WebSocket tri-pane hex + continuum

- [x] 9. Continuum generator scapy synthetic random every 2-5s broadcast
  What to do / Must NOT do: Create `lab/scripts/continuum.py:1-200` background generator synthesizing random TLS variant every `2-5s` (`port 25/587/143/110/993`, `TLS 1.0/1.1/1.2/1.3/none`, `cipher IANA+GREASE 16` filtered `filter_grease`, `KEX ECDHE/DHE/RSA`, `cert cert_type random`, `STARTTLS implicit/upgrade/stripped`, `early_data/psk/ech` toggles) via scapy `TLSRecord/version + TLSClientHello(cipher_suites=list(range(0xff)))` with `hashlib.sha256 %32` deterministic per `PYTHONHASHSEED 0`, `scapy.utils.wrpcap` to `/tmp/continuum.pcap` then `requests.post("http://localhost:8000/analyze", files={"pcap": open(pcap,"rb")})` → broadcast via `api/app.py` WS `asyncio.Queue` fan-out; Dashboard `Live` + `Dashboard` KPIs recompute live (`posture 0-100 gauge`, `policy_dist donut`, `calibrated_prob histogram`, `anomaly scatter thresholds 16.5/14.9 dashed`, `trend capture_epoch line`, `ja4_rarity 0.926 bar`) from `GET /report?format=json`. Include `if DASHBOARD_LIVE_CONTINUUM env` toggle + `jitter_slices` not used (independent capital). MUST every `2-5s` random not fixed families, MUST scapy synthetic not jitter-copy, MUST honor `PYTHONHASHSEED 0`.
  Parallelization: Wave 4 | Blocked by: 6,7,8,12 | Blocks: 14
  References (executor has NO interview context - be exhaustive): lab/scripts/synth_families.py synthesis recipe, lab/scripts/jitter_slices.py -- not to use for continuum, api/app.py WS broadcast, api/db.py query_all <50ms from SQLite, dashboard/src/components/Graphs.jsx 326 6 Recharts + calibration inline, eval/anomaly_baselines.json dual thresholds, shared/ja4_rarity.py GREASE 16
  Acceptance criteria (agent-executable): `test -f lab/scripts/continuum.py && python lab/scripts/continuum.py --count 3 --dry-run | grep -q "synthetic.*random" && python -c "import lab.scripts.continuum; print('continuum ok')"`
  QA scenarios (name the exact tool + invocation): happy - `python lab/scripts/continuum.py --count 5 --interval 2 & sleep 12; curl -s http://localhost:8000/flows | jq length | grep -q "[5-9]"` ; failure - fixed families loop → `grep -q "jitter_slices" lab/scripts/continuum.py && exit 1`; edge - WS disconnected → still `POST /analyze` persists via `query_all`. Evidence .omo/evidence/.../task-9-*.junit.xml + `continuum --dry-run` log
  Commit: Y | feat(lab): scapy synthetic random continuum 2-5s

- [x] 10. Reports tab per-finding cards + PDF/PNG/JSON export
  What to do / Must NOT do: Same 4-skill mandate + read research file. Create `dashboard/src/pages/Reports.jsx` Export Hub: header `Export Report` specific label + `segmented Client quick / Server print` toggle; preview report `KPIs+ThreatMatrix+Timeline triple 127.0.0.11:54330 + HonestyBanner 14/20 REAL` with `@media print` overrides `nav hidden sticky table header repeat`; actions `PDF / PNG / JSON` — Client path `html2canvas scale 2x DPR + jsPDF addImage + jsPDF.text headings selectable text` await `document.fonts.ready + chart animationComplete` via `onclone width 2480px A4 300dpi overflow visible`, Server path `POST /report?format=json` → `Puppeteer/Chromium viewport 1600×1200 A4 landscape scale 0.9 margin 15mm` (stretch: add `api/report_pdf.py` if needed). Per-finding cards: each of 23 checks `severity chip emerald#047857/amber#B45309/red-700 + icon fallback not color-only + spec RFC/CVE + evidence pre_tls_buffer_len/coverage_ratio + remediation + lineage badge manifest vs parsed`. Table `flows × 23 checks` sticky `overscroll-behavior: contain`. MUST `label`+`aria-label` not placeholder only, MUST hide `nav+aside` in print, MUST invoke 4 skills + read research file.
  Parallelization: Wave 4 | Blocked by: 4,5 | Blocks: 14
  References (executor has NO interview context - be exhaustive): .omo/specs/frontend-research-ciphercrest.md report 5-page, dashboard/src/components/CoverageTable.jsx 103 + ThreatMatrix, dashboard/src/components/Graphs.jsx 326 6 charts, api/app.py GET /report?format=json 163-170 {flows,summary:{posture,policy_dist}}, lab/manifest.json lineage, assessment/rules.py 23 checks
  Acceptance criteria (agent-executable): `test -f dashboard/src/pages/Reports.jsx && grep -q "html2canvas" dashboard/src/pages/Reports.jsx && grep -q "GET.*report" dashboard/src/pages/Reports.jsx && curl -s "http://localhost:8000/report?format=json" | jq -e '.flows and .summary'`
  QA scenarios (name the exact tool + invocation): happy - `npm --prefix dashboard run build && grep -q "Reports" dashboard/dist/assets/*.js && curl -s "http://localhost:8000/report?format=json" | jq .summary.policy_dist` ; failure - `html2canvas` without `fonts.ready` → empty canvas `grep -q "fonts.ready" dashboard/src/pages/Reports.jsx || exit 1`; edge - `@media print` preview toggle `width 2480px`. Evidence .omo/evidence/.../task-10-*.junit.xml + `curl /report` + export toast log
  Commit: Y | feat(dashboard): Reports cards + PDF/PNG/JSON export

- [x] 11. 50-family expansion scapy+weberblog+Censys + splits rebalance + risk/anomaly retrain honest
  What to do / Must NOT do: Create `lab/scripts/synth_families.py:1-300` scapy random synthesis (`--count 40 --seed 0 --out lab/pcaps --manifest lab/manifest.json`) generating `family-11..50` distinct `flow_id`/`environment_id family-XX__synth_random_seedN` + distinct 5-tuple pcap via `TLSRecord / TLSClientHello(ciphers=GREASE-filtered IANA) + STARTTLS Bennett` per `patrickmccanna.net/32`, `scapy.utils.wrpcap`, `PYTHONHASHSEED 0` deterministic `hashlib.sha256`. Add `weberblog` `+12` real SMTP/STARTTLS 587/465 pcaps via `webberblog.net/the-ultimate-pcap` `tshark -T json` 4 prefs parse + `Censys +15` `services.smtp.starttls + services.tls.certificate` via `search.censys.io` `datasets/{dataset}/snapshots` sampled into `shared/fixtures/censys_sampled_200.json` (+15). Patch `lab/manifest.json` `45→85` (or `50` minimum `10+12+15+13=50`) + `lab/LEDGER.md` per-family provenance + `shared/fixtures/censys_sampled_200.json` expanded 20→35. Patch `assessment/splits.json` `D1 30/25 families 60%`, `D2 15 families 30% → n_cal15 3 bins max(2,n_val//5)`, `D3 10 families held-out`, `D_prior 20→35 ratio 2.33<3` `StratifiedGroupKFold 5` safe `5≤50`, `WEAK SUPERVISION` verbatim. Retrain `assessment/risk_train.py` `XGB max_depth 1-2 reg_lambda 5-10 min_child_weight 3-5` stump `LeaveOneGroupOut 50-fold` + `CalibratedClassifierCV sigmoid cv=2` Platt only + `family_bootstrap 2000` + `leakage_gap <0.15` + `n_bins 3` + `permutation_test_score 1000` p, `anomaly_train.py` ECOD `contamination 0.10 n_jobs 1` dual `7c+20lab` honest primary `0.47→~0.45` vs inverted vs `ja4 0.926` > ECOD, `models/risk_clf.pkl prot4 <5M 0.16M` + `anomaly.pkl 0.014M` regenerated. MUST independent capital not jitter-copy (`_load_lab_flows` only `ja4_rarity` random is jitter), MUST `p/n 0.10` disclosure, MUST `PYTHONHASHSEED 0`.
  Parallelization: Wave 2 | Blocked by: - | Blocks: 7,12,14
  References (executor has NO interview context - be exhaustive): lab/scripts/synth_families.py to create, lab/manifest.json 45 envs, lab/LEDGER.md, assessment/splits.json 45 groups_by_env, assessment/risk_dataset.py XGB_PARAMS PARAM_GRID, assessment/risk_train.py LOFAM LOGO 10→50, assessment/anomaly_data.py _load_lab 45 + _build_training_matrix 27×5 TOP5 → 50×5, eval/metrics.json risk ece 2-bin 0.21 →3-bin ±0.055, eval/LEAKAGE_REPORT.md gap 0.09, Cawley JMLR 2010 p/n, Niculescu-Mizil ICML05 Platt, Li TKDE 2022 ECOD, Hoeffding ±0.19 at 50
  Acceptance criteria (agent-executable): `python -c "import json; s=json.load(open('assessment/splits.json')); assert len(s['all_environment_ids'])>=50 and len(s['D1_train_groups'])>=25 and len(s['D2_val_groups'])>=12 and len(set(s['D1_train_groups']) & set(s['D_prior_groups']))==0" && python -c "from assessment.features import FEATURES_TOP5; assert len(FEATURES_TOP5)==5" && test -f models/risk_clf.pkl && python -c "import pickle; m=pickle.load(open('models/risk_clf.pkl','rb')); assert m.get_params()['max_depth'] in [1,2]" && python -c "import json; j=json.load(open('eval/metrics.json')); assert j['risk']['n_eff']>=50 or j['risk']['n_risk']>=50" && test $(sha256sum lab/pcaps/*.pcap | cut -d' ' -f1 | sort -u | wc -l) -eq $(ls lab/pcaps/*.pcap | wc -l) && ! grep -rq "jitter_slices" lab/scripts/synth_families.py`
  QA scenarios (name the exact tool + invocation): happy - `python lab/scripts/synth_families.py --count 40 --seed 0 --dry-run | grep -q "family-11"` + `pytest assessment/tests/test_splits.py -q` ; failure - add jitter to families 11-50 → `grep -q "jitter_slices" lab/scripts/synth_families.py && exit 1` (must be independent); edge - `D2 15 → n_bins 3` with counts `5,5,5` not `[6,6]` degenerate. Evidence .omo/evidence/.../task-11-*.junit.xml + `splits.json` + `metrics.json` + `calibration_curve.png 750×600 3-bin`
  Commit: Y | feat(data): 50-family honest expansion + splits 60/30 + ML retrain

- [x] 12. api/app.py WebSocket endpoint + SQLite flows_history versioning + continuum storage wiring
  What to do / Must NOT do: Patch `api/app.py:1-170` add `from fastapi import WebSocket, WebSocketDisconnect` + `@app.websocket("/ws/flows")` accept + `await ws.send_json([FlowVerdict.model_validate(upsert).model_dump() for f in (query_all() or [])])` + `asyncio.Queue` broadcaster fed by `POST /analyze` `_real_pipeline_for_bytes` after `upsert_flows` enrich (`calibrated_prob = risk_clf.predict_proba(vector_top5)[0,1]`, `anomaly_score = anomaly_clf.decision_function([vector])[0]`, `anomaly_honest_score` alias) + `flows_history(flow_id TEXT, version INTEGER, data TEXT, created_at TEXT PRIMARY KEY(flow_id,version))` migration in `api/db.py:137` `init_db()` + `upsert_flows` inserts `history` version auto-inc `COALESCE(MAX(version),0)+1` before `INSERT OR REPLACE flows`, helpers `query_history(flow_id)` + `query_all_history()`, endpoints `GET /flows/history?flow_id=family-09` paginated `limit offset` + `GET /health {"status":"ok"}`. Keep chunk `1MiB` `413 if >100MB` + `BadZipFile→flow_id:error`, `GET /flows <50ms` from `query_all` else `_last_result` else stub, `GET /report?format=json {flows,summary:{posture,policy_dist}}` 400 if not json, lazy `models/risk_clf.pkl anomaly.pkl anomaly_honest.pkl` `try pickle.load else None` fallback `calibrated_prob None` still `200` cold `<3s`. MUST NOT break cold `<3s`, MUST keep `PRIMARY KEY(flow_id)` flows, MUST single-worker safe via `asyncio`.
  Parallelization: Wave 3 | Blocked by: 3,11 | Blocks: 9,14
  References (executor has NO interview context - be exhaustive): api/app.py:1-249 _real_pipeline_for_bytes analyze _db_upsert _db_query_all, api/db.py:1-137 init_db/upsert_flows/query_all flows table, api/ml_enrich.py:21-49 lazy _ensure_models, assessment/features.py TOP5 build_vector_top5, models/risk_clf.pkl 0.16M anomaly.pkl 0.014M, shared/schemas.py FlowVerdict calibrated_prob anomaly_score extra-forbid, lab/scripts/continuum.py broadcast queue
  Acceptance criteria (agent-executable): `pytest api/tests/test_api.py api/tests/test_api_ml_wiring.py -q && python -c "from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); assert c.get('/health').status_code==200" && python -c "from api.db import query_history; query_history('family-09'); print('history ok')"` + `python -c "from fastapi.testclient import TestClient; from api.app import app; import inspect; assert 'ws/flows' in str([r.path for r in app.routes])"`
  QA scenarios (name the exact tool + invocation): happy - `TestClient POST /analyze zip 3 → GET /flows/history?flow_id=family-09` returns version 2 + `websocat ws://localhost:8000/ws/flows | head -3` receives JSON; failure - delete `models/*.pkl` → API still `200 calibrated_prob None`; edge - malformed pcap → `flow_id:error` not 500. Evidence .omo/evidence/.../task-12-*.junit.xml + `GET /flows <50ms` + `POST zip` log + WS capture
  Commit: Y | feat(api): WS + versioned history + ML enrich wiring

- [x] 13. .github/workflows/ci.yml hard-fail everything incl docker buildx + compose --profile lab up --wait
  What to do / Must NOT do: Rewrite `.github/workflows/ci.yml:1-250` to hard-fail all: jobs `Install deps air-gap`, `Build offline artifacts if missing`, `Schema tests`, `Features 28 + TOP5`, `XGB categorical guard hist enable_categorical depth 1-2 max_cat 8`, `Platt only !isotonic`, `ja4 whitelist`, `Splits 50-family grouping 60/30/20 + prior disjoint`, `D5 temporal`, `Metrics.json hard-fail brier<base ece<0.30 kappa>0.45 bootstrap 2000 ece_bins 3 at n=50`, `Pkl prot4 size <5M <1M`, `Feature28 max_cat Vite gzip <3670016`, `Wheelhouse lean <370 no torch`, `TShark parity 4 prefs` `get_tshark_prefs len 4` + `tshark -v | grep 4.2`, `Turnup dry-run --check` + `turndown --check`, `Docker buildx --platform linux/amd64,linux/arm64` via `docker/setup-qemu-action@v3 + setup-buildx-action@v3 + build-push-action@v6`, `docker compose config` + `docker compose --profile lab up -d --wait && docker inspect healthy && dig @172.18.0.53 MX`, `Collect-only >=80 tests >=8 suites`, `Pkl + size + vite + health curl /health`. Add `actions/cache` for `wheelhouse` `pip download` + `dashboard/dist`. Keep `env PYTHONHASHSEED 0 OMP_NUM_THREADS 6`. MUST hard-fail docker compose pull not `config` only, MUST `continue-on-error: false` for TShark now pinned, MUST `lfs: true` retained.
  Parallelization: Wave 4 | Blocked by: 1,2,3,5,11 | Blocks: 14
  References (executor has NO interview context - be exhaustive): .github/workflows/ci.yml:1-250 25 jobs, scripts/turnup.sh --check + turndown.sh --check, Dockerfile tshark pinned 4.2, docker-compose.yml + lab/docker-compose.yml pinned tags, assessment/splits.json 50, eval/metrics.json hard schema shared/schemas_eval.py WEAK_SUPERVISION, dashboard/dist gzip 185k, wheelhouse 361M, lab/reassembler/reassemble.py 4 prefs
  Acceptance criteria (agent-executable): `cat .github/workflows/ci.yml | grep -q "docker compose --profile lab up -d --wait" && cat .github/workflows/ci.yml | grep -q "hard-fail" && python -m py_compile $(git ls-files '*.py' | head -20 | xargs) && grep -q "docker buildx" .github/workflows/ci.yml && grep -q "inspect.*healthy" .github/workflows/ci.yml && grep -q "continuum.*--dry-run" .github/workflows/ci.yml || yamllint .github/workflows/ci.yml 2>&1 | head -5`
  QA scenarios (name the exact tool + invocation): happy - `act -n --container-architecture linux/amd64 2>&1 | head` dry-run + `pytest --collect-only -q | grep "tests collected"` ; failure - `docker compose --profile lab up -d --wait` timeout → `dig @mockdns` fails guard; edge - `wheelhouse` cache miss → `pip download 361M` still passes after rebuild. Evidence .omo/evidence/.../task-13-*.junit.xml + `docker compose config` + `tshark -v`
  Commit: Y | ci: hard-fail everything incl docker buildx + lab up

- [x] 14. eval/EVIDENCE_Day13 6.5/8 interim honest + metrics 3-bin + LEAKAGE gap shrink + docs alignment
  What to do / Must NOT do: Update `eval/metrics.json` canonical `risk: {ece_3bin,ece_kernel,brier,brier_base_rate,leakage_gap 0.05 lofam 0.60 env 0.65 gap<0.15,perm p,brier_ci,bootstrap_n 2000,ece_bins 3,ap}` + flat aliases, anomaly dual `ecod_honest 0.45 vs inverted 0.87 ja4 0.926 contrast 5 entries thresholds honest c05 17.869` + `ndcg delta -0.005±0.08 CI` + `kappa 0.81/0.78` + `n_eff 50 n_risk 85 n_prior 35` + `p/n 0.10`. Write `eval/EVIDENCE_Day13.md` FINAL SYSTEM 6.5/8 interim green honest custody (5/8→6.5/8, 8/8 blocked) sections `Brier+ECE 3-bin 2000-boot`, `LOFAM 50-fold vs EnvCV gap 0.05`, `permutation p`, `dual ECOD honest vs ja4`, `NDCG tie`, `trio lineage manifest 85→reassembled→features vs tshark 4 prefs`, `per-port 25/587/993 + R1-R8 14/20 REAL +3 info`, `Docker single 8000 health + lab 5 healthy + WS tri-pane`, `50-family provenance weber/Censys/scapy`. Update `eval/LEAKAGE_REPORT.md` table `Model|p|n_eff|p/n|EnvCV|LOFAM|Gap|Honest?`, `eval/anomaly_baselines.json` 5 entries, `eval/calibration_curve.png 750×600 3-bin [5,5,5]`, `eval/risk_pr.png`. Keep `eval/human_grades.csv 20×3` κ>0.6 + `lab/LEDGER.md 85 rows` + `assessment/LEDGER.md` + `shared/progress.md` daily poll + `PS_TRACEABILITY.md` PS→family→rule 23→evidence mapping + `README.md` `docker compose after clone` + `docs/LARGE_FILES.md` Releases. MUST disclose `WEAK SUPERVISION n_eff 50` verbatim + `p/n 0.10` + `ECE 3-bin caveat n_val15 5,5,5`.
  Parallelization: Wave 4 | Blocked by: 6,7,8,9,10,11,12,13 | Blocks: -
  References (executor has NO interview context - be exhaustive): eval/metrics.json hard schema shared/schemas_eval.py WEAK_SUPERVISION_VERBATIM, eval/EVIDENCE_Day10.md SYSTEM 5/8 + Day12 8/8 annex, assessment/risk_metrics.py _ece 3-bin + family_bootstrap 2000, eval/anomaly_baselines.json dual, eval/human_grades.csv κ 0.81, lab/LEDGER.md 85, assessment/splits.json 50, lab/manifest.json 85
  Acceptance criteria (agent-executable): `test -f eval/EVIDENCE_Day13.md && grep -q "SYSTEM 6.5/8" eval/EVIDENCE_Day13.md && ! grep -q "SYSTEM 8/8" eval/EVIDENCE_Day13.md && grep -q "WEAK SUPERVISION" eval/EVIDENCE_Day13.md && python -c "from shared.schemas_eval import load_and_validate; load_and_validate(); print('metrics hard-fail ok')" && python -c "import json; j=json.load(open('eval/metrics.json')); assert j['risk']['leakage_gap']<0.15 and j['risk']['bootstrap_n']==2000 and j['risk']['ece_bins']==3"`
  QA scenarios (name the exact tool + invocation): happy - `pytest eval/tests/test_metrics_json.py -q && cat eval/EVIDENCE_Day13.md | head -40 | grep -q "LOFAM"` ; failure - `brier<base false` → red `python -c "assert j['risk']['brier']<j['risk']['brier_base_rate']"` fail; edge - `ece_bins 3` counts `[5,5,5]` 15. Evidence .omo/evidence/.../task-14-*.junit.xml + metrics.json + LEAKAGE_REPORT + calibration_curve.png
  Commit: Y | docs(eval): EVIDENCE_Day13 6.5/8 interim 50-family + LEAKAGE



## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [x] F1. Docker turnup truth — bash scripts/turnup.sh --check && bash scripts/turnup.sh & sleep 8; curl -fsS http://localhost:8000/health && curl -fsS http://localhost:8000/dashboard | grep Shell; bash scripts/turndown.sh; ss -ltn | grep 8000 || echo cleaned + docker compose --profile lab up -d --wait && docker inspect healthy && dig @172.18.0.53 MX lab.local | grep 172.18.0.2
- [x] F2. CI hard-fail gate — pytest --collect-only -q | grep "tests collected" && python -m py_compile $(git ls-files '*.py' | head -20 | xargs) && grep -q "docker buildx" .github/workflows/ci.yml && grep -q "inspect.*healthy" .github/workflows/ci.yml
- [x] F3. Frontend 5-tab + synthesis proof — npm --prefix dashboard run build && npx serve -s dashboard/dist -l 8001 & sleep 2; curl -s http://localhost:8001 | grep -q "Families" && npm --prefix dashboard run test -- --run && python -m lab.scripts.synth_families --synth-one --port 587 --tls TLS1.2 --out /tmp/test_synth.pcap && tshark -r /tmp/test_synth.pcap -T json | jq length
- [x] F4. Backend endpoints metrics — curl -s http://localhost:8000/report?format=json | jq -e '.flows and .summary.policy_dist' && curl -s "http://localhost:8000/flows/history?flow_id=family-09" | jq -e '.[0].version' && websocat ws://localhost:8000/ws/flows --ping-interval 5 | head -3 | grep flow_id

## Commit strategy
- Atomic per todo: `feat(docker)`, `feat(dashboard)`, `feat(lab)`, `feat(api)`, `ci`, `docs(eval)` — each with `Co-authored-by: Prometheus` + evidence path in body.

## Success criteria
- `docker compose up --build` → `curl /health 200` + `curl /dashboard 200` + `lab 5 healthy` + `ws /ws/flows` JSON
- `assessment/splits.json n_eff>=50` + `models/risk_clf.pkl prot4` + `eval/metrics.json ece_bins 3 leakage_gap<0.15 brier<base`
- `dashboard 5 tabs` + `Lab synthesis scapy distinct sha256` + `Live hex tri-pane` + `Reports PDF/JSON`
- `CI 25 jobs hard-fail green` + `pytest --collect-only >=80 tests` + `vite gzip 185k <3670016`
