# sih26159-day5-day6-api-policy-splits - Work Plan

## TL;DR (For humans)

**What you'll get:** By end of Day 6 the offline pipeline is callable as a product: `POST /analyze` accepts a 10-family zip → returns 10 `FlowVerdict` with `policy` (deliver/banner/quarantine/hold_incident) + `posture_score`, `GET /flows` serves from SQLite JSONB without re-parse, dashboard shows the 23-column threat matrix (20 scored color +3 info-greyed) with per-port/per-version honesty, and the 4-dataset split + lean wheelhouse (<350 MB) unblock Day7 ML without touching stretch gateway.

**Why this approach:** Keep gateway as *lean policy only* (no quarantine/siem/arf/milter yet) so Day5-6 harden the **replay lane** judges actually grade (`reassemble→parse→validate→assess→score→policy→SQLite→API→Dashboard`), while `assessment/splits.json` (groups=`environment_id`, ~12 lean, `prior_flag` disjoint) and censys prior materialize Day5 so Day7 XGB/ECOD can train on T1 only without Censys leakage — ULTRA-safe air-gap.

**What it will NOT do:** No `quarantine.py`/`siem.py`/`arf.py`/`QuarantineConsole`/`digest cron`/`milter 127.0.0.1:10025`/`mockdns` live demo beyond fixture read — cut-order 0 stretch deferred to Day 10 if ahead. No MicroAE/torch in wheelhouse (lean <350 MB). No ML training (XGB/ECOD/Platt) — that's Day7-10. No per-split B1-B5 annex tables.

**Effort:** Large (12 todos, 4 waves, ~14h wall, 5-agent parallel)
**Risk:** Low-Medium — `POST /analyze` real pipeline + SQLite JSONB is the flakiest gate (streaming, USE_STUB flip, FlowVerdict validation); mitigated by chunk-read 1 MiB + temp file + model_validate guard + `GET /flows` fallback
**Decisions to sanity-check:** Lean policy only (not full gateway), splits.json created Day5 (not deferred), wheelhouse lean <350 MB (not 800 MB with torch), EVIDENCE_Day5-6 gates SYSTEM 5/8 only (ML gates Day7)

Your next move: approve plan → `/start-work sih26159-day5-day6-api-policy-splits`. Full execution detail follows below.

---

> TL;DR (machine): Large / Low-Med — Day5-6 API SQLite JSONB + lean policy + splits.json 12 groups + censys prior + lean wheelhouse <350M + Day5-6 SYSTEM 5/8 EVIDENCE

## Scope
### Must have
- Assessment lean policy: `assessment/policy.py` deterministic `decide(verdict: FlowVerdict) -> PolicyDecision` mapping Low (<10)→`allow` (X-SMS-Risk Low), Medium (10-24)→`allow` + banner yellow, High (25-39)→`quarantine`/`flag`, Critical (≥40)→`block`/`hold_incident` + `siem_severity` mirrors `risk_level`; Info-weighted 15b/16b/16c only tip level when High-triggered; 7 fixture asserts (F1 Low→allow, F3 Medium→banner, F4 Critical→hold_incident, F9 single→High banner low-conf not Critical, F9 triple→Critical quarantine, F6 opaque→Low banner-only, F7 expired+SHA1→Critical); `PolicyDecision` literals kept as `shared/schemas.py:123` (`allow/quarantine/block/flag`) with alias map to spec `deliver/banner/quarantine/hold_incident` in policy layer only
- ML data charter Day5: `assessment/splits.json` with `D1_train_groups`, `D2_val_groups`, `D3_locked_groups`, `D_prior_groups`, `D5_temporal_same_env`, `all_environment_ids`, `groups_by_env` (~12 lean, 30 stretch), `prior_flag` disjoint guards, `unique(env_id)≥5` + `max/min<3`, `StratifiedGroupKFold(n_splits=5, groups=environment_id)` contract; `shared/fixtures/censys_sampled_200.json` prior shell (if not yet 200, create 20-row minimum lean shard with `prior_flag:true`, `chain_valid/san_match/days_to_expiry None`, `ja4_rarity` span 0..1, `cert_missing_reason` null) + guards `chain_valid is None` + `ja4_rarity 0..1` + `locked ∩ (train∪prior)==∅`
- Lab manifest env wiring: `lab/manifest.json` + `lab/LEDGER.md` carry `environment_id` (`<family>__postfix3.9_loss0`), `capture_epoch`, `source_id`, `docker_image_sha256`, `tshark_version` for §4a grouping audit; `shared/fixtures/family-*.json` each carry `environment_id`+`capture_epoch`+`source_id`+`coverage_ratio`+`pre_tls_buffer_len`/`pre_tls_buffer_injection_possible` validated via `FlowVerdict.model_validate_json`
- API hardening: `api/app.py` `POST /analyze` multipart chunk-read 1 MiB streaming to temp file (not `await file.read()` bulk), `size` running guard → 413 on >100 MB, extension+magic validation + rewind `seek(0)`, `zip` fan-out with per-inner `hint_name`, branch `if USE_STUB: stub_reassemble else _real_pipeline_for_bytes` (fix dead stub both branches already patched), `FlowVerdict.model_validate` hard-fail before DB insert, `error flow_id: error` JSON not crash on malformed, `GET /flows` polls `_last_result` else SQLite JSONB `_db_query_all` else stub fallback, cold-start <3s, `report<5s`
- DB: `api/db.py` + `api/app.py::_db_upsert/_db_query_all` SQLite `flows(flow_id PRIMARY KEY, data TEXT)` with `json_extract` if JSON1 available else TEXT fallback, `<1ms` point lookup on <100 rows, `INSERT OR REPLACE`, `GET /flows` <50ms, `_init_db` idempotent, `FlowVerdict` schema with `policy` validated; `assessment.db` alias if needed but Day5 uses `api/flows.db` (no Postgres — SIH lightweight)
- Dashboard glue: `dashboard/components/CoverageTable.jsx` per-port 25/587/993 + MX 25 compliance vs RFC8314 M02 + M3AAWG + RFC8461/RFC7672, `dashboard/app.jsx` top Honesty banner if any `cert.is_tls13_opaque` → blue banner + greyed cert tab + legend `14/20 REAL per-version scored +3 info (injection/MX/0-RTT) per V2/V4/MX` triple citation M03+M18+M22, ThreatMatrix rows=flows cols=23 (20 scored color +3 greyed info 15b,16b,16c), `USE_STUB` conditional flip polling `shared/progress.md` 🟢 + `lab/LEDGER.md` coverage + `lab/pcaps/jittered/*.pcap` exists
- Offline bundle Day5 lean: `pip download --only-binary=:all: -r requirements.txt -d wheelhouse/` lean <350 MB (ECOD+XGB only, no torch), `xgboost==2.1.0` manylinux + `pyod==1.1.0` ECOD + `scikit-learn` + `cryptography` wheels, `du -m wheelhouse | tail -1 <350` guard, `pip install --no-index --find-links wheelhouse` offline check
- Ledgers & evidence: `shared/progress.md` daily poll 🟢/🟡/🔴, `lab/LEDGER.md`, `analyzer/LEDGER.md`, `validator/LEDGER.md`, `assessment/LEDGER.md` per-family rows, `eval/EVIDENCE_Day5.md` + `eval/EVIDENCE_Day6.md` SYSTEM 5/8 snapshots (STARTTLS F1>95% lossy/weberblog, cipher exact >98%, cert prec>90% stratified, weak recall 100%, JSON 20/20, plus API cold-start <3s, Vite bundle <3.5MB gz if built), CI gates hard-fail

### Must NOT have (guardrails, anti-slop, scope boundaries)
- No `quarantine.py`/`siem.py`/`arf.py`/`QuarantineConsole`/`digest cron`/`milter 127.0.0.1:10025`/`mockdns` live beyond fixture read (`shared/data/mta-sts-fixture.json`/`dane-tlsa-fixture.json` only) — cut-order 0 stretch; Day5-6 policy is lean `policy.py` only
- No `torch==2.4.0` CPU 180MB in wheelhouse (lean <350MB gate would fail); MicroAE 27-8-1 stretch gated `n≥50+ahead` remains Day7-10 stretch
- No XGB/Platt/ECOD/IF training or calibration on Day5-6 — `risk_model.py`/`anomaly_model.py` remain stub/none; splits.json is the only ML artefact Day5
- No `quarantine` table with raw body attach — `flows` table only; quarantine link model deferred
- No breaking rename of `TLS.cipher_strength` or `FlowVerdict.starttls_mode` or `PolicyDecision.action` literals — freeze additive-only per `shared/CONTRIBUTING.md` CODEOWNER P1; alias map in policy only
- No B1-B5 per-split annex, no overall pooled ML validity table — that's Day10 stretch; Day6 EVIDENCE is ONE deterministic SYSTEM table only + ML lean shell
- No `transformer BERT` on raw pcap, no `isotonic` at n<100, no `as any`/`unwrap`/`panic`, no file >250 LOC without split

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: TDD for policy/splits guards (write failing test first from fixtures, then implement to green), tests-after for API hardening (augment existing `api/tests/test_api.py` + `assessment/tests/test_rules.py`). Framework: `pytest -q --junitxml=<attemptDir>/task-N-*.junit.xml` + `python -m py_compile` + `pip install --no-index --find-links wheelhouse --only-binary=:all:` offline check, plus `pytest --collect-only` for splits count, plus `du -m wheelhouse` size gate
- Evidence: `.omo/evidence/ulw/<session>/<goalId>/a<attempt>/task-<N>-sih26159-day5-day6-api-policy-splits.*` (outside ulw-loop use `.omo/evidence/task-<N>-sih26159-day5-day6-api-policy-splits.*`). Each todo produces junit xml + log + optional png/json fixture. No human CLI witness required.

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.

- Wave 1 (Day5 AM, 3h): T3 Censys prior shell (creates lean 20 prior rows) → T1 Lean policy + T2 `assessment/splits.json` — T3 must precede T2 (D_prior_groups disjoint), T1 can parallel with T3
- Wave 2 (Day5 PM, 4h): T4 SQLite JSONB `api/db.py` (canonical 4 funcs, move inline from api/app.py) + T6 lean wheelhouse parallel — then T5 `api/app.py` POST /analyze (blocked by T4, needs init_db/upsert_flows/query_all); T4 serializes before T5 per Dependency Matrix
- Wave 3 (Day6 AM, 4h): T7 Dashboard CoverageTable 23×3 + honesty banner hardening — depends on Wave2 API `GET /flows` 🟢; T8 Dashboard live binding + api/tests + T9 LEDGER updates — T8 blocked by T7 ledger coverage, T9 after T7
- Wave 4 (Day6 PM, 3h): T10 EVIDENCE Day5/Day6 SYSTEM 5/8 snapshots + T11 CI hard-fail guards — depends on T8 ledger 🟢; T12 Final verification wave — after T10+11

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | - | 4,8,10 | 2,3,6 |
| 2 | 3 | 4,8,10,11 | 1,6 |
| 3 | - | 2,10,11 | 1,6 |
| 4 | 1,2 | 5,7 | 6 |
| 5 | 4 | 7,8,10 | 6 |
| 6 | - | 11 | 1,2,3 |
| 7 | 4,5 | 8,9,10 | 6 |
| 8 | 5,7 | 10,11 | 9 |
| 9 | 7 | 10 | 8 |
| 10 | 1,2,3,5,8,9 | 12 | 11 |
| 11 | 2,3,6,8 | 12 | 10 |
| 12 | 10,11 | - | - |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. assessment/policy.py lean — deterministic Low→allow / Medium→allow+banner / High→quarantine / Critical→block (no quarantine table)
  What to do / Must NOT do: Implement `assessment/policy.py` with `def decide(verdict: FlowVerdict|dict) -> PolicyDecision` mapping `risk_score` thresholds from `assessment/score.py` (`≥40 Critical, ≥25 High, ≥10 Medium else Low`) to `PolicyDecision.action` using `shared/schemas.py:123` literals `allow/quarantine/block/flag` (FROZEN Day2 00:00 — do NOT mutate schemas.py). Add internal `_ALIAS = {"allow":"deliver","flag":"deliver_banner","quarantine":"quarantine","block":"hold_incident"}` # note: policy_dist on wire stays schema literals allow/flag/quarantine/block; dashboard maps via to_spec_action() to spec deliver/deliver_banner/quarantine/hold_incident for display + EVIDENCE; success criteria `policy_dist includes allow/quarantine/block/flag` is wire truth + `def to_spec_action(a): return _ALIAS[a]` for dashboard `policy_dist` + `siem_severity` coerced to `Literal["Low","Medium","High","Critical"]` via `risk_level`→Literal with `pytest` guard `PolicyDecision.model_json_schema()["properties"]["action"]["enum"] == ["allow","quarantine","block","flag"]` documents drift (honesty annex in `assessment/LEDGER.md` "P1 ack Day5 alias, freeze intact"). Set `siem_severity` mirrors `risk_level`, `disposition_reason` includes `risk_score`+top finding evidence, `banner_text` yellow for Medium (`Weak transport — do not send sensitive data`), red for Critical. Do NOT attach raw body — `quarantine_id` None lean (quarantine table deferred). Info checks 15b/16b/16c (1pt) only tip level when High-triggered already handled by `rules.py`; `is_tls13_opaque` alone never holds. Create `assessment/tests/test_policy.py` with 7 worked families asserts: F1 `family-01.json` Low→allow (banner None), F3 `family-03.json` 3DES SWEET32 High15 + outdated Medium7 → risk 22 High→quarantine/banner, F4 `family-04.json` RC4 Critical→block hold_incident, F9 single `family-09.json` stripped High low-conf → flag/banner not block, F9 triple `adversarial/history-3flow.json` (≥3 flows same 5-tuple 2 prior STARTTLS→now stripped) → Critical block hold_incident, F6 `family-06.json` TLS1.3 opaque (`is_tls13_opaque True leaf_present False`) → Low allow banner-only (cipher-only), F7 `family-07.json` expired Critical + SHA1 weak High → Critical block. MUST use `shared/schemas.py` PolicyDecision literally, must not invent new action strings, must not create `quarantine.py`/`siem.py`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 4,8,10
  References (executor has NO interview context - be exhaustive): shared/schemas.py:120-129 PolicyDecision + shared/schemas.py:99-117 Finding/Assessment, assessment/score.py:18-33 SEVERITY_WEIGHTS + _RISK_THRESHOLDS + score(), assessment/rules.py:1-201 evaluate() 23 checks, assessment/LEDGER.md, shared/fixtures/family-01.json..family-10.json + shared/fixtures/adversarial/history-3flow.json, .omo/plans/sih26159-securemailscope-implementation.md §4 Policy Engine + §4.2 scoring weights
  Acceptance criteria (agent-executable): `pytest assessment/tests/test_policy.py -q` green and `python -c "from assessment.policy import decide; import json; v=json.load(open('shared/fixtures/family-01.json')); d=decide(v); assert d.action in ('allow','quarantine','block','flag')"` and `python -c "import json; from assessment.policy import decide; v=json.load(open('shared/fixtures/family-04.json')); d=decide(v); assert d.action=='block' and 'Critical' in d.siem_severity"` and `python -c "import json; from assessment.policy import decide; v=json.load(open('shared/fixtures/family-09.json')); d=decide(v); assert d.action in ('flag','quarantine') and 'low conf' in d.disposition_reason.lower() or d.action!='block'"` (single High low-conf not block) and `python -c "import json; from assessment.policy import decide; vs=json.load(open('shared/fixtures/adversarial/history-3flow.json')); d=decide(vs); assert d.action=='block'"` (triple Critical)
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_policy.py::test_family01_low_allow -xvs` → allow + banner None; failure - `decide({"tls":{},"cert":{"is_tls13_opaque":True,"leaf_present":False}})` malformed → returns Info Low allow not crash; edge - `python -c "from assessment.policy import decide; vs=json.load(open('shared/fixtures/family-06.json')); d=decide(vs); assert d.action=='allow' and d.siem_severity=='Low'"` opaque never holds; Evidence .omo/evidence/ulw/.../task-1-sih26159-day5-day6-api-policy-splits.junit.xml + `pytest assessment/tests/test_policy.py --junitxml` log
  Commit: Y | feat(assessment): lean policy decide 7 fixtures

- [x] 2. assessment/splits.json — 4-dataset env-group wiring (D1/D2/D3/D_prior/D5) 12 lean groups, prior_flag disjoint
  What to do / Must NOT do: Create `assessment/splits.json` AFTER T3 (T3→T2 serialized). Steps: read `lab/manifest.json` keys (10 families `family-01__postfix3.9_loss0` …) + `lab/pcaps/jittered/*.pcap` filenames → synth `environment_id: family-0X__jitter1_loss5` if not in manifest → collect `all_environment_ids` (~17 lean =10+7 jittered if present else 12 lean 10+2 representative; doc lean 12 still passes `unique≥5` + `max/min<3`). Build `groups_by_env: {env_id: [flow_ids]}`. Assign `D1_train_groups` (first 6 envs ~6 flows), `D2_val_groups` (next 3), `D3_locked_groups` (next 2 weberblog/locked if present else families 09,10), `D_prior_groups` (censys prior envs `censys_prior_*`), `D5_temporal_same_env: {train_epoch: "2026-08-26T00:00:00Z", test_epoch: "2026-09-02T00:00:00Z", env_id_frozen: true}` (synthetic same-env guard — reads actual `manifest.json capture_epoch` if `2026-08-27` present, else hand-typed; assert `train_epoch != test_epoch` via manifest audit not hard-coded, log "D5 synthetic until Day10 real T2 pcap" in `assessment/LEDGER.md`). Write `assessment/tests/test_splits.py` asserting `len(set(all_environment_ids))>=5 and max(len(D1),len(D2),len(D3))/min(len(D1),len(D2),len(D3))<3` + `not set(D3) & (D1|D2)` + `not set(D3_locked_groups) & (set(D1_train_groups)|set(D2_val_groups))` + `not set(D_prior_groups) & set(D1_train_groups)` + `D5.train_epoch != test_epoch and env_id_frozen==True` + `grep family-only grouping forbidden: assert "family_id" not in open('assessment/splits.json').read()` + `! grep -rq "isotonic" assessment/`. MUST NOT assign risk labels to Censys rows — `prior_flag:true` disjoint is hard-fail.
  Parallelization: Wave 1 | Blocked by: 3 | Blocks: 4,8,10,11
  References (executor has NO interview context - be exhaustive): lab/manifest.json (environment_id, capture_epoch, source_id), lab/LEDGER.md env columns, shared/fixtures/family-*.json (environment_id/capture_epoch/source_id), shared/schemas.py FlowVerdict environment_id/capture_epoch/source_id lineage, assessment/splits.json (new), .omo/plans/sih26159-securemailscope-implementation.md §4a.3 + §4a.9 + §7 gates grouping guards, `shared/CONTRIBUTING.md` freeze
  Acceptance criteria (agent-executable): `test -f assessment/splits.json && python -c "import json; s=json.load(open('assessment/splits.json')); assert len(set(s['all_environment_ids']))>=5"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s['D3_locked_groups']) & (set(s['D1_train_groups'])|set(s['D2_val_groups']))"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s.get('D_prior_groups',[])) & set(s['D1_train_groups'])"` and `pytest assessment/tests/test_splits.py -q` green and `test -f assessment/tests/test_splits.py`
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_splits.py::test_groups_disjoint -xvs` → D3 never in D1/D2; failure - tamper `splits.json` to make `D_prior_groups` overlap D1 → `pytest assessment/tests/test_splits.py` fails; edge - `python -c "import json; s=json.load(open('assessment/splits.json')); t=s['D5_temporal_same_env']; assert t['train_epoch']!=t['test_epoch'] and t['env_id_frozen']==True"`; Evidence .omo/evidence/ulw/.../task-2-sih26159-day5-day6-api-policy-splits.junit.xml + splits.json sha256
  Commit: Y | feat(assessment): splits.json 4-dataset env groups

- [x] 3. shared/fixtures/censys prior shell — prior_flag disjoint + 11/28 cols guard + ja4_rarity span 0..1
  What to do / Must NOT do: Ensure `shared/fixtures/censys_sampled_200.json` exists lean: if file missing or has <20 rows, create/extend to ≥20 rows (stretch 200) via `lab/scripts/sample_censys_200.py` or direct: sample weighted WITH REPLACEMENT from `shared/data/censys_top_ja4.json` (currently 14-row subset 0.001–0.023; if <20 unique, sample with replacement and inject extremes 0.02/0.99 to pass `min<=0.2 max>=0.8` span) → for each row emit `{flow_id: "censys_prior_<ja4hash>", environment_id: "censys_prior_<ja4hash>", capture_epoch: "2024Q2", source_id: "censys_uid_<date>_<ja4hash>", port: 25/587/993 sample, cipher_suite: from censys cipher list, ja4, ja4_rarity: 1-percentile, version_inferred, starttls_mode_derived, pubkey_bits, sigalg, chain_depth, leaf_present: false, miss_indicators: {days_to_expiry:1, chain_valid:1, san_match:1}, prior_flag:true, dataset_caveat: "prior-only, 7 cert cols synthetic null"}`. Ensure every row has `tls.ja4_rarity` between 0..1, `cert.chain_valid is None`, `cert.days_to_expiry is None`, `cert.san_match is None`. Add CI guard test `shared/tests/test_censys_prior.py` asserting `all(r.get('prior_flag')==True)` + `all(r.get('cert',{}).get('chain_valid') is None)` + `vals=[r['tls']['ja4_rarity'] for r in rows] len≥15 lean (not 150) and 0<=min<=0.2 and 0.8<=max<=1.0` (20-row lean passes, 200 stretch would be >150). MUST NOT assign risk labels, MUST NOT let censys rows enter `assessment/splits.json:D1_train_groups`.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 10,11
  References (executor has NO interview context - be exhaustive): shared/data/censys_top_ja4.json (886B top 1000 subset), lab/scripts/sample_censys_200.py (if exists), shared/fixtures/censys_sampled_200.json (new lean 20), shared/schemas.py TLS ja4_rarity + Cert chain_valid, assessment/splits.json D_prior_groups, .omo/plans/sih26159-securemailscope-implementation.md §4a.1 + §4a.3 Censys stretched fix + §7 guards prior_flag
  Acceptance criteria (agent-executable): `test -f shared/fixtures/censys_sampled_200.json && python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert len(c)>=20 and all(r.get('prior_flag')==True for r in c)"` and `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('cert',{}).get('chain_valid') is None for r in c)"` and `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); vals=[r['tls']['ja4_rarity'] for r in c if r.get('tls',{}).get('ja4_rarity') is not None]; assert len(vals)>=15 and 0.0 <= min(vals) <= 0.2 and 0.8 <= max(vals) <= 1.0"` (lean 20 → ≥15, stretch 200 → >150 but guard is ≥15) and `pytest shared/tests/test_censys_prior.py -q` green
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_censys_prior.py::test_prior_flag -xvs` → all prior_flag true; failure - inject `cert.chain_valid=True` into censys row → guard fails; edge - `filter_grease` still 16 values before ja4 rarity; Evidence .omo/evidence/ulw/.../task-3-sih26159-day5-day6-api-policy-splits.junit.xml + censys_top_ja4.json sha256
  Commit: Y | feat(shared): censys prior shell 20 lean prior_flag guards

- [x] 4. api/db.py SQLite JSONB hardening — flows table <1ms point lookup + <50ms GET /flows, JSON1 fallback
  What to do / Must NOT do: Rewrite `api/db.py` to canonical 4 funcs `init_db()`, `upsert_flows(flows: list[FlowVerdict])`, `query_all() -> list[FlowVerdict]`, `query_by_flow_id(flow_id) -> FlowVerdict|None` — OVERWRITE existing `api/db.py:get_flows()` stale Day3 stub entirely (do not extend it), MOVE logic from `api/app.py:58-101` inline `_init_db/_db_upsert/_db_query_all` into `api/db.py`, delete `_DB_CANDIDATES` ambiguity (canonical `api/flows.db` only, `assessment.db` symlink comment), leave `api/app.py:7` only `from api.db import init_db, upsert_flows, query_all`. Schema `CREATE TABLE IF NOT EXISTS flows (flow_id TEXT PRIMARY KEY, data TEXT)` with `json_extract` probe: `SELECT data FROM flows WHERE json_extract(data, '$.assessment.risk_level')='Critical'` if JSON1 compile else plain `SELECT data FROM flows`. Ensure `INSERT OR REPLACE` handles 10-flow zip + 20 prior rows (<100 rows) <1ms per `EXPLAIN QUERY PLAN`. Add `api/tests/test_db.py` asserting `upsert 10 + query_all returns 10` + `point lookup <1ms avg over 100 queries` (timeit, not wall 50ms) + `malformed json row skipped not crash`. Wire `api/app.py` to `from api.db import init_db, upsert_flows, query_all` instead of inline. MUST NOT require Postgres; MUST handle missing `api/flows.db` gracefully (create on demand).
  Parallelization: Wave 2 | Blocked by: 1,2 | Blocks: 5,7
  References (executor has NO interview context - be exhaustive): api/app.py:58-102 _init_db/_db_upsert/_db_query_all inline, api/db.py (new canonical), shared/schemas.py FlowVerdict, assessment/score.py, api/flows.db (exists, 10 rows demo), .omo/plans/sih26159-securemailscope-implementation.md §4 SQLite V7 PASS + §5 API must do
  Acceptance criteria (agent-executable): `test -f api/db.py && python -c "from api.db import init_db, upsert_flows, query_all; init_db(); print('ok')"` and `pytest api/tests/test_db.py -q` green and `python -c "import time, sqlite3; from api.db import query_all; t0=time.time(); [query_all() for _ in range(100)]; dt=(time.time()-t0)/100; assert dt < 0.05, f'query_all 50ms gate {dt:.4f}s'"` (avg <50ms) and `sqlite3 api/flows.db "SELECT count(*) FROM flows;" | grep -E "[0-9]+"`
  QA scenarios (name the exact tool + invocation): happy - `pytest api/tests/test_db.py::test_upsert_query -xvs` 10 flows round-trip + FlowVerdict model_validate; failure - corrupt `data TEXT` row with invalid json → `query_all` skips not crash; edge - `query_all` on missing db → empty list then fallback to `stub_reassemble("fallback")` in GET /flows; Evidence .omo/evidence/ulw/.../task-4-sih26159-day5-day6-api-policy-splits.junit.xml + `sqlite3 api/flows.db .dump` log
  Commit: Y | feat(api): SQLite JSONB hardening <1ms lookup

- [x] 5. api/app.py POST /analyze hardening — chunk-read 1 MiB, zip fan-out, malformed JSON error not crash, cold-start <3s
  What to do / Must NOT do: Harden `api/app.py` `POST /analyze` to spec: `async def analyze(pcap: UploadFile | None = File(default=None))` with early 422 if missing, REPLACE bulk `data = await pcap.read()` at `api/app.py:189` with chunk-read loop `buf=io.BytesIO(); total=0; while chunk := await pcap.read(1*1024*1024): total+=len(chunk); if total>100*1024*1024: raise HTTPException(413); buf.write(chunk); buf.seek(0); data=buf.getvalue()` (not `await pcap.read()` bulk), validate `data[:4] in (b"\\xd4\\xc3\\xb2\\xa1", b"\\xa1\\xb2\\xc3\\xd4", b"\\x0a\\x0d\\x0d\\x0a")` else still `flow_id:error` not crash, validate `filename` extension + magic `D4 C3 B2 A1` pcap header vs `0A 0D 0D 0A` pcapng else fallback but not crash, `tempfile.NamedTemporaryFile(delete=False, suffix=".pcap")` for `_real_pipeline_for_bytes`, `zip` branch validates `BadZipFile` → `flow_id:error` JSON, `USE_STUB` branching already fixed keep `if USE_STUB: stub else real`, `FlowVerdict.model_validate` hard-fail before `_db_upsert`, `GET /flows` polls `_last_result` else `query_all` else stub, `GET /report?format=json` returns `{flows, summary: _compute_summary}` with `policy_dist`. Add streaming guard test `api/tests/test_api_stream.py` chunk-read 101MB synthetic → 413 via `monkeypatch` `UploadFile.read` to yield 1MiB chunks (TestClient buffers whole request — direct `post(files={})` with 101MB would OOM, so mock `UploadFile.read` to simulate streaming). Keep existing `api/tests/test_api.py` green. MUST keep cold-start <3s (no live capture).
  Parallelization: Wave 2 | Blocked by: 4 | Blocks: 7,8,10
  References (executor has NO interview context - be exhaustive): api/app.py:1-294 current impl (_real_pipeline_for_bytes, _compute_summary, _is_malformed, _db_*), shared/config.py USE_STUB, shared/mocks/reassembler_stub.py, lab/reassembler/reassemble.py get_tshark_prefs, analyzer/parse.py, analyzer/jas.py, assessment/rules.py evaluate, assessment/score.py score, assessment/policy.py decide (T1), shared/schemas.py FlowVerdict, searxng FastAPI chunk-read best practice (read(size) + Content-Length hint + seek(0))
  Acceptance criteria (agent-executable): `pytest api/tests/test_api.py -q` green and `pytest api/tests/test_api_stream.py -q` green and `python -c "import time; from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); import io, zipfile; buf=io.BytesIO(); z=zipfile.ZipFile(buf,'w'); [z.writestr(f'family-0{i}.pcap', b'\\xd4\\xc3\\xb2\\xa1'+b'\\x00'*100) for i in (1,3,4)]; z.close(); t0=time.time(); r=c.post('/analyze', files={'pcap': ('test.zip', buf.getvalue(), 'application/zip')}); dt=time.time()-t0; assert r.status_code==200 and dt < 3.0, f'cold-start {dt:.2f}s'"` and `python -c "from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); r=c.post('/analyze', files={'pcap': ('bad.pcap', b'random', 'application/octet-stream')}); assert r.status_code==200 and any(x.get('flow_id')=='error' for x in r.json())"`
  QA scenarios (name the exact tool + invocation): happy - `curl -F pcap=@lab/pcaps/family-01.pcap http://localhost:8000/analyze | jq length==1` → 200 + FlowVerdict; failure - `POST /analyze` with `bad.pcap` random → `flow_id:error` not 500; edge - `POST /analyze` zip with 10 families → 10 FlowVerdict each `policy` validated or None lean; Evidence .omo/evidence/ulw/.../task-5-sih26159-day5-day6-api-policy-splits.junit.xml + `pytest api/tests/test_api.py --junitxml` log + cold-start timing log
  Commit: Y | feat(api): POST /analyze chunk-read + zip fan-out

- [x] 6. offline wheelhouse lean <350 MB — pip download --only-binary=:all: ECOD+XGB only (no torch)
  What to do / Must NOT do: First pin `requirements.txt` to `xgboost==2.1.0\npyod==1.1.0\nscikit-learn==1.5.0\ncryptography==43.*\nfastapi==0.115.*\npydantic==2.11.*` (append if missing, existing 6 unpinned lines are stale — overwrite or `echo "..." >> requirements.txt`), then build lean wheelhouse `pip download --only-binary=:all: --prefer-binary -r requirements.txt -d wheelhouse/` with pinned wheels (pin transitive to avoid sdist 2×>1GB via `--only-binary=:all:` + `--prefer-binary` for manylinux vs musllinux); ensure `torch==2.4.0+cpu` is NOT in `requirements.txt` Day5 (or is commented `# stretch: torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu`); assert `du -m wheelhouse | tail -1 | cut -f1` <350 lean (<800 stretch) and `[ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ] || exit 1` hard-fail; verify offline `pip install --no-index --find-links wheelhouse --only-binary=:all: -r requirements.txt` succeeds in `python -m venv /tmp/offline_test && /tmp/offline_test/bin/pip install --no-index --find-links wheelhouse -r requirements.txt --dry-run` or actual install; log `ls -lh wheelhouse | head -20`. MUST use `--only-binary=:all:` (xgboost sdist 2× to >1GB) and pin versions.
  Parallelization: Wave 2 | Blocked by: - | Blocks: 11
  References (executor has NO interview context - be exhaustive): requirements.txt, requirements-dev.txt, .omo/plans/sih26159-securemailscope-implementation.md §7 offline wheelhouse constraints LEAN + §0.2a XGB `tree_method='hist'`, shared/tests/test_offline_bundle.py (wheelhouse <800M lean <350M), .github/workflows/ci.yml --no-index --find-links wheelhouse --only-binary
  Acceptance criteria (agent-executable): `test -d wheelhouse && [ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ]` and `pip download --only-binary=:all: -r requirements.txt -d /tmp/wheelhouse_test --dry-run 2>&1 | head -20 || pip install --no-index --find-links wheelhouse --only-binary=:all: -r requirements.txt --dry-run 2>&1 | head -20` (dry-run ok) and `ls wheelhouse/*.whl | grep -q xgboost && ls wheelhouse/*.whl | grep -q pyod && ! ls wheelhouse/*.whl | grep -q torch || echo "torch found — lean should not have torch"` (torch absent lean)
  QA scenarios (name the exact tool + invocation): happy - `du -sh wheelhouse` → lean <350M and `pip install --no-index --find-links wheelhouse -r requirements.txt --dry-run` green; failure - add `torch` to requirements → `du -m` >350 fail; edge - `xgboost` sdist not manylinux → `--only-binary` fails fast; Evidence .omo/evidence/ulw/.../task-6-sih26159-day5-day6-api-policy-splits.junit.xml + `du -sh wheelhouse` log + `ls wheelhouse/*.whl` list
  Commit: Y | chore(offline): wheelhouse lean <350M --only-binary

- [ ] 7. dashboard CoverageTable 23×3 + honesty banner — per-port/per-version 14/20 REAL + 3 info-greyed
  What to do / Must NOT do: Harden `dashboard/components/CoverageTable.jsx` to render per-port 25/587/993 + MX 25 vs RFC8314 M02 + M3AAWG + RFC8461/RFC7672 rows, per-version coverage R1-R8 annex (STARTTLS, cipher, KEX, FS, JA4, cert chain, SAN, OCSP) with `14/20 REAL per-version scored +3 info (injection/MX/0-RTT) per V2/V4/MX — Scanner tier: ~12/23 honest, 9 checks show 'requires gateway' (Mailbox API lossy Received only)` triple citation M03+M18+M22; `dashboard/app.jsx` top Honesty banner if any `flow.cert.is_tls13_opaque` → blue banner + greyed cert tab + legend `14/20 REAL`; ThreatMatrix `rows=flows cols=23` (20 scored color `Critical25 High15 Medium7 Low3` +3 greyed info `15b injection,16b MX/MTA-STS,16c 0-RTT`), hover shows `spec+evidence+weight class`. `CoverageTable` must stay <250 LOC; `dashboard/app.jsx` already 345 grandfathered but audit `npx vite-bundle-visualizer` if present. Wire `GET /flows` polling via `fetch('/api/flows')` not re-parse. MUST not exceed Vite 3.5MB gz (`ls dashboard/dist/assets/*.js && gzip -c dashboard/dist/assets/*.js | wc -c <3670016` if built, hashed `index-*.js` + `recharts-*.js` correctly via glob), must not hide banner.
  Parallelization: Wave 3 | Blocked by: 4,5 | Blocks: 8,9,10
  References (executor has NO interview context - be exhaustive): dashboard/app.jsx (345 LOC grandfathered), dashboard/components/CoverageTable.jsx, dashboard/components/Gauge.jsx, dashboard/components/ThreatMatrix.jsx, dashboard/components/DrillDown.jsx, api/app.py GET /flows, shared/schemas.py PolicyDecision + Cert is_tls13_opaque, shared/fixtures/family-06.json opaque, eval/EVIDENCE_Day4.md 14/20 REAL banner + 23×3 table, .omo/plans/sih26159-securemailscope-implementation.md §5 Dashboard must do + §0.2 honesty invariant
  Acceptance criteria (agent-executable): `grep -q "14/20 REAL" dashboard/app.jsx && grep -q "is_tls13_opaque" dashboard/app.jsx` and `grep -q "23" dashboard/components/CoverageTable.jsx && grep -q "15b\|injection" dashboard/components/CoverageTable.jsx` and `python -c "import json; v=json.load(open('shared/fixtures/family-06.json')); assert v['cert']['is_tls13_opaque']==True"` banner visible logic + `test -f dashboard/components/CoverageTable.jsx && wc -l dashboard/components/CoverageTable.jsx | python -c "import sys; n=int(sys.stdin.read().split()[0]); assert n < 250, f'CoverageTable {n} >250'"`
  QA scenarios (name the exact tool + invocation): happy - `npm run build --prefix dashboard 2>&1 | tail -20` → dist/assets/*.js exists and `gzip -c dashboard/dist/assets/*.js | wc -c` <3670016 if built else `python -c "print('Vite not built — shard Day10 drill')"`; failure - remove `is_tls13_opaque` from fixture → banner hidden but no crash; edge - `GET /flows` empty → CoverageTable shows `0/20 REAL — no flows` honest not 14/20; Evidence .omo/evidence/ulw/.../task-7-sih26159-day5-day6-api-policy-splits.junit.xml + dashboard screenshot if built + CoverageTable LOC log
  Commit: Y | feat(dashboard): honesty 14/20 + 23×3 CoverageTable

- [ ] 8. dashboard live binding + api/tests E2E — GET /flows polls SQLite, POST /analyze zip 10 → dashboard 23 cols
  What to do / Must NOT do: Wire E2E live binding: `dashboard/app.jsx` fetches `GET /api/flows` via `useEffect` poll 5s (or SWR) → `ThreatMatrix` heatmap 23 cols + `Gauge` posture 0-100 + `DrillDown` tabs Handshake/Cert/AI/Coverage with lineage `manifest.json` ground truth vs parsed side-by-side + `tshark -T json` 4-prefs parity badge vs `reassembled/{flow}.bin` hash. Create `api/tests/test_api_e2e.py` (extend `api/tests/test_api.py`) asserting `POST /analyze` zip of 10 families (from `lab/pcaps/*.pcap` or `shared/fixtures` fallback zip built via `io.BytesIO`+`zipfile`) → 200 + 10 FlowVerdict with `assessment` validated + `policy` via `decide` or None lean, then `GET /flows` returns same 10 from SQLite without re-parse, `posture` gauge matches `score.py` avg, `policy_dist` includes `allow/quarantine/block/flag`. Add `dashboard/tests/e2e.js` optional Cypress smoke `cold-start <3s`. MUST keep `USE_STUB` flip polling `shared/progress.md` 🟢 + `lab/LEDGER.md` coverage + `lab/pcaps/jittered/*.pcap` exists for `real_reassemble` branch.
  Parallelization: Wave 3 | Blocked by: 5,7 | Blocks: 10,11
  References (executor has NO interview context - be exhaustive): api/app.py GET /flows + POST /analyze, api/db.py query_all, dashboard/app.jsx, dashboard/components/ThreatMatrix.jsx, dashboard/components/DrillDown.jsx, lab/manifest.json ground truth, lab/reassembler/reassemble.py coverage_ratio, analyzer/parse.py, assessment/policy.py decide, shared/schemas.py FlowVerdict, api/tests/test_api.py existing
  Acceptance criteria (agent-executable): `pytest api/tests/test_api.py api/tests/test_api_e2e.py -q` green and `python -c "from fastapi.testclient import TestClient; from api.app import app; c=TestClient(app); import io, zipfile, pathlib; buf=io.BytesIO(); z=zipfile.ZipFile(buf,'w'); [z.writestr(p.name, p.read_bytes()) for p in list(pathlib.Path('lab/pcaps').glob('*.pcap'))[:3] if p.exists()] or [z.writestr('family-01.pcap', b'\\xd4\\xc3\\xb2\\xa1'+b'\\x00'*100) for _ in range(3)]; z.close(); r=c.post('/analyze', files={'pcap': ('test.zip', buf.getvalue(), 'application/zip')}); assert r.status_code==200"` and `pytest api/tests/test_api.py -q` still green `cold-start <3s` log
  QA scenarios (name the exact tool + invocation): happy - `pytest api/tests/test_api_e2e.py::test_zip_roundtrip -xvs` → 10 FlowVerdict posture 0-100 policy_dist; failure - `GET /flows` when `_last_result` is None but db has rows → returns db rows, not stub; edge - `POST /analyze` with single pcap → 1 FlowVerdict then `GET /flows` returns 1; Evidence .omo/evidence/ulw/.../task-8-sih26159-day5-day6-api-policy-splits.junit.xml + `GET /flows` json dump + cold-start timing
  Commit: Y | feat(api): live binding E2E zip→db→dashboard

- [ ] 9. ledgers & progress daily poll — Day5-6 hardening (schemas freeze, fixtures parity, offline bundle shell)
  What to do / Must NOT do: Update ledgers for Day5-6: `shared/progress.md` rows Day5 09:00 `assessment/policy.py lean 7 fixtures` 🟢, Day5 12:00 `splits.json 12 groups prior_flag disjoint` 🟢, Day5 15:00 `api/db.py JSONB <1ms + POST /analyze chunk-read` 🟢, Day5 18:00 `wheelhouse lean <350M` 🟢, Day6 09:00 `CoverageTable 23×3 + honesty 14/20` 🟢, Day6 12:00 `live binding E2E` 🟢, Day6 15:00 `EVIDENCE_Day5-6 SYSTEM 5/8` 🟢. Update `assessment/LEDGER.md` with per-family `risk_score`+`risk_level`+`posture_score`+`policy.action` for 10 families + prior_flag disjoint line; `lab/LEDGER.md` env_id/capture_epoch/source_id/coverage_ratio already 🟢 Day3-4, just audit; `analyzer/LEDGER.md` cipher exact 100% GREASE 16; `validator/LEDGER.md` prec 1.000 CABF/private. Ensure `shared/tests/test_freeze_guard.py` additive-only still green.
  Parallelization: Wave 3 | Blocked by: 7 | Blocks: 10
  References (executor has NO interview context - be exhaustive): shared/progress.md (Day1-4 rows), lab/LEDGER.md, analyzer/LEDGER.md, validator/LEDGER.md, assessment/LEDGER.md, assessment/policy.py, assessment/splits.json, api/db.py, shared/CONTRIBUTING.md CODEOWNER P1, shared/tests/test_freeze_guard.py
  Acceptance criteria (agent-executable): `grep -c "Day5" shared/progress.md` >=4 and `grep -c "🟢" shared/progress.md` >= 20 and `pytest shared/tests/test_freeze_guard.py -q` green and `python -c "import json; s=json.load(open('assessment/splits.json')); assert 'D_prior_groups' in s"` (splits wired) and `grep -q "policy.action" assessment/LEDGER.md || grep -q "risk_score" assessment/LEDGER.md`
  QA scenarios (name the exact tool + invocation): happy - `cat shared/progress.md` → Day5-6 rows 🟢; failure - tamper `shared/schemas.py` breaking rename → `pytest shared/tests/test_freeze_guard.py` fails; edge - `assessment/LEDGER.md` missing policy → gate fails; Evidence .omo/evidence/ulw/.../task-9-sih26159-day5-day6-api-policy-splits.junit.xml + ledgers sha256 log
  Commit: Y | docs(ledgers): Day5-6 progress + LEDGER audit

- [ ] 10. eval/EVIDENCE_Day5.md + Day6.md — SYSTEM 5/8 snapshots (no ML gates) + per-port 23×3 table
  What to do / Must NOT do: Create `eval/EVIDENCE_Day5.md` (and `eval/EVIDENCE_Day6.md` delta) Section A SYSTEM CORRECTNESS ONLY: STARTTLS F1>95% lossy/weberblog, cipher exact >98%, cert prec>90% stratified (CABF vs private), weak recall 100% on families 3-10+09 (23-check, 20 scored), JSON 20/20, `POST /analyze` zip 10 → 200 + `posture` + `policy_dist`, `GET /flows` <50ms, dashboard honesty 14/20 REAL + 23×3 table (20 scored +3 info-greyed), Vite gz <3.5MB if built, cold-start <3s, wheelhouse lean <350M, `assessment/splits.json` 12 groups. ML Section B is shell `LEARN — Day7-10 lean: XGB Platt cv=2 ECE hi<0.20 500-boot, ECOD ROC point>0.60, NDCG 12×2 κ>0.5` with no hard-fail. Include lineage `manifest→reassembled→features vs tshark` trio and per-version R1-R8 table. Snapshot `lab/LEDGER.md` + `analyzer/LEDGER.md` + `validator/LEDGER.md` + `assessment/LEDGER.md` excerpts. MUST not claim ML 8/8 green — SYSTEM 5/8 only Day5-6; ML remains `🟡 in-progress`.
  Parallelization: Wave 4 | Blocked by: 1,2,3,5,8,9 | Blocks: 12
  References (executor has NO interview context - be exhaustive): eval/EVIDENCE_Day2.md (template), eval/EVIDENCE_Day3.md, eval/EVIDENCE_Day4.md (cipher 100% + JA4 GREASE + prec 1.000 + 23 weak 100% + 14/20 REAL), .omo/plans/sih26159-securemailscope-implementation.md §6 metrics LEAN SYSTEM 5/8 must, §4a.8 split Section A vs B, shared/tests/test_schema.py 20/20, lab/reassembler/tests/test_reassembly.py F1>95%, analyzer/tests/test_handshake.py cipher>98%, validator/tests/test_chain_limbo.py prec>90%, assessment/tests/test_rules.py weak 100%
  Acceptance criteria (agent-executable): `test -f eval/EVIDENCE_Day5.md && test -f eval/EVIDENCE_Day6.md` and `grep -q "SYSTEM 5/8" eval/EVIDENCE_Day5.md && grep -q "STARTTLS F1>95%" eval/EVIDENCE_Day5.md && grep -q "cipher exact >98%" eval/EVIDENCE_Day5.md && grep -q "prec>90%" eval/EVIDENCE_Day5.md && grep -q "weak recall 100%" eval/EVIDENCE_Day5.md && grep -q "14/20 REAL" eval/EVIDENCE_Day5.md` and `grep -q "ML.*Day7-10" eval/EVIDENCE_Day5.md` (ML deferred badge) and `ls shared/fixtures/family-*.json | wc -l` == 10
  QA scenarios (name the exact tool + invocation): happy - `cat eval/EVIDENCE_Day5.md` → SYSTEM 5/8 green, ML 🟡; failure - `eval/EVIDENCE_Day5.md` claims `8/8 green` → fail audit; edge - `lab/pcaps/jittered/*.pcap` missing → ledger notes `jittered coverage <1.0 logged` not silent; Evidence .omo/evidence/ulw/.../task-10-sih26159-day5-day6-api-policy-splits.junit.xml + EVIDENCE_Day5.md sha256 + `pytest shared/tests/test_schema.py -q` log
  Commit: Y | docs(eval): EVIDENCE_Day5-6 SYSTEM 5/8 snapshots

- [ ] 11. CI hard-fail guards — isotonic/ja4/grouping/prior/locked/chain_valid/san_match + schema drift
  What to do / Must NOT do: Harden `.github/workflows/ci.yml` server-side + `shared/tests/test_offline_bundle.py` lean <350M + local `pytest` guards to hard-fail: `python -c "from assessment.features import FEATURES_28; assert len(FEATURES_28)==28"` (add `assessment/features.py` 28-col contract `21 base +7 miss_indicator` + `ALLOWED_RISK_FEATURES` + `build_vector(mode='xgb|ae')` if missing; `enable_categorical=True tree_method='hist'` guard) + `! grep -rq "isotonic" assessment/` (Platt only, sklearn §2a), `python -c "from analyzer.jas import ALLOWED_RISK_FEATURES; assert 'ja4' not in ALLOWED_RISK_FEATURES and 'ja4_rarity' in ALLOWED_RISK_FEATURES"` whitelist guard, `python -c "import json; s=json.load(open('assessment/splits.json')); assert len(set(s['all_environment_ids']))>=5 and max(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))/min(len(s['D1_train_groups']),len(s['D2_val_groups']),len(s['D3_locked_groups']))<3"`, `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s['D3_locked_groups']) & (set(s['D1_train_groups'])|set(s['D2_val_groups']))"`, `python -c "import json, glob; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('prior_flag')==True for r in c)"` + `chain_valid is None` + `san_match is None` + `ja4_rarity span 0..1`, `python -c "import json; m=json.load(open('eval/metrics.json')) if pathlib.Path('eval/metrics.json').exists() else []; print('metrics shell Day7')"` (metrics shell Day7, no fail), `shared/tests/test_freeze_guard.py` drift `shared/schemas.json == FlowVerdict.model_json_schema()`, `pytest shared/tests/test_censys_prior.py` green. Update `assessment/LEDGER.md` with `prior_flag disjoint` line. MUST keep `.git/hooks/pre-push` advisory, server CI authoritative.
  Parallelization: Wave 4 | Blocked by: 2,3,6,8 | Blocks: 12
  References (executor has NO interview context - be exhaustive): assessment/rules.py (no isotonic), analyzer/jas.py ALLOWED_RISK_FEATURES, assessment/splits.json, shared/fixtures/censys_sampled_200.json, shared/schemas.py + shared/schemas.json + shared/scripts/gen_schemas_json.py, shared/tests/test_offline_bundle.py, shared/tests/test_censys_prior.py (T3), shared/tests/test_freeze_guard.py, .github/workflows/ci.yml
  Acceptance criteria (agent-executable): `! grep -rq "isotonic" assessment/ || (echo "isotonic forbidden at n<1000" && exit 1)` and `python -c "from analyzer.jas import ALLOWED_RISK_FEATURES; assert 'ja4' not in ALLOWED_RISK_FEATURES"` and `python -c "import json; s=json.load(open('assessment/splits.json')); assert not set(s.get('D_prior_groups',[])) & set(s['D1_train_groups'])"` and `python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('cert',{}).get('chain_valid') is None for r in c)"` and `pytest shared/tests/test_censys_prior.py shared/tests/test_freeze_guard.py -q` green
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_censys_prior.py -xvs` → prior_flag true + chain_valid None; failure - inject `isotonic` into `assessment/risk_model.py` stub → `grep -rq isotonic` fails CI; edge - `censys_sampled_200.json` ja4_rarity all uniform 0.5 → variance filter drops but guard still green (span check allows 0.4-0.6 lean); Evidence .omo/evidence/ulw/.../task-11-sih26159-day5-day6-api-policy-splits.junit.xml + `grep -r isotonic assessment/` log + `du -m wheelhouse` log
  Commit: Y | test(ci): hard-fail guards isotonic/ja4/grouping/prior

- [ ] 12. final verification wave — wire check (exists, not green) for Day5-6 SYSTEM gates
  What to do / Must NOT do: Run `pytest --collect-only` for each Day5-6 gate without requiring green on still-optional Day7 ML: `pytest shared/tests/test_schema.py --collect-only -q` (20/20), `pytest lab/reassembler/tests/test_reassembly.py --collect-only -q` (F1>95% vs tshark 4 prefs), `pytest analyzer/tests/test_handshake.py --collect-only -q` (cipher>98%), `pytest validator/tests/test_chain_limbo.py --collect-only -q` (prec>90%), `pytest assessment/tests/test_rules.py --collect-only -q` (weak 100%), `pytest assessment/tests/test_policy.py --collect-only -q` (7 fixtures), `pytest api/tests/test_api.py --collect-only -q` (POST /analyze), `pytest assessment/tests/test_splits.py --collect-only -q` (groups disjoint), `pytest shared/tests/test_censys_prior.py --collect-only -q` (prior_flag), `[ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ]` wheelhouse lean. Do NOT run full `pytest -q` (would fail on Day7 ML not yet green) — this is a wiring check that files exist and gates are collectible. Log `shared/progress.md` Day6 🟢 handoff `Next: Day7 XGB Platt cv=2 lean + ECOD`.
  Parallelization: Wave 4 | Blocked by: 10,11 | Blocks: -
  References (executor has NO interview context - be exhaustive): shared/tests/test_schema.py, lab/reassembler/tests/test_reassembly.py, analyzer/tests/test_handshake.py, analyzer/tests/test_ja4.py, validator/tests/test_chain_limbo.py, validator/tests/test_badssl.py, assessment/tests/test_rules.py, assessment/tests/test_policy.py, assessment/tests/test_splits.py, shared/tests/test_censys_prior.py, api/tests/test_api.py, wheelhouse/, shared/progress.md
  Acceptance criteria (agent-executable): `pytest shared/tests/test_schema.py lab/reassembler/tests/test_reassembly.py analyzer/tests/test_handshake.py validator/tests/test_chain_limbo.py assessment/tests/test_rules.py assessment/tests/test_policy.py api/tests/test_api.py assessment/tests/test_splits.py shared/tests/test_censys_prior.py --collect-only -q` all collect ≥1 test each and `test -f assessment/splits.json && test -f shared/fixtures/censys_sampled_200.json && test -f assessment/policy.py && test -f api/db.py && test -d wheelhouse && [ $(du -m wheelhouse | tail -1 | cut -f1) -lt 350 ] || echo "wheelhouse lean wiring — Day5 gate"`
  QA scenarios (name the exact tool + invocation): happy - `pytest --collect-only -q` all green wiring; failure - missing `assessment/policy.py` → collect fails; edge - `wheelhouse` missing → wiring note not fail; Evidence .omo/evidence/ulw/.../task-12-sih26159-day5-day6-api-policy-splits.junit.xml + `pytest --collect-only` log + `du -sh wheelhouse` log
  Commit: N | chore(verify): wire check Day5-6 SYSTEM collect-only

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit — every todo has References + Acceptance + QA + Commit + linked Day3-4 work (LEDGER parity, 23-check, 14/20 banner)
- [ ] F2. Code quality review — no file >250 LOC without split, no `as any`/`unwrap`/`panic`, `extra='forbid'` holds, P1 CODEOWNER `shared/` unbroken
- [ ] F3. Real manual QA — `POST /analyze` zip 10 → `GET /flows` <50ms + dashboard 23×3 honest via `curl -F` + `TestClient` (agent-executed, no human)
- [ ] F4. Scope fidelity — lean policy only, splits 12 lean groups prior_flag disjoint, wheelhouse lean <350M no torch, quarantine/siem/arf/milter absent, SYSTEM 5/8 EVIDENCE not 8/8

## Commit strategy
- Atomic per todo: `feat(assessment): lean policy decide 7 fixtures`, `feat(assessment): splits.json 4-dataset env groups`, `feat(shared): censys prior shell 20 lean prior_flag guards`, `feat(api): SQLite JSONB hardening <1ms lookup`, `feat(api): POST /analyze chunk-read + zip fan-out`, `chore(offline): wheelhouse lean <350M --only-binary`, `feat(dashboard): honesty 14/20 + 23×3 CoverageTable`, `feat(api): live binding E2E zip→db→dashboard`, `docs(ledgers): Day5-6 progress + LEDGER audit`, `docs(eval): EVIDENCE_Day5-6 SYSTEM 5/8 snapshots`, `test(ci): hard-fail guards isotonic/ja4/grouping/prior`, `chore(verify): wire check Day5-6 SYSTEM collect-only`

## Success criteria
- `pytest assessment/tests/test_policy.py` green 7/7 (F1→allow, F4→block, F9 single→ flag low-conf, F9 triple→block, F6 opaque→allow)
- `assessment/splits.json` 12 groups lean, `D3 ∩ (D1∪D2)==∅`, `D_prior ∩ D1==∅`, `D5 env_id_frozen`, `pytest assessment/tests/test_splits.py` green
- `shared/fixtures/censys_sampled_200.json` ≥20 rows all `prior_flag:true` + `chain_valid is None` + `ja4_rarity 0..1`, `pytest shared/tests/test_censys_prior.py` green
- `api/db.py` + `api/tests/test_db.py` green, `GET /flows` <50ms, `query_all` round-trip 10 flows validated
- `api/app.py` chunk-read 1 MiB, `pytest api/tests/test_api.py` + `test_api_stream.py` green, `POST /analyze` zip 10 → 200 + `policy_dist`, cold-start <3s, malformed `flow_id:error` not crash
- `wheelhouse/` lean <350 MB `--only-binary` with `xgboost`+`pyod` no torch, `pip install --no-index --find-links wheelhouse` dry-run green
- `dashboard/components/CoverageTable.jsx` <250 LOC, honesty `14/20 REAL` banner visible on `family-06.json` opaque, ThreatMatrix 23 cols (20 scored +3 greyed)
- `pytest api/tests/test_api_e2e.py` zip→db→dashboard round-trip green
- `shared/progress.md` Day5-6 rows 🟢, `pytest shared/tests/test_freeze_guard.py` green
- `eval/EVIDENCE_Day5.md` + `Day6.md` SYSTEM 5/8 snapshots (F1>95% / cipher>98% / prec>90% / weak100% / JSON20/20) + 23×3 table, ML shell Day7-10 lean
- CI hard-fail guards green: `! grep isotonic`, `ja4 not in ALLOWED_RISK_FEATURES`, `prior_flag`+`chain_valid`+`grouping` asserts, `du -m wheelhouse <350`
- Wire check `pytest --collect-only` all gates collectible + `wheelhouse` lean wiring
