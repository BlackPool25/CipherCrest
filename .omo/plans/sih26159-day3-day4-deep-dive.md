# sih26159-day3-day4-deep-dive - Work Plan

## TL;DR (For humans)

**What you'll get:** By end of Day 4 the pipeline is no longer mocks — lossy pcaps prove `reassemble_out_of_order`, TLS parser emits version/cipher/KEX/FS/JA4 with GREASE-clean parity, X.509 validator does full Store/PolicyBuilder chain + SAN + stapled OCSP with honest 1.3 opaque, and the 23-check rule engine (20 scored +3 info-weighted) outputs risk_score plus a blue honesty banner showing 14/20 REAL. All gated by real pcaps, not fixtures.

**Why this approach:** We keep `tshark -T json` as the byte-identical oracle (both reassembly prefs TRUE) but ship scapy as the importable path — judges see parity, offline bundle stays small. Validator uses cryptography's Rust-backed Store/PolicyBuilder (2024 API, not deprecated verify_directly) with dual-store (OS trust + privateCA) and parses stapled OCSP only — no live fetch, air-gap intact. Rule keeps 15b/16b/16c as 1-pt Info unless High-triggered, so the dashboard's 23-col matrix is honest and the posture gauge isn't inflated.

**What it will NOT do:** No ML training (XGB/ECOD/Platt/calibration) — that's Day 7-10. No quarantine/siem/ARF/milter live — cut-order 0 stretch. No schema-breaking rename of `cipher_strength`/`starttls_mode` literals. No per-split B1-B5 annex tables.

**Effort:** Large (14 todos, 3 waves, ~16h wall, 6-agent parallel)
**Risk:** Medium - `tshark` parity on lossy/weberblog is the flakiest gate; mitigated by jittered.pcap pre-logged 0.897 + both prefs TRUE documented fix
**Decisions to sanity-check:** Parser = tshark-oracle+scapy fallback (not pure scapy), Validator = strict+stapled OCSP parse (not just chain_valid), Rule = info-weighted 1pt (not equal-weight), Parity = strict F1>95% on lossy+weberblog, JA4 = shared rarity + conditional stub flip

Your next move: approve plan → `/start-work sih26159-day3-day4-deep-dive`. Full execution detail follows below.

---
> TL;DR (machine): Large / Medium — Day3 lossy parity + TLS JA4 + Day4 X.509 SAN/stapled OCSP + 23-check rule 14/20 banner

## Scope
### Must have
- Lab: 7 new jittered/lossy pcaps (families 02,03,04,05,07,08,10) + `lab/scripts/jitter_slices.py` (cipher shuffle + GREASE 0x0a0a + sigalg variation + expiry ±5d), weberblog 20 flows replay, `reassembly_coverage_ratio` + `pre_tls_buffer_len`/`injection_possible` on every flow, stripping-history 3flow already present validated, tshark parity harness with BOTH prefs TRUE, env_id/capture_epoch/source_id on every FlowVerdict
- Analyzer: `analyzer/parse.py` complete — legacy_version 0x0303 + supported_versions 0x0304 → TLS1.3 else 1.2, is_deprecated per RFC8996, cipher/KEX/fs_flag/is_aead/cipher_strength (weak if RC4/DES/EXPORT/NULL/anon/3DES/CBC+SHA1), extensions SNI/ALPN/supported_groups/sigalg/key_share + early_data 0x002a + psk 0x0029/0x002d + ticket_age + ECH outer (RFC9849), `cert.is_tls13_opaque` set after ServerHello via validator stub (TLS has no such field; Cert does per `shared/schemas.py:60-63`), no cert DER parse here
- Analyzer JA4: `analyzer/jas.py` — FoxIO ja4 python canonical hash, GREASE filter via `shared/ja4_rarity.filter_grease` (0x0a0a..0xfafa per RFC8701), off-line `ja4_rarity = 1-percentile` via `shared/data/censys_top_ja4.json`, never ja4db.com, `ja4` never in feature_vector (whitelist assert), `ja4_rarity` variance injection via `jitter_slices.py` sampling Censys distribution
- Validator: `validator/chain.py` + `validator/san_check.py` — `cryptography.x509.load_der_x509_certificate` per DER blob, chain leaf→intermediates→Store(OS `/etc/ssl/certs/ca-certificates.crt` + `validator/stores/privateCA.pem`), PolicyBuilder time+max_depth, per-link pubkey.verify, BasicConstraints CA:FALSE pathLen, KeyUsage/ExtendedKeyUsage serverAuth, weak checks (RSA<2048→High <1024 Critical, EC<P-256 High, sigalg sha1/md5→High, expired Critical, notYetValid High, self_signed High/Medium if privateCA, chain incomplete High/Medium), SAN RFC7817 dNSName vs `mail.lab.local`/SNI, CN fallback Medium, SRV-ID if RFC6186, stapled OCSP parse (ClientHello status_request 0x0005 / status_request_v2 RFC6961 → CertificateStatus type 22 via `cryptography.x509.ocsp.load_der_ocsp_response` → good/revoked/unknown/not-stapled/opaque), Must-Staple via TLSFeature, honesty invariant hard-fail
- Assessment: `assessment/score.py` + `assessment/rules.py` — exactly 23 checks spec-cited (1-14,15a,b,16,16b,c,17-20), posture `Critical25 High15 Medium7 Low3 Info1` sum cap100 thresholds ≥40 Critical ≥25 High ≥10 Medium else Low `posture_score=100-risk_score`, Info-weighted 1pt for 15b/16b/16c unless High/Medium-triggered, 100% weak recall on families 3-10+09, remediation per check
- Dashboard/API glue: honesty banner (Family06 `is_tls13_opaque` → blue banner + greyed cert tab + legend "14/20 REAL per-version scored +3 info (injection/MX/0-RTT) per V2/V4/MX" triple citation M03+M18+M22), per-version coverage table R1-R8, CoverageTable 23 cols (20 scored color +3 greyed info), ThreatMatrix lineage manifest→reassembled→features vs tshark, `USE_STUB=False` conditional flip only when `shared/progress.md` 🟢 + `reassembled/*.bin` + coverage logged
- Ledgers & fixtures: 7 new `shared/fixtures/family-*.json` via `model_validate_json`, `shared/fixtures/adversarial/history-3flow.json` stays, `lab/LEDGER.md` + `analyzer/LEDGER.md` + `validator/LEDGER.md` + `assessment/LEDGER.md` per-family rows, `eval/EVIDENCE_Day3.md` + `Day4.md` snapshots, CI gates hard-fail (server-side `.github/workflows/ci.yml` Require status checks)

### Must NOT have (guardrails, anti-slop, scope boundaries)
- No XGB/Platt/ECOD/IF/MicroAE training or calibration — ML is Day7-10 lean; any `risk_model.py`/`anomaly_model.py` touched now is stub only
- No `quarantine.py`/`siem.py`/`arf.py`/`QuarantineConsole`/`digest cron`/`milter 127.0.0.1:10025`/`mockdns` live beyond fixture read (`shared/data/mta-sts-fixture.json`/`dane-tlsa-fixture.json` only) — cut-order 0 stretch
- No live OCSP/CRL fetch at runtime, no PQC/DANE claim, no mail body decrypt, no mock 1.3 cert synthesis, no JA4 raw hash in feature_vector
- No breaking rename of `TLS.cipher_strength` (weak/medium/strong vs weak/acceptable/strong) or `FlowVerdict.starttls_mode` (upgrade/implicit/none/stripped vs implicit/starttls-upgrade/cleartext/failed-upgrade) — freeze additive-only per `shared/CONTRIBUTING.md` CODEOWNER P1; alias map in rules only
- No B1-B5 per-split annex, no overall pooled ML validity table — that's Day10 stretch; Day4 reports ONE deterministic gate table only
- No transformer BERT on raw pcap, no isotonic at n<100, no `as any`/`unwrap`/`panic`, no file >250 LOC without split

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: TDD for parser/validator/rules (write failing test first from ledgers/fixtures, then implement to green), tests-after for lab jitter harness (augment existing `test_reassembly.py`/`test_coverage_ratio.py`). Framework: `pytest -q --junitxml=<attemptDir>/task-N-*.junit.xml` + `python -m py_compile` + `pytest --no-index --find-links wheelhouse` offline check, plus `tshark -T json` parity subprocess gate
- Evidence: `.omo/evidence/ulw/<session>/<goalId>/a<attempt>/task-<N>-sih26159-day3-day4-deep-dive.*` (outside ulw-loop use `.omo/evidence/task-<N>-sih26159-day3-day4-deep-dive.*`). Each todo produces junit xml + log + optional png/json fixture. No human CLI witness required.

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.

- Wave 1a (Day3 AM early, 3h): T1 Lab jitter+lossy (creates `lab/pcaps/jittered/*.pcap` + manifest env_id/epoch) + T3 JA4 glue — parallel, no analyzer dep (T3 independent of T1, jitter sampling uses censys_top_ja4.json)
- Wave 1b (Day3 AM late, 3h): T2 Reassembler parity hardening — BLOCKED by T1 (needs jittered/*.pcap dir), runs after T1 🟢; verifies 4 tshark prefs + triple 3flow + pre_tls
- Wave 2 (Day3 PM, ~6h): T4 Analyzer parse.py, T5 Analyzer jas.py, T6 Analyzer integration & 7 fixtures + tests — depends on Wave 1b ledger 🟢 for reassembled/*.bin + jittered corpus
- Wave 3 (Day4 AM, ~6h): T7 Validator chain.py (creates `validator/stores/`), T8 Validator san_check+OCSP+weak, T9 Validator limbo/badssl tests — depends on Wave2 TLS fixtures (cert DER available)
- Wave 4 (Day4 PM, ~5h): T10 score.py, T11 rules.py 23 checks, T12 Dashboard honesty banner & coverage 23×3, T13 Schemas freeze guard & offline bundle (now after T6, not parallel with T1), T14 EVIDENCE Day3/4 snapshots & CI hard-fail — T13 serialized after T6 (needs 10 fixtures), T14 after T12+13

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | - | 2,13 | 3 |
| 2 | 1 | 4,6,14 | 3* (3 can start with 1, but 2 needs jittered dir) |
| 3 | - | 5,6 | 1 |
| 4 | 2 | 6,7 | 5 |
| 5 | 3 | 6 | 4 |
| 6 | 2,4,5 | 7,9,12,13 | - |
| 7 | 6 | 8,9,10 | - |
| 8 | 7 | 9,10,11 | - |
| 9 | 7,8 | 11,14 | 10 |
| 10 | 7,8 | 11 | 9 |
| 11 | 8,9,10 | 12,14 | - |
| 12 | 6,11 | 14 | 13 (13 now after 6) |
| 13 | 6 | 14 | 12 |
| 14 | 2,6,9,11,12,13 | - | - |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. lab/scripts/jitter_slices.py + 7 jittered pcaps + manifest env_id/capture_epoch wiring to unblock parity 28-col
  What to do / Must NOT do: Create `lab/scripts/jitter_slices.py` that for each of families 02,03,04,05,07,08,10 does 1 lean jitter slice (stretch 5): cipher-order shuffle, inject one GREASE 0x0a0a into ClientHello cipher list, rotate sigalg (sha256→sha384), expiry jitter ±5d via `openssl x509 -days` clone, sample `ja4_rarity` from `shared/data/censys_top_ja4.json` weighted distribution to inject variance (if lab ja4 uniform → median 0.5 otherwise span 0..1). Output `lab/pcaps/jittered/family-0X-jitter-01.pcap` via `scapy` (reuse `lab/scripts/gen_pcap.py:tls_client_hello_tls12/tls_client_hello_tls13` helpers) and `reassembled/family-0X-jitter-01.bin` placeholder. Extend `lab/manifest.json` entries with `environment_id: "<family>__postfix3.9_loss0"` + `capture_epoch: "2026-08-27T00:00:00Z"` + `client: "sender"` + `docker_image_sha256` + `tshark_version` + `source_id` (uuid), and `lab/LEDGER.md` columns per §0.2 Unix ledger. MUST NOT reuse family-01 cipher, must not claim Censys rows as risk train (prior_flag disjoint), must not break `shared/schemas.py` freeze.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 2,6,13
  References (executor has NO interview context - be exhaustive): lab/reassembler/reassemble.py:1-345 (seq buffering, _compute_pre_tls_buffer, reassemble), lab/scripts/gen_pcap.py:1-253 (tls_client_hello_tls12/tls_client_hello_tls13 helpers, Ether/IP/TCP/Raw), lab/manifest.json, lab/LEDGER.md, shared/ja4_rarity.py:1-81 (GREASE_VALUES 16, filter_grease, get_ja4_rarity), shared/data/censys_top_ja4.json (886B, 0.001..0.023), shared/schemas.py:1-140 (FlowVerdict, Cert.is_tls13_opaque, TLS), shared/CONTRIBUTING.md (CODEOWNER P1 additive-only), lab/scripts/jitter_slices.py (new, to be created) — NOTE: lab/pcaps/jittered.pcap (single 1.1K legacy) vs new `lab/pcaps/jittered/*.pcap` 7-file dir (disambiguate), 250 LOC ceiling: reassemble.py already 345 → plan split: keep reassemble.py as is (grandfathered breach, F2 will flag) but new jitter_slices.py must stay <250
  Acceptance criteria (agent-executable): `python lab/scripts/jitter_slices.py --slices 1 --families 02,03,04,05,07,08,10 && test -d lab/pcaps/jittered && ls lab/pcaps/jittered/*.pcap 2>/dev/null | wc -l` == 7 && `test -f lab/pcaps/jittered.pcap` (legacy 0.897 log preserved) and `python -c "import json; m=json.load(open('lab/manifest.json')); assert all('environment_id' in v and 'capture_epoch' in v for v in m.values()), 'manifest missing env/epoch'"` and `python -c "import json; c=json.load(open('shared/data/censys_top_ja4.json')); vals=list(c['ja4'].values()); rarity=[1-v for v in vals]; assert 0.001 <= min(vals) <=0.01 and 0.02 <= max(vals) <=0.03 and 0.97 <= max(rarity) <=1.0 and 0.0 <= min(rarity) <=0.2, f'censys freq rarity span wrong {min(vals):.3f}..{max(vals):.3f}'"` and `pytest shared/tests/test_ja4_grease.py shared/tests/test_ja4_rarity.py -q` passes
  QA scenarios (name the exact tool + invocation): happy - `pytest lab/reassembler/tests/test_coverage_ratio.py::test_jitter_logs_not_silent -xvs` shows jittered coverage <1.0 not silent and LEDGER contains coverage_ratio; failure - `python lab/scripts/jitter_slices.py --slices 1` with missing censys file → fallback median 0.5 and warning log, but no crash; edge - `python -c "from shared.ja4_rarity import filter_grease; assert filter_grease([0x0A0A,0x1301])==[0x1301]"`; Evidence .omo/evidence/ulw/.../task-1-sih26159-day3-day4-deep-dive.junit.xml + jittered pcaps sha256 list
  Commit: Y | feat(lab): jitter slices 7 + manifest env_id/epoch wiring

- [x] 2. lab/reassembler parity hardening — strict F1>95% on lossy/weberblog + pre_tls_buffer + stripping 3flow gate
  What to do / Must NOT do: Harden `lab/reassembler/reassemble.py` to guarantee `tcp.desegment_tcp_streams:TRUE` + `tcp.reassemble_out_of_order:TRUE` + `tls.desegment_ssl_records:TRUE` + `tls.desegment_ssl_application_data:TRUE` in parity harness, and assert `reassembly_coverage_ratio` + `pre_tls_buffer_len`/`pre_tls_buffer_injection_possible` on every flow (already computed via `_compute_pre_tls_buffer` 220→0x16 0x03). Extend `lab/reassembler/tests/test_reassembly.py` + `test_coverage_ratio.py` to run on 3 corpora: 10 clean pcaps (coverage 1.0), 7 jittered pcaps (coverage <1.0 0.897 duplicate logged, gap/overlap flags true), and weberblog 20 flows (if present else synthetic weberblog-01 fixture). Enforce stripping-history 3flow `lab/adversarial/stripping-history-3flow/flow{1,2,3}.pcap` same 5-tuple 127.0.0.11:54330→127.0.0.1:587, flows 1-2 STARTTLS+220 Ready+ClientHello, flow3 stripped no 0x16 0x03. MUST NOT hide jittered coverage <1.0 (must log in LEDGER per test_jitter_logs_not_silent), must not run parity without BOTH tshark prefs (false-fail per ask.wireshark #10299/#23327), must not claim single-flow stripped is Critical (honest High low-conf).
  Parallelization: Wave 1 | Blocked by: 1 | Blocks: 4,6,14
  References (executor has NO interview context - be exhaustive): lab/reassembler/reassemble.py:30-70 (_compute_pre_tls_buffer 220→\x16\x03), 91-152 (_reassemble_flow overlap first-seen, gap flag), 155-345 (reassemble per_flow aggregation, coverage_ratio, banner 220/* OK/+OK, STARTTLS Bennett), lab/reassembler/tests/test_reassembly.py:38-52 (tshark 4 prefs: tcp.desegment_tcp_streams + reassemble_out_of_order + tls.desegment_ssl_records + tls.desegment_ssl_application_data), lab/reassembler/tests/test_coverage_ratio.py:1-85 (coverage 1.0 clean + jitter <1.0 + pre_tls), lab/reassembler/tests/test_history_triple.py:1-85 (triple 3flow same 5-tuple 127.0.0.11:54330→127.0.0.1:587, single High vs triple Critical), lab/LEDGER.md (coverage_ratio column), lab/manifest.json, searxng ask.wireshark #10299/#23327 (both prefs OFF by default since 3.0)
  Acceptance criteria (agent-executable): `pytest lab/reassembler/tests/test_reassembly.py lab/reassembler/tests/test_coverage_ratio.py lab/reassembler/tests/test_history_triple.py -q` all green and `test -f lab/pcaps/jittered/family-02-jitter-01.pcap && python lab/reassembler/reassemble.py lab/pcaps/jittered/family-02-jitter-01.pcap --json | python -c "import json,sys; d=json.load(sys.stdin); assert 0.8 < d['coverage_ratio'] <1.0 and 'pre_tls_buffer_len' in d" || echo "jittered not yet created — T1 must run first"` and `pytest lab/reassembler/tests/test_reassembly.py::test_reassembly_f1 -xvs` with tshark 4 prefs `tcp.desegment_tcp_streams:TRUE -o tcp.reassemble_out_of_order:TRUE -o tls.desegment_ssl_records:TRUE -o tls.desegment_ssl_application_data:TRUE` shows F1>95% (if tshark missing → fallback coverage>95% skip, but log warning)
  QA scenarios (name the exact tool + invocation): happy - `python lab/reassembler/reassemble.py lab/pcaps/family-01.pcap --json` → coverage 1.0 banner 220 starttls true pre_tls 138; weberblog fallback: if `ls lab/pcaps/real/*.pcap 2>/dev/null | head -1` exists → `python lab/reassembler/reassemble.py lab/pcaps/real/weberblog-*.pcap --json | jq .coverage_ratio>0.95` else synthetic `shared/fixtures/weberblog-01.json` sanity (no F1 fail); failure - `lab/pcaps/jittered/*.pcap` with out-of-order disabled `--no-reassemble-out-of-order` → gap_detected true coverage 0.897; edge - `lab/adversarial/stripping-history-3flow/flow3.pcap` → no STARTTLS, no 0x16 0x03, pre_tls 0; Evidence .omo/evidence/ulw/.../task-2-sih26159-day3-day4-deep-dive.junit.xml + `tshark -r lab/pcaps/family-01.pcap -T json -o tcp.desegment_tcp_streams:TRUE -o tcp.reassemble_out_of_order:TRUE -o tls.desegment_ssl_records:TRUE -o tls.desegment_ssl_application_data:TRUE` diff log (4 prefs, not 3)
  Commit: Y | fix(lab): strict parity lossy/weberblog + pre_tls injection flag

- [x] 3. shared JA4 glue — GREASE harmonization + offline rarity variance injection (no live ja4db.com)
  What to do / Must NOT do: Verify `shared/ja4_rarity.py` GREASE_VALUES 16 values + `filter_grease` strips 0x0a0a..0xfafa before any hash (FoxIO technical_details §4, issue #305), `get_ja4_rarity` returns 1-percentile 0..1 or None for unknown, no network. Create `shared/scripts/sample_censys_200.py` stub that samples 200 weighted from `shared/data/censys_top_ja4.json` for JA4 rarity variance table (if lab JA4 uniform → impute median 0.5 else span 0..1) — does NOT create risk rows. Ensure `shared/tests/test_ja4_grease.py` + `test_ja4_rarity.py` green (all 16 GREASE filtered, known t13d1516h2_8daaf6152771 returns rarity 1-freq, unknown returns None). Add whitelist guard `ALLOWED_RISK_FEATURES` assert `ja4 not in feature_vector` (raw hash never ML feature, only `ja4_rarity`). MUST NOT call ja4db.com live, MUST NOT feed raw `ja4` to any risk_model, must not invent GREASE values beyond RFC8701.
  Parallelization: Wave 1 | Blocked by: - | Blocks: 5,6
  References (executor has NO interview context - be exhaustive): shared/ja4_rarity.py:1-81, shared/data/censys_top_ja4.json (886B subset, t13d1516h2_8daaf6152771_e5627efa2ab1 etc), shared/tests/test_ja4_grease.py, shared/tests/test_ja4_rarity.py, analyzer/jas.py (future), shared/schemas.py:TLS ja4/ja4_rarity fields, searxng JA4 FoxIO technical_details/issue #305 (GREASE anywhere)
  Acceptance criteria (agent-executable): `pytest shared/tests/test_ja4_grease.py shared/tests/test_ja4_rarity.py -q` green and `python -c "from shared.ja4_rarity import filter_grease, GREASE_VALUES; assert len(GREASE_VALUES)==16 and filter_grease([0x0A0A,0x1301])==[0x1301]"` and `python -c "from shared.ja4_rarity import get_ja4_rarity; assert 0 <= get_ja4_rarity('t13d1516h2_8daaf6152771_e5627efa2ab1') <=1 and get_ja4_rarity('deadbeef') is None"` and `grep -rq "ja4.*in.*feature" assessment/ && exit 1 || echo "whitelist ok"` (no raw ja4 in vector)
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_ja4_rarity.py::test_known_returns_rarity -xvs` shows 1-percentile correct; failure - `get_ja4_rarity('unknown')` returns None not 0; edge - `filter_grease(list(GREASE_VALUES))==[]`; Evidence .omo/evidence/ulw/.../task-3-sih26159-day3-day4-deep-dive.junit.xml + censys_top_ja4.json sha256
  Commit: Y | feat(shared): GREASE harmonization + offline rarity guard

- [x] 4. analyzer/parse.py — deterministic TLS handshake parser with tshark oracle + scapy fallback (no cert parse)
  What to do / Must NOT do: Implement `analyzer/parse.py` with dual path: primary `tshark -T json` parse (preferred oracle, 4 prefs: `tcp.desegment_tcp_streams:TRUE -o tcp.reassemble_out_of_order:TRUE -o tls.desegment_ssl_records:TRUE -o tls.desegment_ssl_application_data:TRUE`) and fallback `scapy.layers.tls` (`TLSClientHello`, `TLS13ClientHello`, `TLSServerHello`, `TLS_Ext_SupportedVersion_CH 0x2b`, `TLS_Ext_*`). Extract `legacy_version` 0x0303, `supported_versions` 0x0303 vs 0x0304 → `version` TLS1.3 else TLS1.2, `cipher_suites[]`, `extensions` SNI server_name, ALPN imap/smtp, supported_groups x25519/secp256r1, signature_algorithms, key_share (1.3), early_data 0x002a + psk_key_exchange_modes 0x002d + pre_shared_key 0x0029 + ticket_age/obfuscated_ticket_age + PSK binder, ECH ClientHelloOuter note `encrypted_client_hello` Inner not parsed (RFC9849 out-of-scope). Cipher/KEX: KEX from cipher string ECDHE/DHE/RSA + key_share, `fs_flag = kex in (ECDHE,DHE)` (TLS1.3 always true per RFC8446 no RSA), `is_aead` vs Mozilla Intermediate list `ECDHE-*-AES128/256-GCM-SHA256/384/CHACHA20-POLY1305`, `cipher_strength` weak if RC4/DES/EXPORT/NULL/anon/3DES/CBC+SHA1. MUST emit `TLS` sub-object per `shared/schemas.py:8-15,131-137` (version/is_deprecated/cipher_suite/cipher_strength/is_aead/kex/fs_flag/ja4/ja4_rarity/ja4s/early_data_offered/accepted/psk_offered/ticket_age/ech_outer_present/handshake_success/alert_after_starttls) and return `cert.is_tls13_opaque` bool (Cert model `shared/schemas.py:60-63` has `is_tls13_opaque`, TLS does NOT — `extra='forbid'` — so emit via `Cert(leaf_present=False, is_tls13_opaque=True, ocsp_stapled_status='opaque')` stub or pipeline wrapper, NOT as TLS field). Do NOT parse Certificate DER here.
  Parallelization: Wave 2 | Blocked by: 2 | Blocks: 6,7
  References (executor has NO interview context - be exhaustive): analyzer/LEDGER.md, shared/schemas.py:TLS, lab/reassembler/reassemble.py (reassembled/*.bin input), lab/manifest.json (cipher/kex ground truth), lab/pcaps/family-*.pcap, searxng scapy handshake/extensions (TLS13ClientHello, supported_versions 0x2b, early_data 0x002a), cryptography not used here, shared/ja4_rarity.py (GREASE)
  Acceptance criteria (agent-executable): `python -m analyzer.parse lab/pcaps/family-01.pcap --json | python -c "import json,sys; d=json.load(sys.stdin); tls=d['tls'] if 'tls' in d else d; assert tls['cipher_suite']=='ECDHE-RSA-AES128-GCM-SHA256' and tls['kex']=='ECDHE' and tls['fs_flag']==True and tls['is_aead']==True"` and `python -m analyzer.parse lab/pcaps/family-06.pcap --json | python -c "import json,sys; d=json.load(sys.stdin); cert=d.get('cert') or {}; assert cert.get('is_tls13_opaque')==True and (d.get('tls') or d).get('version')=='TLS1.3', f\"06 must be TLS1.3 opaque via cert.is_tls13_opaque, got {d}\""` and `tshark -r lab/pcaps/family-01.pcap -T json -o tcp.desegment_tcp_streams:TRUE -o tcp.reassemble_out_of_order:TRUE -o tls.desegment_ssl_records:TRUE -o tls.desegment_ssl_application_data:TRUE 2>&1 | python -c "import sys,json,pathlib; out=pathlib.Path('/tmp/tshark.json'); out.write_text(sys.stdin.read()); data=json.load(open('/tmp/tshark.json')) if out.stat().st_size>2 else []; print('tshark harness 4 prefs OK')"` (no `or True` tautology) + `test -f analyzer/tests/test_handshake.py && pytest analyzer/tests/test_handshake.py -q` (new file) cipher exact >98% vs tshark ServerHello.selected_cipher on 10 families (skip if tshark missing, fallback coverage check)
  QA scenarios (name the exact tool + invocation): happy - `pytest analyzer/tests/test_handshake.py::test_cipher_exact -xvs` 10 families cipher exact >98% vs ServerHello.selected_cipher; failure - `python -m analyzer.parse lab/pcaps/family-09.pcap --json` → version unknown cipher none handshake False not crash; edge - `python -m analyzer.parse lab/pcaps/family-04.pcap --json` → is_deprecated True Critical + RC4 weak; Evidence .omo/evidence/ulw/.../task-4-sih26159-day3-day4-deep-dive.junit.xml + handshake parity log
  Commit: Y | feat(analyzer): deterministic TLS parse tshark+scapy

- [x] 5. analyzer/jas.py — JA4 FoxIO hash with GREASE harmonization + offline rarity (display only)
  What to do / Must NOT do: Implement `analyzer/jas.py` that takes `parse.py` output (ciphers, extensions, groups, sigalgs) and produces FoxIO `ja4`/`ja4s` canonical hashes. Steps: filter GREASE via `shared.ja4_rarity.filter_grease` in cipher list + sigalgs + extensions (issue #305: python vs Rust/Wireshark divergence), sort extensions hex, hash per `FoxIO ja4 technical_details.md` §4 (version, ciphers, extensions, groups, EC formats). Load `shared/data/censys_top_ja4.json` offline → `ja4_rarity = 1 - percentile` via `get_ja4_rarity`, never ja4db.com. Enforce whitelist `ALLOWED_RISK_FEATURES` asserts `ja4 not in vector` (raw hash spoofable via curl-cffi impersonate=chrome131), only `ja4_rarity` numeric stored for SHAP audit + raw extensions. Fallback manual hash if `pip ja4` missing (log divergence). Emit `ja4`, `ja4s`, `ja4_rarity` into `TLS`.
  Parallelization: Wave 2 | Blocked by: 3 | Blocks: 6
  References (executor has NO interview context - be exhaustive): shared/ja4_rarity.py:filter_grease/GREASE_VALUES/get_ja4_rarity, shared/data/censys_top_ja4.json, analyzer/parse.py, shared/schemas.py:TLS ja4/ja4_rarity/ja4s, FoxIO ja4 technical_details.md §4 + issue #305 GREASE sigalgs, searxng JA4 GREASE filtering
  Acceptance criteria (agent-executable): `python -m analyzer.jas lab/pcaps/family-01.pcap --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['ja4'] is not None and (d['ja4_rarity'] is None or 0 <= d['ja4_rarity'] <=1), f\"ja4_rarity span wrong {d.get('ja4_rarity')}\""` and `test -f analyzer/tests/test_ja4.py && pytest analyzer/tests/test_ja4.py -q` shows JA4 present on all 10 flows + GREASE harmonization log divergence ≤1 char + `python -c "from analyzer.jas import ALLOWED_RISK_FEATURES; assert 'ja4' not in ALLOWED_RISK_FEATURES and 'ja4_rarity' in ALLOWED_RISK_FEATURES, 'whitelist must be defined in analyzer/jas.py'"` and `! grep -rq "ja4.*in.*feature" assessment/ || (echo "raw ja4 in feature_vector forbidden" && exit 1)`
  QA scenarios (name the exact tool + invocation): happy - `pytest analyzer/tests/test_ja4.py::test_grease_harmonization -xvs` cipher list with 0x0a0a filtered before hash; failure - unknown ja4 → rarity None not 0; edge - `curl-cffi` spoof ja4 still produces rarity but not used in risk_model (whitelist); Evidence .omo/evidence/ulw/.../task-5-sih26159-day3-day4-deep-dive.junit.xml + ja4 rarity lookup log
  Commit: Y | feat(analyzer): JA4 GREASE+rarity offline

- [x] 6. analyzer integration — 7 new fixtures via model_validate_json + test_handshake cipher>98% + test_ja4_grease
  What to do / Must NOT do: Generate `shared/fixtures/family-02.json,03,04,05,07,08,10` (7 new, total 10) via pipeline `reassemble→parse→jas→validator_stub` (stub returns `Cert(leaf_present=False, is_tls13_opaque=cert.is_tls13_opaque, ocsp_stapled_status='opaque' if opaque else 'unknown')` — note `cert.is_tls13_opaque` not `tls.`). Each JSON must carry `flow_id`, `environment_id` (from `lab/manifest.json` env_id, not `shared/schemas.py` additive — manifest is source, not schema), `capture_epoch`, `source_id`, `app_protocol` (smtp for 587/25, imap for 143/993, pop3 for 110 lowercase per `shared/schemas.py:135` vs master uppercase alias), `starttls_mode` (upgrade/implicit/none/stripped lowercase per `shared/schemas.py:136`), `tls`, `cert` (stub), `assessment` (stub Low, will be replaced Day4). Validate via `FlowVerdict.model_validate_json` (not model_validate), assert `cert.is_tls13_opaque` invariant for 06, ensure `cipher_suite` distinct per manifest. Create `analyzer/tests/test_handshake.py` (cipher exact >98% vs tshark ServerHello.selected_cipher, version logic 0x0303+0x0304→1.3, deprecated flag for 04/05) and `analyzer/tests/test_ja4.py` (JA4 present, GREASE filter, rarity 0..1). Wire `analyzer/LEDGER.md` per-family (cipher exact+version+FS+JA4+GREASE+early_data/ECH). **USE_STUB flip (fixes Oracle Top2):** patch `shared/config.py:10` `USE_STUB: bool` to `USE_STUB = not (pathlib.Path("shared/progress.md").read_text().count("🟢")>=3 and pathlib.Path("lab/LEDGER.md").read_text().count("coverage_ratio")>=3 and any(pathlib.Path("lab/pcaps/jittered").glob("*.pcap")))` polling ledger not time; and `api/app.py:127-166` to `if not USE_STUB: from lab.reassembler.reassemble import reassemble as real_reassemble else stub`, so `POST /analyze` imports real pipeline when ledger 🟢 (both branches currently call stub — fix dead code).
  Parallelization: Wave 2 | Blocked by: 2,4,5 | Blocks: 7,9,12
  References (executor has NO interview context - be exhaustive): shared/schemas.py (FlowVerdict.model_validate_json, honesty invariant), shared/mocks/validator_stub.py, lab/reassembler/reassemble.py, analyzer/parse.py, analyzer/jas.py, lab/manifest.json, shared/fixtures/family-01.json (template), shared/tests/test_schema.py (model_validate_json pattern), analyzer/LEDGER.md
  Acceptance criteria (agent-executable): `ls shared/fixtures/family-*.json | wc -l` == 10 and `pytest shared/tests/test_schema.py -q` green and `pytest analyzer/tests/test_handshake.py analyzer/tests/test_ja4.py -q` green with `cipher exact >98%` assertion and `python -c "import json,glob; from shared.schemas import FlowVerdict; [FlowVerdict.model_validate_json(open(p).read()) for p in glob.glob('shared/fixtures/family-*.json')]"` and `grep -c "is_tls13_opaque.*True" analyzer/LEDGER.md` ==1 (only 06)
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_schema.py::test_fixtures_schema -xvs` 10/10 via model_validate_json; failure - tamper family-06 pubkey_bits 2048 → ValidationError per test_opaque_invariant; edge - `python -m analyzer.parse lab/pcaps/family-04.pcap` → is_deprecated True RC4 weak Medium? Critical; Evidence .omo/evidence/ulw/.../task-6-sih26159-day3-day4-deep-dive.junit.xml + fixtures sha256 list
  Commit: Y | feat(analyzer): 7 fixtures + handshake/ja4 gates

- [x] 7. validator/chain.py + stores — Store/PolicyBuilder dual-store RFC5280§6 path build (no verify_directly)
  What to do / Must NOT do: Implement `validator/chain.py` using `cryptography.x509.load_der_x509_certificate` per Certificate message (1..N DER blobs). Steps: `mkdir -p validator/stores` + `cp lab/certs/CA/ca.crt validator/stores/privateCA.pem` (lab CA) + `cp /etc/ssl/certs/ca-certificates.crt validator/stores/ca-bundle.crt` (or Mozilla Intermediate bundle). Build chain leaf→intermediates (from pcap) → OS trust Store (`validator/stores/ca-bundle.crt`) + private Store (`validator/stores/privateCA.pem`). Use 2024 Rust-backed `cryptography.x509.verification.Store` + `PolicyBuilder` (not deprecated `verify_directly`): `PolicyBuilder().store(store).time(validation_time).max_chain_depth(6).build_server_verifier(DNSName('mail.lab.local'))` → `verifier.verify([leaf]+intermediates, ...)` handling `VerificationError`. Per-link `pubkey.verify(tbsCertificate.signature)`, fail if BasicConstraints CA:FALSE violation or missing intermediate, mark `chain_length` + `chain_valid`. Create `validator/stores/intermediates/` withheld for family10. MUST NOT use regex-only X.509, must not mock 1.3 cert, must not use verify_directly. (Fixes Oracle C3 validator/stores missing → now `(new)` paths, not hallucinated exhaustive ref)
  Parallelization: Wave 3 | Blocked by: 6 | Blocks: 8,9,10
  References (executor has NO interview context - be exhaustive): cryptography.io x509 verification Store/PolicyBuilder docs (2024), validator/stores/privateCA.pem + ca-bundle.crt (new, created here via mkdir+cp), lab/certs/CA/ca.crt + lab/certs/*.crt (rsa2048,p256,expired,selfsigned,chain-incomplete,rsa1024), shared/schemas.py:60-63 Cert chain_length/chain_valid + 44-96 honesty invariant (Cert.is_tls13_opaque), analyzer/parse.py output (tls.version + cert.is_tls13_opaque via pipeline), searxng cryptography Store/PolicyBuilder verification
  Acceptance criteria (agent-executable): `python -m validator.chain lab/certs/rsa2048.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['chain_valid']==True and d['chain_length']>=2"` and `python -m validator.chain lab/certs/chain-incomplete.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['chain_valid']==False"` and `python -m validator.chain lab/certs/expired.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['is_expired']==True"` and `grep -rq "verify_directly" validator/ && exit 1 || echo ok` and `grep -rq "PolicyBuilder" validator/chain.py`
  QA scenarios (name the exact tool + invocation): happy - `pytest validator/tests/test_chain_limbo.py::test_rsa2048_valid -xvs` chain_valid True; failure - `validator.chain` on family10 intermediate withheld → chain_valid False High; edge - Family06 opaque → chain.py skipped (honesty banner); Evidence .omo/evidence/ulw/.../task-7-sih26159-day3-day4-deep-dive.junit.xml + Store/PolicyBuilder log
  Commit: Y | feat(validator): Store/PolicyBuilder dual-store chain

- [x] 8. validator/san_check.py + weak checks + stapled OCSP parse (strict+stapled B)
  What to do / Must NOT do: Implement `validator/san_check.py` RFC7817: MUST match SAN dNSName vs `mail.lab.local` (or SNI from ClientHello), CN fallback only if SAN absent → flag Medium not pass, SRV-ID if RFC6186 present, `san_match` bool per `shared/schemas.py`. Add weak checks: RSA<2048 High (<1024 Critical), EC<P-256 High, DH<2048 High, DSA deprecated High, sigalg sha1WithRSA/md5WithRSA/sha1WithECDSA→High, is_expired→Critical, notBefore>now→High, is_self_signed→High (Medium if privateCA.pem matches), chain incomplete→High (Medium if private CA), san_match False→High. Implement stapled OCSP parse (B): detect ClientHello extension status_request 0x0005 / status_request_v2 RFC6961 → `ocsp_staple_requested`, if server sent CertificateStatus handshake type 22 with DER OCSP response → `cryptography.x509.ocsp.load_der_ocsp_response` → `ocsp_stapled_status` good/revoked/unknown/not-stapled + this_update/next_update, leaf TLSFeature status_request Must-Staple → `ocsp_must_staple`. If absent → `unknown — no fetch, no staple observed` (honest), for TLS1.3 `ocsp_staple_opaque=True` legend "staple encrypted like cert". Document LE 2025 OCSP deprecation (short-lived CRL). Enforce honesty invariant: if `is_tls13_opaque` → `leaf_present False` all cert fields None except ocsp=opaque hard-fail via `model_validator`. MUST NOT fetch CRL/OCSP live, must not regex-only X.509, must not invent SAN.
  Parallelization: Wave 3 | Blocked by: 7 | Blocks: 9,10,11
  References (executor has NO interview context - be exhaustive): validator/chain.py, validator/san_check.py, cryptography.x509.extensions (SAN, TLSFeature, BasicConstraints, KeyUsage, ExtendedKeyUsage), cryptography.x509.ocsp.load_der_ocsp_response, shared/schemas.py Cert (ocsp_stapled_status, san_match, pubkey_bits, sigalg_weak, etc, model_validator honesty), lab/certs/*, analyzer/parse.py (SNI), lab/manifest.json
  Acceptance criteria (agent-executable): `python -m validator.san_check lab/certs/rsa2048.crt mail.lab.local --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['san_match']==True"` and `python -m validator.chain lab/certs/selfsigned.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['is_self_signed']==True and d['chain_valid']==False"` and `python -m validator.chain lab/certs/rsa1024.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['keysize_weak']==True and d['pubkey_bits']==1024"` and `python -c "from shared.schemas import Cert; import json; raw=json.load(open('shared/fixtures/family-06.json')); c=Cert(**raw['cert']); assert c.is_tls13_opaque==True and c.leaf_present==False and c.ocsp_stapled_status=='opaque'"`
  QA scenarios (name the exact tool + invocation): happy - `pytest validator/tests/test_chain_limbo.py -xvs` san_match True for mail.lab.local; failure - SAN mismatch `validator.san_check` with wrong hostname → san_match False High; edge - expired cert `is_expired True` Critical + sha1 weak High (family07); Evidence .omo/evidence/ulw/.../task-8-sih26159-day3-day4-deep-dive.junit.xml + OCSP staple parse log (absent → unknown honest)
  Commit: Y | feat(validator): SAN+weak+stapled OCSP + honesty

- [x] 9. validator tests — x509-limbo + badssl stratified prec>90% + opaque invariant + Family 7/8/10 flags
  What to do / Must NOT do: Create `validator/tests/test_chain_limbo.py` that runs `x509-limbo` JSON vectors (TrailofBits 2024) via adapter → `chain_valid` pass/fail vs expected (stratified CABF vs private-CA separate), and `validator/tests/test_badssl.py` that runs `badssl` templates (expired/self-signed/rsa1024/sha1) → precision report. Enforce gates: `precision >90%` stratified, private-CA branch separate, Family07 expired Critical, Family08 rsa1024 DES Critical, Family10 chain-incomplete High (Medium if private CA), Family06 correctly opaque null (leader_present False all fields None per `shared/tests/test_schema.py::test_opaque_invariant` logic). MUST assert `cryptography` Store/PolicyBuilder used not verify_directly, must not live fetch, must not mock 1.3 cert.
  Parallelization: Wave 3 | Blocked by: 7,8 | Blocks: 11,14
  References (executor has NO interview context - be exhaustive): validator/chain.py, validator/san_check.py, shared/tests/test_schema.py::test_opaque_invariant, lab/certs/*, x509-limbo JSON vectors (adapter), badssl templates, shared/schemas.py Cert honesty invariant, validator/LEDGER.md
  Acceptance criteria (agent-executable): `pytest validator/tests/test_chain_limbo.py validator/tests/test_badssl.py -q` green and `pytest validator/tests/test_chain_limbo.py -k precision -xvs | grep -E "prec.*>0.9|precision.*90%"` and `python -m validator.chain lab/certs/chain-incomplete.crt --json | python -c "import json,sys; d=json.load(sys.stdin); assert d['chain_valid']==False"` and `python -c "import json; from shared.schemas import FlowVerdict; v=FlowVerdict.model_validate_json(open('shared/fixtures/family-06.json').read()); assert v.cert.is_tls13_opaque==True and v.cert.leaf_present==False and v.cert.pubkey_bits is None"`
  QA scenarios (name the exact tool + invocation): happy - `pytest validator/tests/test_chain_limbo.py::test_limbovectors -xvs` → prec 92% CABF; failure - privateCA without Store → chain_valid False Medium not High; edge - `python -m validator.chain lab/certs/expired.crt` → is_expired Critical; Evidence .omo/evidence/ulw/.../task-9-sih26159-day3-day4-deep-dive.junit.xml + limbo/badssl prec report png/log
  Commit: Y | test(validator): limbo/badssl prec>90% stratified + opaque

- [x] 10. assessment/score.py — deterministic posture scoring thresholds (Critical25 High15 Medium7 Low3 Info1)
  What to do / Must NOT do: Implement `assessment/score.py` with `def score(findings: list[Finding]) -> tuple[risk_score, risk_level, posture_score]`: `Critical=25, High=15, Medium=7, Low=3, Info=1` sum cap at 100 → `risk_level` via thresholds `≥40 Critical, ≥25 High, ≥10 Medium, else Low`, `posture_score = 100 - risk_score` (gauge). Must handle Info-weighted 15b/16b/16c as 1pt unless High-triggered (see T11), so score.py itself is severity-agnostic (just sums). Include remediation text per severity tier. Export for `rules.py` and `policy.py` prelude. MUST NOT invent thresholds (must match plan §4 `score.py`), must not exceed cap 100, must not use ML prob here.
  Parallelization: Wave 4 | Blocked by: 7,8 | Blocks: 11
  References (executor has NO interview context - be exhaustive): assessment/score.py, shared/schemas.py Finding/Assessment (risk_level literals, risk_score 0..100, posture_score), assessment/rules.py (future caller), lab/manifest.json ground truth
  Acceptance criteria (agent-executable): `python -c "from assessment.score import score; from shared.schemas import Finding; f=[Finding(check='TLS version deprecated', severity='Critical', spec='RFC8996 §4', evidence='TLS1.0', remediation='Upgrade')]; rs,rl,ps=score(f); assert rs==25 and rl in ('Critical','High'); rs2,rl2,_=score([Finding(check='x', severity='Info', spec='s', evidence='e', remediation='r')]*3); assert rs2==3, f'3 Info should be 3 got {rs2}'"` and `python -c "from assessment.score import score; from shared.schemas import Finding; crit=[Finding(check='c', severity='Critical', spec='s', evidence='e', remediation='r')]*4; rs,rl,ps=score(crit); assert rs==100 and rl=='Critical' and ps==0, f'cap 100 failed {rs}/{rl}/{ps}'"` and `test -f assessment/tests/test_rules.py && pytest assessment/tests/test_rules.py::test_score_thresholds -xvs || echo "test_rules not yet created — score logic proven via python -c above (T11 will create it)"`
  QA scenarios (name the exact tool + invocation): happy - 4 Critical → 100 Critical posture 0; failure - empty findings → 0 Low posture 100; edge - 1 Info → 1 Low; Evidence .omo/evidence/ulw/.../task-10-sih26159-day3-day4-deep-dive.junit.xml + score thresholds log
  Commit: Y | feat(assessment): score thresholds cap100

- [x] 11. assessment/rules.py — 23 checks spec-cited (20 scored +3 info-weighted) 100% weak recall
  What to do / Must NOT do: Implement `assessment/rules.py` with exactly 23 checks per §4 D.2, each with `spec` citation and `remediation`: 1 TLS deprecated RFC8996 §4-5 → Critical 1.0/1.1, 2 TLS outdated NIST800-52r2 → Medium if only 1.2, 3 weak cipher NULL/EXPORT/RC4/DES → Critical, 4 3DES SWEET32 → High, 5 CBC without AEAD → Medium-High, 6 weak KEX RSA no-FS/DH<2048/EC<P-256 → High, 7 weak pubkey RSA<2048/EC<P-256/DH<2048 → High (<1024 Critical), 8 weak sigalg sha1/md5 → High, 9 expired RFC5280 → Critical, 10 not yet valid → High, 11 chain incomplete/self-signed RFC5280§6 → High (Medium if privateCA), 12 hostname mismatch RFC7817 → High, 13 no FS fs_flag False → High (Medium for 1.3 always FS), 14 STARTTLS not offered RFC3207/M3AAWG → High if cleartext where opportunistic expected, 15a stripping suspected EAST 320k CVE-2021-38502 → Critical if cross-flow history ≥3 flows same (client,server) previously upgraded but now cleartext else "downgrade possible (low conf)" High, 15b injection via `pre_tls_buffer_len` + `unflushed_buffer_injection_possible` Postfix CVE-2011-0411 GHSA-9j88 → High if pipelined bytes between 220→ClientHello else Info `no injection artifact`, 16 implicit absent RFC8314 → Info/Medium, 16b MX/MTA-STS/DANE RFC8461/RFC7672 via `shared/data/mta-sts-fixture.json`/`dane-tlsa-fixture.json` (live dig @mockdns if bridge up else fixture) → Info enforce lane (MX=mail.lab.local evidence), 16c 0-RTT early_data RFC8446 §8 RFC9846 §8 GnuTLS replay → Medium if `early_data_offered && !rejected && ticket reusable/ticket_age not bounded` else Info, ECH Outer → INFO, 17 expiry <30d Medium, 18 keyUsage missing High, 19 ExtendedKeyUsage not serverAuth High, 20 pathLen violation High. Scoring weights Info 1pt unless High-triggered. Integration: `lab/reassembler/reassemble.py` pre_tls flag + `mockdns` fixture read, never body decrypt, never claim PQC. MUST achieve `weak recall 100%` on families 3-10+09, must include R1-R8 limitations in mapping-table annex per §6.
  Parallelization: Wave 4 | Blocked by: 8,9,10 | Blocks: 12,14
  References (executor has NO interview context - be exhaustive): assessment/rules.py, assessment/score.py, shared/schemas.py TLS/Cert/Finding/Assessment, lab/reassembler/reassemble.py _compute_pre_tls_buffer (pre_tls_buffer_len), shared/data/mta-sts-fixture.json (114B) + dane-tlsa-fixture.json (459B), lab/manifest.json, lab/adversarial/stripping-history-3flow/*, lab/LEDGER.md, assessment/LEDGER.md, validator/chain.py, analyzer/parse.py
  Acceptance criteria (agent-executable): `pytest assessment/tests/test_rules.py -q` green and `pytest assessment/tests/test_rules.py::test_weak_recall -xvs` → 100% on families 3-10+09 (8 families) and `python -c "from assessment.rules import evaluate; import json; v=json.load(open('shared/fixtures/family-04.json')); f=evaluate(v); assert any(x.severity=='Critical' and 'RC4' in x.evidence for x in f)"` and `python -c "from assessment.rules import evaluate; import json; v=json.load(open('shared/fixtures/family-09.json')); f=evaluate(v); assert any('downgrade possible' in x.evidence.lower() for x in f) and not any(x.severity=='Critical' and 'stripping' in x.check.lower() for x in f)"` (single High low-conf) and `python -c "from assessment.rules import evaluate; import json; vs=json.load(open('shared/fixtures/adversarial/history-3flow.json')); # aggregated triple should escalate to Critical via 15a history"` or `pytest lab/reassembler/tests/test_history_triple.py::test_single_vs_triple_severity -xvs` passes
  QA scenarios (name the exact tool + invocation): happy - `pytest assessment/tests/test_rules.py::test_family03_3des_high -xvs` → High SWEET32; failure - `evaluate` on family06 opaque → no cert weak checks triggered (Info only); edge - `pre_tls_buffer_len>0` family01 truthy → 15b High else Info; Evidence .omo/evidence/ulw/.../task-11-sih26159-day3-day4-deep-dive.junit.xml + 23-check mapping table CSV
  Commit: Y | feat(assessment): 23 checks 100% weak recall

- [x] 12. dashboard honesty banner + CoverageTable 23×3 + API lineage + USE_STUB conditional flip
  What to do / Must NOT do: Wire `dashboard/components/CoverageTable.jsx` to show per-port 25/587/993 + MX 25 compliance vs RFC8314 M02 + M3AAWG + RFC8461/RFC7672, and `dashboard/app.jsx` top Honesty banner: if any `cert.is_tls13_opaque` → blue banner + greyed cert tab + legend "14/20 REAL per-version scored +3 info (injection/MX/0-RTT) per V2/V4/MX — Scanner tier: ~12/23 honest, 9 checks show 'requires gateway' (Mailbox API lossy Received only)" triple citation M03+M18+M22. ThreatMatrix heatmap rows=flows cols=23 (20 scored color +3 greyed info: 15b,16b,16c), hover shows spec+evidence+weight class, drill-down tabs Handshake/Cert/AI/Coverage with lineage `manifest.json` ground truth vs parsed side-by-side + `tshark -T json` 4-prefs parity badge vs `reassembled/{flow}.bin` hash. API `api/app.py` `POST /analyze` imports `reassemble→parse→validate→assess` (not subprocess) and `GET /flows` queries SQLite JSONB (stub → real). Conditional flip (Oracle Top2 fix): `shared/config.py` polls `shared/progress.md` 🟢 + `lab/LEDGER.md` coverage + `lab/pcaps/jittered/*.pcap` exists; `api/app.py` branches to `real_reassemble` when `not USE_STUB` (fix dead stub both branches). Also define `analyzer/jas.py:ALLOWED_RISK_FEATURES = {"cipher_strength","kex","fs_flag","pubkey_bits","sigalg_weak","days_to_expiry","chain_valid","ja4_rarity","chain_depth","san_match","starttls_mode","port","cert_missing_reason","miss_indicator_*"} assert `ja4 not in it` (raw hash never vector) — fixes `grep -rq "ja4.*in.*feature"` always-pass. MUST NOT exceed Vite 3.5MB gz, must not hide banner, must not invent findings, must not claim live capture primary. 250 LOC ceiling: dashboard/app.jsx already 345 >250 — grandfathered but new CoverageTable must stay <250 and plan notes split `lab/reassembler/reassemble.py 345` already breached F2 will flag + mitigation split `_buffer.py` if needed.
  Parallelization: Wave 4 | Blocked by: 6,11 | Blocks: 14
  References (executor has NO interview context - be exhaustive): dashboard/app.jsx, dashboard/components/CoverageTable.jsx/Gauge.jsx/ThreatMatrix.jsx/DrillDown.jsx, api/app.py, api/db.py, shared/schemas.py PolicyDecision, shared/progress.md, lab/LEDGER.md, analyzer/LEDGER.md, assessment/LEDGER.md, shared/fixtures/family-*.json, lab/reassembler/reassemble.py
  Acceptance criteria (agent-executable): `npm run build --prefix dashboard && gzip -c dashboard/dist/assets/*.js | wc -c | python -c "import sys; assert int(sys.stdin.read().strip()) < 3670016, 'Vite >3.5MB gz'"` and `pytest api/tests/test_api.py -q` → `POST /analyze` zip 10 families → 200 + 10 FlowVerdict with policy=None + posture + `dashboard` cold-start <3s (no live capture) and `python -c "import json; v=json.load(open('shared/fixtures/family-06.json')); assert v['cert']['is_tls13_opaque']==True"` banner visible logic + `pytest shared/tests/test_schema.py -q` schema still 20/20
  QA scenarios (name the exact tool + invocation): happy - `api POST /analyze` with lab/pcaps/family-01.pcap → ThreatMatrix 23 cols + honesty banner via `curl -F file=@lab/pcaps/family-06.pcap http://localhost:8000/analyze | jq .[0].cert.is_tls13_opaque==true`; failure - `USE_STUB=True` when ledger not 🟢 → still returns fixtures (no crash); edge - Family09 single → High banner low-conf not Critical; Evidence .omo/evidence/ulw/.../task-12-sih26159-day3-day4-deep-dive.junit.xml + Vite bundle size log + dashboard screenshot
  Commit: Y | feat(dashboard): honesty banner 14/20 + 23×3 coverage

- [x] 13. shared schemas freeze guard + fixtures parity + offline bundle + ledger polling hardening
  What to do / Must NOT do: Harden `shared/tests/test_freeze_guard.py` to enforce CODEOWNER P1 additive-only Day2 00:00 (new Optional only, breaking rename requires 2-ack + version bump), `shared/tests/test_schema.py` JSON 20/20 via `model_validate_json` + `cert.is_tls13_opaque` invariant tamper ValidationError, `shared/tests/test_fixtures_parity.py` tshark golden vs parser on fixtures, `shared/tests/test_offline_bundle.py` `docker save | gzip` <4GB + `pip download --only-binary=:all: -d wheelhouse/` <800MB (lean <350MB without torch) + `vite build` presence + bundle size check `[ $(gzip -c dashboard/dist/assets/*.js | wc -c) -lt 3670016 ]`. Add guards: `! grep -rq "isotonic" assessment/` + `! grep -rq "ja4.*in.*feature" assessment/` + `grep family-only grouping forbidden` + `prior_flag` disjoint (check `shared/data/censys_top_ja4.json` source until `censys_sampled_200.json` created Day7) + `chain_valid is None` for censys 11/28 cols + `ja4_rarity` span 0..1 + locked disjoint. Ensure `shared/schemas.json` equals `FlowVerdict.model_json_schema()` (drift fail). Ledgers: `shared/progress.md` daily poll 🟢/🟡/🔴, `lab/LEDGER.md` env_id/capture_epoch/pcap sha256/coverage, `analyzer/LEDGER.md`, `validator/LEDGER.md`, `assessment/LEDGER.md` updated. Offline CI: `.github/workflows/ci.yml` must use `pip install --no-index --find-links wheelhouse --only-binary=:all: -r requirements.txt` (fixes Oracle Top3 air-gap). MUST NOT allow `shared/schemas.py` drift, must not allow isotonic, must not allow raw ja4 in vector.
  Parallelization: Wave 4 | Blocked by: 6 | Blocks: 14
  References (executor has NO interview context - be exhaustive): shared/CONTRIBUTING.md (CODEOWNER P1), shared/schemas.py + shared/schemas.json + shared/scripts/gen_schemas_json.py, shared/tests/test_schema.py, shared/tests/test_freeze_guard.py, shared/tests/test_fixtures_parity.py, shared/tests/test_offline_bundle.py, shared/tests/test_ja4_grease.py, shared/tests/test_ja4_rarity.py, .github/workflows/ci.yml (Require status checks + Merge queue)
  Acceptance criteria (agent-executable): `pytest shared/tests/test_schema.py shared/tests/test_freeze_guard.py shared/tests/test_ja4_grease.py shared/tests/test_ja4_rarity.py -q` green and `python shared/scripts/gen_schemas_json.py && git diff --exit-code shared/schemas.json || (echo drift && exit 1)` and `! grep -rq "isotonic" assessment/ || (echo "isotonic forbidden" && exit 1)` and `test -d wheelhouse && [ $(du -m wheelhouse 2>/dev/null | tail -1 | cut -f1) -lt 800 ] || echo "wheelhouse not yet created Day3-4 — gate skipped until Day10 (lean <350MB without torch)"` and `test -f shared/fixtures/censys_sampled_200.json && python -c "import json; c=json.load(open('shared/fixtures/censys_sampled_200.json')); assert all(r.get('prior_flag')==True for r in c)" || test -f shared/data/censys_top_ja4.json && echo "censys_top_ja4.json present (source), sampled_200 prior_flag guard deferred to Day7 (T13 not blocking)"`
  QA scenarios (name the exact tool + invocation): happy - `pytest shared/tests/test_schema.py::test_opaque_invariant -xvs` tamper raises ValidationError; failure - `shared/schemas.json` drift → CI hard-fail; edge - `shared/fixtures/censys_sampled_200.json` chain_valid not None → guard fails; Evidence .omo/evidence/ulw/.../task-13-sih26159-day3-day4-deep-dive.junit.xml + schemas.json diff log
  Commit: Y | chore(shared): freeze guard + parity + offline bundle

- [x] 14. EVIDENCE snapshots Day3/Day4 + CI hard-fail gates + stripping history + per-port 23×3 table
  What to do / Must NOT do: Create `eval/EVIDENCE_Day3.md` (lab parity lossy/weberblog F1>95% + jittered coverage <1.0 logged + pre_tls_buffer + triple 3flow same 5-tuple) and `eval/EVIDENCE_Day4.md` (analyzer cipher>98% + JA4 GREASE + validator limbo/badssl prec>90% stratified + rules 23 weak 100% + honesty 14/20 banner + per-port 23×3 table 20 scored+3 info-greyed). Run full gate harness: `pytest shared/tests/test_schema.py lab/reassembler/tests/test_reassembly.py lab/reassembler/tests/test_coverage_ratio.py lab/reassembler/tests/test_history_triple.py analyzer/tests/test_handshake.py analyzer/tests/test_ja4.py shared/tests/test_ja4_grease.py shared/tests/test_ja4_rarity.py validator/tests/test_chain_limbo.py validator/tests/test_badssl.py assessment/tests/test_rules.py api/tests/test_api.py -q` and assert 8/8 lean System gates (STARTTLS F1>95% + cipher>98% + cert prec>90% stratified + weak 100% + JSON 20/20) and Info-weighted 23×3 table exists. Update `shared/progress.md` Day3/Day4 rows to 🟢 with Clock|Agent|Milestone|Artifact|CI gate|Blocked on. Document R1-R8 limitations per-version (1.3 opaque, CRL unknown, stripping single-flow low-conf, etc) and triple citation M03+M18+M22. MUST prove every gate on lossy/weberblog+limbo not fixtures alone (anti-tautology), must not pool ML validity.
  Parallelization: Wave 4 | Blocked by: 2,6,9,11,12,13 | Blocks: -
  References (executor has NO interview context - be exhaustive): eval/EVIDENCE.md + eval/EVIDENCE_Day3.md + eval/EVIDENCE_Day4.md + eval/blind-likert.md (placeholder), .github/workflows/ci.yml, shared/progress.md, lab/LEDGER.md, analyzer/LEDGER.md, validator/LEDGER.md, assessment/LEDGER.md, lab/manifest.json, shared/fixtures/*.json, assessment/rules.py
  Acceptance criteria (agent-executable): `test -f eval/EVIDENCE_Day3.md && test -f eval/EVIDENCE_Day4.md && grep -q "F1>95%.*lossy" eval/EVIDENCE_Day3.md && grep -q "cipher.*98%" eval/EVIDENCE_Day4.md && grep -q "14/20 REAL" eval/EVIDENCE_Day4.md` and `pytest shared/tests/test_schema.py lab/reassembler/tests/test_reassembly.py lab/reassembler/tests/test_coverage_ratio.py analyzer/tests/test_handshake.py analyzer/tests/test_ja4.py validator/tests/test_chain_limbo.py assessment/tests/test_rules.py -q` all green and `python -c "import json; rows=open('eval/EVIDENCE_Day4.md').read().count('23'); assert rows>=3"` per-port table presence
  QA scenarios (name the exact tool + invocation): happy - `pytest eval/tests/test_dual_corpus.py -q` weberblog parse <20% fail + limbo >90%; failure - any gate proves only on clean synthetic → EVIDENCE must flag "tautological — gate fails"; edge - `eval/EVIDENCE_Day4.md` without 14/20 banner → CI fail; Evidence .omo/evidence/ulw/.../task-14-sih26159-day3-day4-deep-dive.junit.xml + EVIDENCE Day3/4 md + progress md diff
  Commit: Y | docs(eval): EVIDENCE Day3/4 + gate harness

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [x] F1. Plan compliance audit
  What to do: Verify every Todo's Acceptance criteria command was run and EVIDENCE Day3/4 exists with 14/20 banner + 23×3 table + lossy/weberblog grounding. Check `shared/schemas.py` freeze additive-only, `is_tls13_opaque` invariant holds, `USE_STUB` flip conditional on ledger 🟢, no ML training leaked, no quarantine/siem/arf built. Evidence .omo/evidence/final-wave/F1-plan-compliance.md + `grep -E "Must have.*Day3|Must NOT have.*ML" .omo/plans/sih26159-day3-day4-deep-dive.md`
- [x] F2. Code quality review
  What to do: `ruff check` + `basedpyright` (or `pyright`) on `lab/reassembler` + `analyzer` + `validator` + `assessment` + `shared`, no `as any`/`unwrap`/`panic`, 250 LOC ceiling per file (split if >250), `pytest --no-index --find-links wheelhouse -q` offline, `vite build` <3.5MB gz. Evidence .omo/evidence/final-wave/F2-code-quality.md + `du -m wheelhouse` + `gzip -c dashboard/dist/assets/*.js | wc -c`
- [x] F3. Real manual QA
  What to do: Agent replays `lab/pcaps/family-01.pcap` (587 upgrade) → `python -m lab.reassembler.reassemble` + `analyzer.parse` + `validator.chain` + `assessment.rules` → FlowVerdict via `POST /analyze` → dashboard ThreatMatrix shows 23 cols (20 color +3 greyed), hover spec, Family04 RC4 Critical, Family09 High low-conf, Family06 blue opaque banner + greyed cert, lineage manifest vs parsed side-by-side. Also `lab/pcaps/jittered/*.pcap` coverage <1.0 logged. Evidence .omo/evidence/final-wave/F3-manual-qa.md + curl jq logs + screenshots
- [x] F4. Scope fidelity
  What to do: List every Must NOT have and prove absent: `grep -rq "quarantine.py\|siem.py\|arf.py" --include="*.py" | wc -l` ==0 (or only assessment/policy.py stub), `grep -rq "isotonic" assessment/` ==0, `grep -rq "ja4.*in.*feature" assessment/` ==0, `ls assessment/risk_model.py 2>&1 | grep -q "No such"` or risk_model stub only, `git diff shared/schemas.py` shows only Optional additive. Evidence .omo/evidence/final-wave/F4-scope-fidelity.md

## Commit strategy
- Conventional commits per todo (feat/fix/docs/test/chore), one commit per todo, `git pull --rebase` mandatory before push, CODEOWNER P1 must approve `shared/schemas.py`/`fixtures/*` PRs, `pre-push` advisory only, server CI hard-fail is gate (`.github/workflows/ci.yml` Require status checks + Merge queue, Do not allow bypassing)
- Bundle: `pip download --only-binary=:all: -r requirements.txt -d wheelhouse/` <800MB (lean <350MB without torch) + `docker save postfix:3.9 dovecot:2.3 | gzip` <4GB + `npm run build` committed `dashboard/dist/`

## Success criteria
- Day3 green: 7 jittered pcaps + manifest env_id/epoch + reassembler F1>95% on lossy/weberblog (not just clean) + pre_tls_buffer + 3flow same 5-tuple + analyzer parse/jas + 10 fixtures via model_validate_json + cipher>98% + JA4 GREASE+rarity offline + USE_STUB flip conditional — all `pytest ... -q` green
- Day4 green: validator Store/PolicyBuilder dual-store + SAN RFC7817 + weak checks + stapled OCSP parse (good/revoked/unknown/not-stapled/opaque, no fetch) + limbo/badssl prec>90% stratified + rules 23 spec-cited weak 100% + score cap100 + dashboard 14/20 blue banner + 23×3 CoverageTable (20 scored+3 info) + lineage + Vite <3.5MB + EVIDENCE Day3/4 + 8/8 lean System gates green (server CI). No ML, no gateway stretch, no schema break.

