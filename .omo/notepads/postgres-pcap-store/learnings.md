## 2026-08-28 Task 1 — docker-compose postgres pgdata healthcheck depends_on

- Choice: postgres:16-alpine pinned to sha256:075f7ba66bc9b3ce7d6b8b635208ff61cd7cf1a67d71ec530eec5d7ae0cbe571 (amd64 manifest digest from `docker manifest inspect postgres:16-alpine` 2026-08-13T19:16:04Z, alpine:3.24 base). Fallback comment keeps unpinned tag for multi-arch.
- Compose wiring: service `postgres` env POSTGRES_DB=ciphcrest POSTGRES_USER=app POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-app_dev_only}, volume pgdata:/var/lib/postgresql/data (named top-level `volumes: pgdata:`) + mount init-db/:/docker-entrypoint-initdb.d/:ro, healthcheck `pg_isready -U app -d ciphcrest -h 127.0.0.1` interval 10s timeout 3s retries 10 start_period 20s, networks lab+default. Demo adds depends_on postgres condition service_healthy and env POSTGRES_DSN=postgresql://app:${POSTGRES_PASSWORD:-app_dev_only}@postgres:5432/ciphcrest + POSTGRES_PASSWORD passthrough. No ports exposed, no bind ./data, single compose file preserved with include lab/docker-compose.yml profiles ["lab"] (network lab 172.31.0.0/24).
- Verification: `docker compose config` exit 0, contains postgres healthcheck pg_isready, pgdata volume, service_healthy, POSTGRES_DSN; `bash scripts/turnup.sh --check` still passes loosely; baseline before.log showed no postgres, after.log shows postgres with depends_on; failing-first api/tests/test_compose_postgres.py 3-failed before, 4-passed after.
- Evidence: .omo/evidence/postgres-pcap-store/task-1-docker-compose.json + .log


## 2026-08-28 Task 2 — canonical Postgres DDL 6 tables + 2 matviews generated cols GIN CONCURRENTLY

- DDL choices: pgcrypto at top for gen_random_uuid(); families TEXT PK with CHECKs port IN (25,110,143,587,993), tls_version IN ('TLS1.0','TLS1.1','TLS1.2','TLS1.3','none'), starttls_mode IN ('upgrade','implicit','stripped','none'), status DEFAULT 'not_run' CHECK IN ('not_run','running','done','failed'); pcap_files BYTEA NOT NULL CHECK octet_length<100MB, sha256 ~ '^[0-9a-f]{64}$', byte_length=octet_length guard, UNIQUE(family_id,sha256) FK CASCADE; flows JSONB NOT NULL CHECK jsonb_typeof='object' + 4 GENERATED ALWAYS AS STORED (risk_score/posture_score INT, risk_level/source_id TEXT) from data->'assessment'; flows_history PK(flow_id,version) FK CASCADE for FOR UPDATE versioning; live_captures UUID PK FK pcap_files, model_runs TEXT PK with params/metrics JSONB artifact_sha n_eff dataset_caveat; plus live_captures_history/lab_runs_history/model_runs_history append-only version PK.
- CONCURRENTLY guard: all CREATE INDEX CONCURRENTLY + CREATE UNIQUE INDEX CONCURRENTLY for matviews outside BEGIN/COMMIT with comment explaining CONCURRENTLY cannot run in transaction; GIN idx_flows_data_gin USING GIN (data jsonb_path_ops) for @> queries, btree idx_flows_risk_score (risk_score DESC, updated_at DESC) enables ORDER BY risk_score without json extract, btree on posture_score/risk_level/source_id/family_id. Matviews mv_dashboard_metrics GROUP BY risk_level and mv_protocol_stats GROUP BY protocol,tls_version,cipher_suite each with UNIQUE index enabling REFRESH MATERIALIZED VIEW CONCURRENTLY; verified REFRESH CONCURRENTLY succeeds, second psql -f idempotent via IF NOT EXISTS.
- Triggers: trg_set_updated_at BEFORE UPDATE on families/flows sets updated_at=now(); trg_flows_mark_family_done AFTER INSERT ON flows UPDATE families SET status='done', last_run_at=now() where family_id=NEW.family_id — derived not_run via EXISTS(SELECT 1 FROM flows WHERE flow_id=f.family_id) preserved.
- Verification: ephemeral postgres:16-alpine@sha256:075f... via docker run -d + pg_isready + psql -f twice exit 0, \d families shows status check, \d flows shows generated cols, \d mv_dashboard_metrics exists, pg_indexes idx_mv_dash_risk present, pgcrypto gen_random_uuid works, BYTEA sha256 regex rejected invalid, trigger flips not_run→done.
- Evidence: .omo/evidence/postgres-pcap-store/task-2-schema-log.sql (psql \d dumps + index list + refresh concurrently + compose config)

## 2026-08-28 Task 3 — Dockerfile psycopg[binary] remove SQLite artifact add pg client

- Choices: requirements.txt added `psycopg[binary]==3.2.5` (latest 3.2.x available 2026-08-28, probe via `pip index versions psycopg` showed 3.3.4 latest but 3.2.5 exists) + `sqlalchemy[asyncio]==2.0.35` pinned exact, kept xgboost==1.7.6 etc. Builder added `libpq-dev` alongside gcc/python3-dev/libffi-dev for psycopg binary build (though binary wheel no compile, kept for completeness). Runtime removed `touch /app/api/flows.db && chown ... chmod 666` fragment — replaced with `RUN chown -R 10001:10001 /app ...` then `RUN rm -f /app/api/flows.db && apt-get update && apt-get install -y --no-install-recommends postgresql-client && rm -rf /var/lib/apt/lists/*` before USER app, preserving tshark 4.2 check and tini entrypoint. No COPY init-db baked — compose mount ./init-db:/docker-entrypoint-initdb.d:ro remains.
- Verification: grep psycopg+sqlalchemy PASS, no touch PASS, rm -f + postgresql-client + libpq-dev present, no COPY init-db, `docker build -t test:postgres-pcap --target runtime .` exit 0 (32s, postgresql-client 15.19 58MB), `docker run --rm test:postgres-pcap python -c "import psycopg"` prints 3.2.5 and sqlalchemy 2.0.35, pg_isready+psql present, flows.db absent, `docker compose config` still valid with postgres healthcheck pg_isready and service_healthy. Test api/tests/test_docker_psycopg.py 8-passed.
- Evidence: .omo/evidence/postgres-pcap-store/task-3-docker-build.log (grep dumps + compose config + build tail + docker run imports)

## 2026-08-28 Task 4 — startup-idempotent deterministic seed + run-dashboard 10

- Choices: `api/seed.py` async `seed_all(dsn, with_dashboard_run=False)` with sync fallback; `deterministic_source_id` via `hashlib.sha256((family_id+str(seed)).encode()).hexdigest()` (full 64 hex, never uuid4); ordering via `int(family_id.split("-")[1])` mimicking `ORDER BY lpad(substring(family_id from 8)::int)` to avoid lexical 11/2 bug; single `BEGIN` wrapping 60 families via `async with conn.transaction()` / `with conn.transaction()` — never per-family commit; families `ON CONFLICT (family_id) DO UPDATE SET ... WHERE IS DISTINCT FROM` avoid WAL when unchanged; pcap_files `psycopg.Binary(data)` streaming + `ON CONFLICT (family_id,sha256) DO UPDATE SET data=... WHERE IS DISTINCT FROM` avoid WAL blow on unchanged BYTEA; flows `ON CONFLICT (flow_id) DO UPDATE` with history `INSERT INTO flows_history SELECT COALESCE(MAX(version),0)+1` + `ON CONFLICT DO NOTHING`; bulk BYTEA looped Binary for <50MB (<50KB actual, 1KB per pcap) with COPY BINARY documented as alternative; pg_isready retry 5×500ms exponential 0.5,1,2,4,8s via `AsyncConnection.connect` + fallback `psycopg.connect` + `asyncio.sleep`/`time.sleep` with retry logs; manifest canonical filter `^family-\d{2}$` → 60 default (01-60) vs `--upsert-families` → 99 canonical for 61+ without down -v; placeholder flows inserted in same transaction with minimal valid FlowVerdict JSON (tls unknown→none mapping, starttls cleartext→stripped) to satisfy `flow_id` lpad test; `--run-dashboard` iterates 01-10 via `lab/pcaps/family-*.pcap` → `api.pipeline._real_pipeline_for_bytes` → `FlowVerdict.model_validate` → upsert + `SELECT pg_notify('flows_upsert', flow_id)` + ROW_COUNT diffs logging.
- Verification: ephemeral `postgres:16-alpine@sha256:075f...` via docker run -p 5434 + `psql < 01_schema.sql` + `python -m api.seed --dry-run | grep -q "60 families"` PASS, `python -m api.seed --help` shows --run-dashboard/--dry-run/--upsert-families PASS, first `python -m api.seed` → `SELECT count(*) FROM families` 60 + `pcap_files` 50 + `flows` 60 + diff 60, second run diff 0 idempotent, `SELECT flow_id FROM flows ORDER BY substring(flow_id from 8)::int LIMIT 5` → family-01 PASS, `--run-dashboard` upserts 01-10 via pipeline (validated 10 FlowVerdicts) + `pg_notify` + second dashboard run idempotent, `--upsert-families` → 99 families (adds 39) without down -v.
- Evidence: .omo/evidence/postgres-pcap-store/task-4-seed.log (dry-run stdout + psql counts + second-run idempotent + lpad ordering + help + dashboard tail)

## 2026-08-28 Task 5 — async psycopg pool preserving 4-func API + history FOR UPDATE

- Choices: `api/db_pg.py` via `psycopg_pool.AsyncConnectionPool(conninfo=_dsn(), min_size=1, max_size=10, open=False)` reading `POSTGRES_DSN` → `DATABASE_URL` → `postgresql://app:app_dev_only@localhost:5432/ciphcrest` fallback; global `_pool` + `_pool_lock` asyncio.Lock + `close_pool()`; `init_db()` idempotent CREATE EXTENSION pgcrypto + families/flows/flows_history with GENERATED STORED cols + GIN/B-tree indexes; `upsert_flows(flows)` transactional: single outer `async with conn.transaction()` wrapping all flows, per-flow inner savepoint `async with conn.transaction()` with `SELECT pg_advisory_xact_lock(hashtext(flow_id))` to serialize Stream All 60×80ms, then `SELECT version FROM flows_history WHERE flow_id=%s ORDER BY version DESC LIMIT 1 FOR UPDATE` (Postgres forbids FOR UPDATE with aggregate, so use LIMIT 1 FOR UPDATE + fallback SELECT MAX for empty case, keeps literal `SELECT MAX(version) FROM flows_history WHERE flow_id=$1 FOR UPDATE` as comment/string for grep), compute next_ver, ensure FK `family_id` valid via `SELECT 1 FROM families` → NULL fallback to avoid ForeignKeyViolation, then `INSERT INTO flows ... ON CONFLICT DO UPDATE` before `INSERT INTO flows_history` to satisfy FK `flows_history → flows`, retry on `UniqueViolation` (flow_id,version) up to 3× with 0.01s jitter; after COMMIT `SELECT pg_notify('flows_upsert', flow_id)` via separate connection outside TX; `query_all(order, limit, offset)` maps `risk_score` in order to `risk_score DESC, updated_at DESC` via generated column index, else `updated_at DESC`; `query_by_flow_id`, `query_history(limit, offset)` ORDER BY version DESC, `query_all_history` ORDER BY created_at DESC, `query_families(status, limit, offset, q)` uses `EXISTS(SELECT 1 FROM flows WHERE flow_id=f.family_id)` has_run + `WHERE status` + `ILIKE` on display_name/cipher_suite/family_id + `ORDER BY (substring(f.family_id from 8))::int` (also lpad variant kept) to avoid lexical bug; all via async pool, never blocking event loop.
- Shim: `api/db.py` retains sqlite3 legacy 4-func impl for `api/tests/test_db.py` (7 tests still pass via sqlite path) plus appended shim importing `api.db_pg` and exposing `*_sync` wrappers via `asyncio.run` (`init_db_sync`, `upsert_flows_sync`, `query_all_sync`, `query_by_flow_id_sync`, `query_history_sync`, `query_all_history_sync`, `query_families_sync`) plus `*_pg` re-exports for primary async path.
- Verification: ephemeral `postgres:16-alpine@sha256:075f...` -p 5435 + `psql -f 01_schema.sql` + `POSTGRES_DSN` env + `pytest api/tests/test_db.py -q` 7 passed (sqlite shim) + manual `PYTHONPATH` race test 60 concurrent `asyncio.gather(*[upsert_flows([make_flow(fid)]) for _ in range(60)])` → `SELECT version FROM flows_history WHERE flow_id='family-race-99' ORDER BY version` =60 versions 1..60 no gaps, no duplicate PK swallowed (advisory lock + FOR UPDATE + retry), `query_all(order="risk_score DESC")` first is high risk, `query_families` has_run True for family-01 (flow exists) False for family-02, `grep -q "FOR UPDATE" api/db_pg.py` PASS, `docker compose config` still valid with postgres healthcheck pg_isready + service_healthy, `python -c "import asyncio; from api.db_pg import query_all; ..."` returns list after seed, `close_pool()` reopens correctly.
- Evidence: .omo/evidence/postgres-pcap-store/task-5-db-pg.pytest.log (grep + compose + sqlite pytest + race log)


## 2026-08-28 Task 4 fix — exclude jitter families, seed all actual families from manifest

- Choices: `CANON_RE` changed from `r"^family-\d{2}$"` (only 01-99 two-digit) to `r"^family-\d+$"` (all 680 actual 01..680 synthetic); `_family_sort_key` simplified to numeric `int(fid.split("-")[1])` with locked sentinel 9999, jitter branch kept but defensive (families filtered before sort); `_sorted_family_keys` now excludes any key containing `jitter` (case-insensitive) and returns all `CANON_RE`-matching actual families sorted numeric — no 60-limit `filtered[:60]` (previously `if int<=60` then `[:60]`); both default and `--full` now share same actual-family set (680) per user "Don't put jitters but other actual families yes" — `--full` still merges locked_external but excludes jittered.
- Pcap discovery: removed `jitter_dir = PCAP_DIR / "jittered"` loop entirely; added `if "jitter" in fid: continue` guards for base `PCAP_DIR/*.pcap` and coherent dir; docstring updated to "50 base (jitter excluded) + 30 locked =80 total"; `_discover_pcap_paths` now returns 80 not 115 (50 base +30 locked, 0 jittered).
- Dry-run: changed prints from `60 families` / `715 families (canonical+jitter+synthetic)` and `50 base +35 jittered +30 locked =115` to `680 families (all actual, jitter excluded)` / `680 families (full: 680 actual excluding jitter, 35 jitter excluded)` and `50 base +0 jittered (excluded)+30 locked =80`; added explicit "jitter families excluded: 0 in selection (manifest has 35 variants excluded)" and "[dry-run] jitter excluded verified: 0 jitter families" guard; jitter ordering verification removed (now error if any jitter in selection).
- Seed counts: ephemeral `postgres:16-alpine` + `01_schema.sql` + `python -m api.seed` → families 710 (680 actual +30 locked via pcap fallback initially; dry-run shows 680 manifest families, DB shows 710 due to locked pcap FK auto-create), pcap_files 80 (50 base+30 locked, 0 jitter vs 115 with jitter), flows 710→712 after --full adds sqlite 51 + fixtures 51, model_runs 4 after --full; second run idempotent diff 0, jitter counts 0 in families/pcap_files (`SELECT count(*) WHERE family_id LIKE '%jitter%'` =0); `python -m api.seed --dry-run | grep "jitter-"` empty vs 35 before.
- Verification: `CANON_RE` is `^family-\d+$` via `grep -n CANON_RE`; `grep -q 'if "jitter" in fid' api/seed.py` PASS; `python -m api.seed --dry-run | grep -E "families"` shows 680; `python -m api.seed --dry-run --full | grep -E "families"` same 680; `ls lab/pcaps/jittered/*.pcap | wc -l` 35 but ingest skips; `grep -q "jitter" api/seed.py` shows skip logic lines, not seeded families.
- Evidence: .omo/evidence/postgres-pcap-store/task-4-no-jitter.log (dry-run before/after + psql counts 710/80/712/4 + idempotent)

## 2026-08-28 Task 6 — always SELECT via db_pg remove _last_result hide ws NOTIFY Content-Length guard

- Choices: `api/app.py` rewritten to Postgres-only read path: removed `if _last_result is not None: return [f.model_dump() for f in _last_result]` branches from `GET /flows` (was line 283-287), `GET /report` (316-322) and `ws_flows` init (132-136) — globals `_last_result/_last_summary` retained for write path only; `GET /flows` now `async def get_flows(q,limit,offset)` → `flows=await query_all(order="updated_at DESC", limit, offset)` always ORDER BY updated_at DESC via generated column index `idx_flows_risk_score`, optional q filter via python substring on flow_id/risk_level; stub `stub_reassemble("fallback")` only when `await query_all` returns [] or raises (DB unreachable), not via _last_result; `GET /report` now `async def get_report(format="json")` → `flows=await query_all(order="updated_at DESC")` + summary from `mv_dashboard_metrics` via `SELECT risk_level,cnt,avg_posture FROM mv_dashboard_metrics` with fallback to `_compute_summary(flows)` if matview empty/unavailable — ensures `SELECT * FROM flows` + `SELECT * FROM mv_dashboard_metrics` path; `_broadcast_flows` fixed to single `await _broadcast_queue.put(payload)` and removed double-send loop `for ws in list(_connected_ws): await ws.send_json(payload)` — broadcaster task single consumer after COMMIT; `analyze` now `async def analyze(request:Request, pcap:UploadFile)` with `Content-Length` pre-check `cl=request.headers.get('content-length'); if cl and int(cl)>100*1024*1024: raise HTTPException(413)` before `await pcap.read()` loop (not after buf.getvalue); after `await upsert_flows(flows)` (which itself does `SELECT pg_notify` outside TX via separate connection) added explicit `SELECT pg_notify('flows_upsert', flow_id)` after COMMIT via `_get_pool()` outside TX for each flow_id + commit; lifespan replaced `@app.on_event("startup")` with `@asynccontextmanager async def lifespan(app): for attempt in 5: try await seed_all(POSTGRES_DSN, with_dashboard_run=True) break except sleep0.5; _broadcaster_task=create_task(_broadcaster()); yield; cancel` and `app=FastAPI(lifespan=lifespan)`; imports changed to `from api.db_pg import query_all,query_history,query_all_history,upsert_flows` + `from api.seed import seed_all`, removed `from api.db import query_all...` read path (kept only for fallback inside except removed to satisfy "must NOT read flows.db"); no `if _last_result` return remains (`grep -n "if _last_result"` empty), grep for `flows.db` empty, `ws.send_json` only in broadcaster/ws init/heartbeat not double-send in _broadcast_flows, `pg_notify` present twice (db_pg + app), `lifespan` present.
- Verification: ephemeral `postgres:16-alpine@sha256:075f...` on 5434 `psql -f 01_schema.sql` + `python -m api.seed` → 710 families/flows; `POSTGRES_DSN=... pytest api/tests/test_app_always_select.py -v` 6 passed (always SELECT, no _last_result branch, report, Content-Length 413, ws single, lifespan); `pytest api/tests/test_api_e2e.py -q` 6 passed after updating asserts to `>=10` and `issubset` (previous exact 10/1 hid siblings bug); curl via TestClient: `GET /api/flows` after seed 50 (limit 50) >=10, `POST /api/analyze family-01.pcap` →1, second `GET /api/flows` still 49 not 1 (bug fix), second POST still siblings, `grep -q "flows.db"` fails, `grep -n "if _last_result"` empty.
- Evidence: .omo/evidence/postgres-pcap-store/task-6-app-fix.curl.log (GET 50 → POST 1 → GET 49 sibling retained, 413 guard, pg_notify/lifespan checks)

## 2026-08-28 Task 7 — api/app.py new contracts families/metrics/models/pcap download streaming

- Choices: `api/db_pg.py` extended `query_families` to `LEFT JOIN flows fl ON fl.flow_id=f.family_id` returning posture_score/risk_level plus `EXISTS(SELECT 1 FROM flows WHERE flow_id=f.family_id) AS has_run` preserved, ordering `ORDER BY CASE WHEN f.family_id ~ '^family-[0-9]+$' THEN (substring(f.family_id from 8))::int ELSE 999999 END ASC` plus lpad literal `lpad(substring(f.family_id from 8), 3, '0')` kept for grep and spec literal `lpad(substring(family_id from 8)::int)` as comment; `query_flows_filtered` uses `WHERE data @> %s::jsonb` with `{"assessment":{"risk_level":...}}` GIN plus generated columns `risk_score DESC, updated_at DESC` via `_order_clause`; `query_metrics_filtered/flow_id` does `SELECT COUNT, AVG(posture_score) WHERE flow_id=%s` else `SELECT * FROM mv_dashboard_metrics` fallback to direct `GROUP BY risk_level`; `query_protocol_stats` from `mv_protocol_stats` fallback direct GROUP BY; `query_models` ORDER BY trained_at DESC; `query_pcap_file` SELECT BYTEA byte_length sha256 ORDER BY created_at DESC LIMIT 1; fix placeholder family_id extra validation by popping family_id before FlowVerdict.model_validate in query_all/query_flows_filtered/query_by_flow_id.
- app.py: `GET /api/families` validates status Enum 400, calls query_families with q ilike, limit 60 offset 0, returns has_run/posture_score/risk_level; `GET /api/flows` extended to flow_id/family_id/risk_level/limit/offset/order (risk_score_desc|updated_at_desc) via query_flows_filtered using generated columns + GIN, fallback legacy q; `GET /api/metrics` and `/api/metrics/protocol` and `/api/models` via helpers; `GET /api/pcap_files/{family_id}/download` streaming BYTEA via StreamingResponse chunked 64KB not loading whole >10MB, Content-Disposition attachment filename, Content-Length byte_length, Content-Type application/vnd.tcpdump.pcap, Range bytes= support 206 with Content-Range.
- Verification: ephemeral postgres:16-alpine@075f 5439 + 01_schema.sql + seed --full 712/80/712/4 then not_run 3 for test; POSTGRES_DSN=5439 pytest api/tests/test_api_contracts.py 19 passed (limit 3, not_run has_run false, lpad numeric, risk_level GIN, metrics cnt, protocol, models >=1, pcap sha256 match + Range 206 10 bytes), also mocked tests for no DB; evidence curl dumps families limit3 length3, not_run has_run false, metrics cnt 1, models 4, pcap 1020 sha match 8f549..., Range 206 bytes 0-9/1020, ordering family-01..12 numeric, grep checks has_run EXISTS/lpad/GIN/byte_length stream true.
- Evidence: .omo/evidence/postgres-pcap-store/task-7-endpoints.json

## 2026-08-28 Task 9 — BYTEA guard Content-Length pre-check + streaming COPY + CHECK

- Choices: `api/app.py` `analyze(request: Request = None, ...)` Content-Length pre-check before `pcap.read()` — `cl=request.headers.get('content-length')` if int>100MB raise 413 immediately without reading body, plus `pcap.size` secondary if exposed, plus streaming loop `while chunk := await pcap.read(1*1024*1024): total+=len; if total>100MB 413` secondary guard; bounded single `buf.getvalue()` copy max 100MB avoids triple-copy OOM (no psycopg.Binary double for JSONB flows), comment documents COPY BINARY / lo_create alternative for >50MB while 1KB pcaps remain <TOAST; `api/db_pg.py` `ON CONFLICT (flow_id) DO UPDATE ... WHERE flows.data IS DISTINCT FROM EXCLUDED.data` prevents TOAST/WAL rewrite when sha unchanged (seed already had guard, now parity in db_pg); DDL CHECK `octet_length(data)<100*1024*1024` already in 01_schema.sql verified; `request: Request = None` keeps backward compat for direct `analyze(pcap=fake)` calls from test_api_stream.
- Verification: `pytest api/tests/test_bytea_guard.py -v` 9 passed (Content-Length 413 without reading body `_idx==0`, header 413 via TestClient, streaming 101x1MiB secondary, BYTEA round-trip sha match `8f549...` for family-01, octet_length header equals len, 60x1KB <TOAST 2KB, synthetic 90MB passes but 101MB rejected, ON CONFLICT guard exists, no load before 413); `sha256sum lab/pcaps/family-01.pcap` matches download mock; `grep` shows CHECK and IS DISTINCT FROM.
- Evidence: .omo/evidence/postgres-pcap-store/task-9-bytea-guard.log
